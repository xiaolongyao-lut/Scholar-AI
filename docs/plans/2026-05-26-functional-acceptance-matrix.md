# 功能验收矩阵 (Functional Acceptance Matrix)

## 范围与说明

**数据源**: `docs/plans/active/` 全量 (70+ plans)  
**粒度**: 每个 task / slice 一行  
**状态**: 每行按当前证据记录；可能是静态定位、自动化测试、构建验证或浏览器验收，未完成项必须保留待验收说明
**状态口径**: ✅ 已实现（静态定位）= 当前代码能对上前端 + 后端/API 链路；⚠️ 部分实现/契约不一致 = 只有一侧、路径/字段不同、范围缩水或需要开关；❓ 未定位/待验证 = 未找到足够当前代码证据  
**优先级口径**: P0 = 文献助手核心闭环必须做；P1 = 对核心体验明显增益，应排入近期；P2 = 可延后，不阻塞主闭环；P3 = 暂缓或偏发布/运维  
**基础设施口径**: API 凭证、模型路由、MCP 协议兼容、工具审批、Agent 权限隔离、Skill 沙箱、Wiki 权限属于 AI 可用性/安全基础设施；即使看起来偏生态，也应按 P0/P1 优先考虑  
**执行方口径**: Claude 负责后端、数据模型、API 契约、OpenAPI/类型客户端、接口测试；Codex 负责前端 UI、交互逻辑、布局、浏览器验收；Claude→Codex 表示先由 Claude 补齐接口，再由 Codex 接 UI
**接续队列口径**: A-K 是当前功能矩阵主队列；A-K 中所有 P0/P1 行完成、阻塞并记录、或被明确降级后，执行者不得停顿，必须继续执行 L 组“项目级思维偏置注入计划”。

---

## A. 检索内核 (RAG / TOLF / Rerank / 证据链)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| A1 | Rerank Stage0 验证 | BGE API ≈ Local (ρ=0.951)，material-dedup 白拿收益 | 无前端 UI | `test_rerank_stage0.py` 通过 | rerank-stage0-findings | 可延后/P2 | Claude(后端/接口) | ✅ 已完成本地离线 Stage0 验证闭环 (2026-06-05)：新增 `tools/eval/evaluate_rerank_stage0.py`，读取 frozen JSONL score rows，按 query 计算 local-vs-reference Spearman rho、nDCG@K、chunk recall@K、material recall@K、unique materials@K 和 material-dedup delta；固定 `external_api_calls: 0`，不读取真实凭据、不调用 BGE/provider、不执行 promotion。新增矩阵点名测试 `tests/test_rerank_stage0.py`，用内联 frozen scores 证明 local/reference rho > 0.95、baseline 相关性更弱、material-dedup 提升相关材料覆盖，并覆盖 CLI report 写出。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_rerank_stage0.py tests\test_run_pinned_rerank_manifest.py -q -o addopts=""` 22/22 passed；`.\.venv-1\Scripts\python.exe -m compileall -q tools\eval\evaluate_rerank_stage0.py tests\test_rerank_stage0.py` passed；`git diff --check -- tools\eval\evaluate_rerank_stage0.py tests\test_rerank_stage0.py ...` passed（仅矩阵既有 CRLF warning）。成熟方案核对：TREC-style offline IR metrics (nDCG/Recall)、Spearman rank correlation 口径、现有 pinned rerank manifest dry-run guard。Rollback：checkpoint `20260605-130113-a1-rerank-stage0-local-eval-preedit`，实体快照 `.rollback_snapshots/a1-rerank-stage0-local-eval-preedit-20260605_130114`。残余风险：没有复现历史真实 BGE API ≈ Local `ρ=0.951` 原始产物；当前闭合的是本地可重复 Stage0 evaluator/test harness，真实 provider correlation/promotion 仍需显式授权和凭据边界。 |
| A2 | 本地 Rerank 主线化 | 默认启用本地 rerank，Settings 可切换 | Settings → 语义路由/功能开关可配置 rerank | `feature_flags.py` ENABLE_LOCAL_RERANK=True | experimental-mainline-promotion E1 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并浏览器验收 (2026-05-28)：`feature_flags.py:67-78` 注册 `local_rerank`，默认 `true`，沿用矩阵旧 env 名 `ENABLE_LOCAL_RERANK` 作为环境覆盖；`feature_flags_router.py:46-58` 现有 `/api/feature-flags` 自动暴露该开关；Settings 语义路由页已有 `RerankCard` 调 `/api/rerank/config` 保存本机/云端 rerank 服务，功能开关页 `Settings.tsx:2367-2370` 增加“本地 Rerank 主线”用户可读文案且不泄漏 env 名。验证：`pytest tests/test_feature_flags.py tests/test_reranker.py::test_rerank_config_router_endpoints -q` 12/12 passed，覆盖默认开启、`ENABLE_LOCAL_RERANK=0` env 覆盖和 rerank config 掩码往返；`compileall` passed；`npm run build` passed；API smoke `GET /api/feature-flags` 返回 `local_rerank True True ENABLE_LOCAL_RERANK`；浏览器 smoke `A2_FRONTEND_PORT=5317 node workspace_artifacts/runtime_state/a2-local-rerank-feature-flag-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/a2-local-rerank-feature-flag-20260528.png`。成熟方案核对：沿用本仓 feature-flag registry 的默认值 + env override + runtime override 模式，并按 FastAPI 官方 `APIRouter` response model 模式暴露。Rollback：checkpoint `20260528-045354-a2-local-rerank-mainline-flag`，矩阵实体快照 `.rollback_snapshots/matrix-a2-20260528_045623`。残余风险：该开关目前是 Settings/回滚可见契约；rerank 执行路径仍按现有 `reranker_client` 配置和无凭证 fallback 工作，未新增强制调用本地服务或真实模型连通性测试。 |
| A3 | TOLF 跨语言桥接修复 | hash-based embedding 改为语义 embedding，跨语言对齐 | 无前端 UI | 中英文检索 NDCG 提升 | tolf-bridge-expansion-fix (memory) | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并冻结 qrels 评估 (2026-05-28)：新增 `tools/eval/evaluate_tolf_bilingual_qrels.py`，按 TREC/qrels 风格读取 `query_id/chunk_id/relevance`，计算 NDCG@K 与 Recall@K，并复用现有 raw default / bilingual-control / TOLF comparison，不做真实 provider 调用也不伪造指标；冻结 goldset 位于 `workspace_tests/evaluation_scripts/tolf_bilingual_queries_20260528.jsonl`、`tolf_bilingual_chunks_20260528.jsonl`、`tolf_bilingual_qrels_20260528.jsonl`。评估产物：`workspace_artifacts/generated/output/tolf-bilingual-qrels-eval-20260528.json`，raw default NDCG@3/Recall@3 = `0.0/0.0`，bilingual-control = `0.97244/0.833333`，TOLF = `0.47244/0.5`；这证明跨语言 bridge 召回和排序证据已可重复量化，同时暴露 TOLF graph ranking 仍弱于 bilingual-control。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_tolf_text_selector.py tests\test_pipeline_tolf_embedding_alignment.py tests\test_compare_tolf_context_selector.py tests\test_evaluate_tolf_bilingual_qrels.py -q -o addopts=""` 22/22 passed；`compileall` passed；`git diff --check` passed。成熟方案核对：采用 TREC-style qrels、NDCG@K、Recall@K 的离线 IR 评估口径；保留 `external_api_calls: 0`。Rollback：`.rollback_snapshots/matrix-a3-tolf-qrels-20260528_182607`。残余风险：当前完成的是冻结小 goldset 的跨语言语义桥接证据，不等于大型真实语料 provider embedding benchmark；TOLF 排名仍需后续优化。 |
| A4 | Evidence refs 路由 | 证据引用支持 source_labels 过滤 | 无前端 UI (后端 API) | `/api/evidence_refs` 返回 filtered results | TASK-202~229 (master plan) | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-27)：`python_adapter_server.py:640` 引入 `evidence_router`，`:706` 注册路由；`evidence_router.py:38-75` 提供 `GET /api/evidence_refs`，支持 `material_id`、`source_labels`、分页过滤，`:95-120` 提供 `POST /api/evidence_refs` 写入 `EvidenceRefPayload`，保留 `bbox/page/source_labels`；`models/evidence.py:35-57` 定义响应模型。验证：`pytest tests/test_evidence_refs_api.py -q` 10/10 passed，覆盖路由存在、创建 evidence ref、按 `source_labels=method` 过滤且排除非匹配 ref、bbox 序列化。成熟方案核对：FastAPI 官方 `TestClient` 路由测试模式 + Pydantic `Field(default_factory=list)` 字段默认模式。Rollback：checkpoint `20260527-234703-20260527-a4-a5-e6-evidence-refs-api-verification`，矩阵快照 `.rollback_snapshots/matrix-a4-a5-e6-20260527_235508`。残余风险：当前为进程内 store，不是持久化 DB；生产持久层需另列后端 slice。 |
| A5 | Source labels 持久化 | 用户标注的 source 标签存入 DB | 无前端 UI | `source_labels` 表 CRUD | TASK-210 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-27)：`evidence_router.py:127-193` 提供 `GET/POST /api/source_labels` 与 `GET/PUT/DELETE /api/source_labels/{label_id}`，`models/evidence.py:8-27` 定义 `SourceLabelPayload/CreateSourceLabelRequest/UpdateSourceLabelRequest`；`python_adapter_server.py:706` 注册后成为应用级 API。验证：`pytest tests/test_evidence_refs_api.py -q` 10/10 passed，覆盖 source label create/update/delete/get 404 与模型导出。成熟方案核对：FastAPI 官方 `TestClient` 路由测试模式 + Pydantic 字段模型。Rollback：checkpoint `20260527-234703-20260527-a4-a5-e6-evidence-refs-api-verification`，矩阵快照 `.rollback_snapshots/matrix-a4-a5-e6-20260527_235508`。残余风险：矩阵原期望写 `source_labels` 表；当前闭合的是 API CRUD 和过滤契约，仍未接真实 DB 持久化。 |
| A6 | Goldset v3.1 L1 Active | n=50 canary, R@5=0.86 NDCG@10=0.60, per_difficulty floors | 无前端 UI | `goldset_v3_promotion.md` 验收通过 | goldset-v3-promotion (memory) | 可延后/P2 | Claude(后端/接口) | ✅ 已完成本地只读 readiness 审计闭环 (2026-06-05)，但旧 promotion 记忆指标不被当前 artifact 证明：新增 `tools/eval/check_goldset_v31_readiness.py` 与 `tests/test_goldset_v31_readiness.py`，只读检查 `workspace_tests/evaluation_data/eval_queries_v3.1_canary50_ALIGNED.jsonl` 50 queries、`canary50_goldset_v3.1.jsonl` 509 qrels / 47 non-`no_gold` query 覆盖、两份既有 metrics artifact，并固定 `external_api_calls: 0`、不读凭据、不调用 provider、不修改 goldset/qrels/query、不执行 promotion。真实本地报告 `workspace_artifacts/evaluations/a6-goldset-v31-local-readiness-20260605.json` 状态为 `LOCAL_GATE_PASS_LEGACY_PROMOTION_MISMATCH`：当前 post-drift gate `recall_at_5 >= 0.84`、`mrr >= 0.6857`、hard R@5 `>=0.7778`、medium R@5 `>=0.94` 均通过；旧矩阵/记忆口径 `R@5=0.86` 未满足（当前 0.84），`NDCG@10=0.60` 未被 2026-05-15 metrics emitted。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_goldset_v31_readiness.py tests\wiki\test_canary_goldset_drift.py -q -o addopts=""` 10/10 passed；`.\.venv-1\Scripts\python.exe -m compileall -q tools\eval\check_goldset_v31_readiness.py tests\test_goldset_v31_readiness.py` passed；CLI report 生成 passed。成熟方案核对：NIST/TREC qrels/IR metrics、Google SRE canary metric attribution、scikit-learn holdout evaluation principle；本仓 replacement-gate plan 已明确 NDCG 是 record-only、当前 hard floors 是 R@5/MRR。Rollback：checkpoint `20260605-131002-a6-goldset-v31-local-readiness-preedit`，实体快照 `.rollback_snapshots/a6-goldset-v31-local-readiness-preedit-20260605_131001`。残余风险：没有 provider-backed rerun、没有 public release/readiness 声明、没有 `goldset_v3_promotion.md` 物化；若要恢复旧 R@5/NDCG promotion 口径，需要显式授权 provider eval 或补 NDCG 计算/评估计划。 |
| A7 | Chunk-page locator L0 | 前端传 chunk_id，后端返回 PDF page + bbox | 无前端调用 (API ready) | `/api/chunk_to_page` 200 OK | chunk-page-locator L0 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：保留现有 `GET /resources/chunks/{chunk_id}/locator`，新增兼容入口 `GET /api/chunk_to_page?project_id=&chunk_id=`；`resources_router/endpoints_search_upload.py:326-360` 的 `find_chunk_locator()` 仍只读 `_load_chunk_store()`，返回 `material_id/chunk_id/page/chunk_index`，并仅在 chunk 元数据已有合法四数数组时透传 `bbox`；`evidence_router.py:201-278` 将 `/api/chunk_to_page` 与 `/api/chunks/{chunk_id}/locator` 接到同一 store 查询 helper；`models/evidence.py:64-72` 将 `ChunkLocatorPayload` 补齐 `chunk_index/bbox`。验证：`pytest tests/test_resources_chunk_locator.py -q` 23/23 passed，覆盖 bbox 透传、非法 bbox 忽略、alias 200/404/422、locator 不调用 `_save_chunk_store`/`_ensure_project_chunks`；`pytest tests/test_evidence_refs_api.py tests/test_resources_chunk_locator.py -q` 33/33 passed；`compileall` passed；package app 路由检查确认 `/api/chunk_to_page`、`/api/chunks/{chunk_id}/locator`、`/resources/chunks/{chunk_id}/locator` 都已注册；`git diff --check` passed。成熟方案核对：FastAPI 官方 `APIRouter` + `Query` 参数校验 + `response_model` 序列化/过滤模式。Rollback：checkpoint `20260528-044730-a7-chunk-to-page-alias`，矩阵实体快照 `.rollback_snapshots/matrix-a7-20260528_045136`。残余风险：本 slice 不做 PDF 原始坐标推断，只透传已归一化四数 bbox；真实 bbox 生成仍依赖上游 chunk/PDF layout 提取。 |
| A8 | Chunk-page locator L1 | PDF Reader 高亮定位到 chunk 位置 | PDF Reader 显示高亮框 | 前端调 `/api/chunk_to_page` + 渲染 | chunk-page-locator L1 | 是/P0 必做 | Codex(UI) | ✅ 前端 bbox deep-link 与 PDF overlay 已完成并浏览器验收 (2026-05-28)：`resourcesApi.ts:21-63` 将 locator 响应扩展为可选 `bbox` 并防守解析四数数组；`EvidencePill.tsx:24-33` 编码 bbox，`:113-136` 在证据点击 URL 中保留 `page/chunk/bbox`，locator 缺页时仍按原逻辑查页码；`SparkEvidencePills.tsx:42-47` 将 Inspiration evidence ref 的 bbox 透传到通用证据 pill；`ResearchWorkbench.tsx:38-58` 解析 `bbox=` 查询参数，支持归一化 `[x,y,w,h]` 或 `[x0,y0,x1,y1]`，`:153-165` 生成“当前跳转证据位置”临时高亮并传给 `PdfReaderShell`；`PdfViewer.tsx:798-830` 现有 rect overlay 按页渲染。验证：`npm run test -- src/services/resourcesApi.test.ts src/components/evidence/EvidencePill.test.tsx src/components/PdfViewer/PdfViewer.test.tsx --run` 33/33 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；浏览器 smoke `node .\workspace_artifacts\runtime_state\a8-c11-pdf-bbox-highlight-smoke.mjs` 通过，打开 `/workbench/paper/mat-a8-c11?page=2&chunk=chunk-a8-c11&bbox=0.18,0.22,0.34,0.12` 后 PDF 第 2 页出现蓝色 overlay，截图 `workspace_artifacts/screenshots/a8-c11-pdf-bbox-highlight-20260528.png`。成熟方案核对：react-pdf 官方 `Document`/`Page` + Text/AnnotationLayer CSS 模式、PDF.js 页面 viewport/overlay 分层实践；沿用项目已有归一化 highlight rect 契约。Rollback：checkpoint `20260528-011117-20260528-a8-c11-pdf-bbox-highlight-ui`，矩阵快照 `.rollback_snapshots/matrix-a8-c11-20260528_015845`。残余风险：后端 canonical `/resources/chunks/{chunk_id}/locator` 当前多数仍只返回 `page/chunk_index` 且无真实 `bbox`；本次前端仅保证有归一化 bbox 时可高亮，PDF 原始坐标 bbox 仍需后端统一坐标归一化或返回页面尺寸后再渲染。 |
| A9 | Chunk-page locator L2 | 智能研读引用点击跳转 PDF | 引用气泡点击 → PDF Reader 跳页 | 前端路由 + locator API | chunk-page-locator L2 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：Dialog 统一智能研读继续复用 `Conversation`/`MessageRenderer`/`EvidencePill`，`EvidencePill.tsx:104-137` 保留默认点击跳 `/workbench/paper/:materialId?page=&chunk=&bbox=` 且可从 locator 补页码/bbox；`ResearchWorkbench.tsx:80,355-358` 将同文献 deep-link 的 `page/chunk/bbox` 纳入 `PdfReaderShell` key，并让 URL page 优先于持久阅读页，保证从 Dialog 进入 PDF Reader 后实际跳到目标页；`ResearchWorkbench.tsx:159-170` 用 bbox 生成“当前跳转证据位置”临时蓝色 overlay。验证：`npm run test -- src/components/evidence/EvidencePill.test.tsx src/components/chat/Message.test.tsx src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 22/22 passed；浏览器 smoke `A9_A10_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/a9-a10-smartread-evidence-jump-smoke.mjs` passed，断言 `/dialog` 证据 pill 点击进入 `/workbench/paper/mat-a9-a10?page=2&chunk=chunk-a9-dialog&bbox=0.2,0.24,0.36,0.14`，PDF 第 2 页出现 overlay；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；截图 `workspace_artifacts/screenshots/a9-a10-smartread-evidence-jump-20260528.png`。成熟方案核对：React 官方事件处理 props、React Router `useNavigate` deep-link、Playwright 官方 `page.route`/locator/screenshot smoke 模式。Rollback：checkpoint `20260528-080300-a9-a10-smartread-inspector-evidence-jump-smoke-c`，矩阵实体快照 `.rollback_snapshots/matrix-a9-a10-20260528_081344.md`。残余风险：后端 locator 若缺少归一化 bbox 仍只能 page/chunk 跳转；真实 PDF 原始坐标归一化仍属后端/提取链路。 |
| A10 | Chunk-page locator L3 | Workbench Inspector 引用跳转 | Inspector 引用 → PDF 跳页 | 同 L2 | chunk-page-locator L3 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：原静态判断存在缺口，Inspector 传入 `onSelectEvidence` 时会截获 `EvidencePill` 默认导航；现用 `navigateAfterActivate`/`navigateEvidenceAfterSelect` 显式分离“选中证据”和“继续跳 PDF”，`EvidencePill.tsx:80,104-113` 保留默认 `onActivate` 截获语义并允许 Workbench opt-in 继续导航，`MessageRenderer.tsx:98,131,179`、`Conversation.tsx:17,72,104`、`ResearchWorkbenchInspector.tsx:30,67,116,233-261` 逐层透传，`ResearchWorkbench.tsx:383-402` 仅在 Workbench Inspector/证据抽屉启用；同文献 deep-link 通过 `ResearchWorkbench.tsx:80,355-358` 重挂载 PDF reader 并以 URL page 优先。验证：`npm run test -- src/components/evidence/EvidencePill.test.tsx src/components/chat/Message.test.tsx src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 22/22 passed，新增覆盖 “select + navigate” 模式；浏览器 smoke `A9_A10_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/a9-a10-smartread-evidence-jump-smoke.mjs` passed，断言 Workbench Inspector 证据 pill 点击进入 `/workbench/paper/mat-a9-a10?page=3&chunk=chunk-a10-inspector&bbox=0.18,0.32,0.42,0.1`，第 3 页 overlay 可见且 pill `aria-pressed=true`；`npm run build` passed。成熟方案核对：React 官方自定义事件 handler props + React Router `useNavigate` + Playwright route/locator 验收模式。Rollback：checkpoint `20260528-080300-a9-a10-smartread-inspector-evidence-jump-smoke-c`，矩阵实体快照 `.rollback_snapshots/matrix-a9-a10-20260528_081344.md`。残余风险：当前验证使用本地 mock PDF 和 localStorage 会话；真实材料仍依赖后端返回可用 `material_id/page/bbox` 或 locator 数据。 |
| A11 | Chunk-page locator L4 | 写作系统引用跳转 | Writing Citations → PDF 跳页 | 同 L2 | chunk-page-locator L4 | 是/P0 必做 | Claude→Codex | ✅ 写作来源 → PDF Reader page/chunk/bbox 跳转已完成并浏览器验收 (2026-05-28)：`SourcesCitations.tsx:88-103` 从 `listProjectChunks()` 读取每个 material 首个 `chunk_id/page/bbox`，只接受归一化四数 bbox；`:384-388` 在来源卡片提供“在 PDF Reader 中打开文献”Link；`:626-645` 用 `URLSearchParams` 构造 `/workbench/paper/{materialId}?page=&chunk=&bbox=`；`resources.ts:80-89` 为 `ProjectChunkResource` 增加 `bbox` 类型；`ResearchWorkbench.tsx:38-58` 解析 bbox deep-link，`:153-164` 生成“当前跳转证据位置”临时高亮并交给 PDF overlay。验证：`npm run build` passed；浏览器 smoke `A11_H6_FRONTEND_PORT=5315 node workspace_artifacts/runtime_state/a11-h6-writing-sources-pdf-bbox-smoke.mjs` passed，断言 `/writing/sources` 链接 href 包含 `page=2&chunk=chunk-a11-h6&bbox=0.2,0.24,0.36,0.14`，点击后进入 `/workbench/paper/mat-a11-h6` 且 PDF 第 2 页出现蓝色 overlay；截图 `workspace_artifacts/screenshots/a11-h6-writing-source-pdf-bbox-20260528.png`。成熟方案核对：React Router 官方 `<Link>` 路由链接模式 + Web 标准 `URLSearchParams` 查询串构造；沿用 A8/C11 的 react-pdf/PDF.js overlay 分层实践。Rollback：checkpoints `20260528-020032-20260528-a11-h6-writing-source-pdf-bbox-jump`、`20260528-021801-a11-h6-writing-sources-pdf-bbox-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-a11-h6-20260528_023500`。残余风险：真实后端 `/resources/chunks` 若不返回归一化 bbox，则仍退化为 page/chunk 跳转；PDF 原始坐标 bbox 需要后端归一化后再开放。 |

---

## B. 智能研读 (SmartRead / Chat / IntelligentChat)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| B1 | Dialog 合并 A0 | 三入口统一路由到 `/dialog` | 顶栏"智能研读"单一入口 | 路由表移除 `/chat` `/intelligent-chat` | dialog-merge A0 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：`App.tsx:74-77` 统一将 `/chat`、`/inspiration`、`/intelligent-chat` 兼容入口用 React Router `Navigate replace` 重定向到 `/dialog`，顶栏仍以 `MainLayout` 的“智能研读”作为主入口；浏览器 smoke 打开 `/intelligent-chat` 后断言 URL 变为 `/dialog` 且显示统一“智能研读”Dialog。验证：`node ..\workspace_artifacts\runtime_state\b1-b2-dialog-merge-smoke.mjs` passed；`npm run build` passed；截图 `workspace_artifacts/screenshots/b1-b2-dialog-merge-20260527.png`。成熟方案核对：React Router 官方路由级 `Navigate` 重定向模式。Rollback：checkpoint `20260527-222039-b1-b2-dialog-merge-compat-smoke`；矩阵快照 `.rollback_snapshots/matrix-b1-b2-20260527_222422`。残余风险：旧 `IntelligentChat.tsx` 文件仍保留为未路由兼容代码，后续可单独清理。 |
| B2 | Dialog 合并 A1 | `Conversation` 组件统一渲染 | Dialog 页用 `Conversation` | `MessageRenderer` 替代 `MessageBubble` | dialog-merge A1 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：`Dialog.tsx:4` 引入 `Conversation`，`:709` 渲染统一对话组件，`Conversation` 内部走 `MessageRenderer`；`App.tsx:76` 让旧 `/intelligent-chat` URL 不再进入旧页面。验证：`node ..\workspace_artifacts\runtime_state\b1-b2-dialog-merge-smoke.mjs` passed，证明旧 URL 落到 `/dialog` 且显示统一 Dialog；`npm run build` passed；截图 `workspace_artifacts/screenshots/b1-b2-dialog-merge-20260527.png`。成熟方案核对：React Router 官方路由级 `Navigate` 重定向模式。Rollback：checkpoint `20260527-222039-b1-b2-dialog-merge-compat-smoke`；矩阵快照 `.rollback_snapshots/matrix-b1-b2-20260527_222422`。残余风险：`IntelligentChat.tsx:4/385` 仍引用 `MessageBubble`，但当前已无活动路由指向它；删除旧文件需另列兼容清理 slice。 |
| B3 | Dialog 合并 A2 | 后端 `/api/chat` 统一入口 | 前端只调 `/api/chat` | `/chat/ask` 标记 deprecated | dialog-merge A2 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`Dialog.tsx:485-535` 发送统一智能研读请求时只调用 `streamIntelligentChatMessage()`，`intelligentChatApi.ts:311-384` 将请求发往 `POST /api/chat/stream` 并解析 SSE；浏览器 smoke 显式拦截并禁止 `POST /api/chat` 与 `/chat/ask`，只允许 `/api/chat/stream` 成功，且请求体包含 `mode: literature_qa`、活动 `project_id` 和由 Settings 默认档位映射出的 `tier`。验证：浏览器 smoke `B3_B4_B6_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/b3-b4-b6-dialog-stream-smoke.mjs` passed；`npm run test -- src/components/evidence/EvidencePill.test.tsx src/components/chat/Message.test.tsx src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 22/22 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；截图 `workspace_artifacts/screenshots/b3-b4-b6-dialog-stream-20260528.png`。成熟方案核对：MDN/HTML SSE `text/event-stream` 格式 + Playwright 官方 route fulfill/locator/screenshot 模式；本 slice 是本地 UI 契约验收，无新增依赖。Rollback：checkpoint `20260528-081657-b3-b4-b6-dialog-chat-stream-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-b3-b4-b6-20260528_082010.md`。残余风险：本次用 mock SSE 验证前端路由与请求体，不连接真实 LLM；后端 `/chat/ask` deprecated 状态仍由后端契约维护。 |
| B4 | SmartRead 流式输出 | SSE 流式返回，前端逐 token 渲染 | 输入框发送 → 逐字显示 | `/api/chat/stream` 200 OK | smart-read-streaming B4 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`intelligentChatApi.ts:333-384` 通过 `fetch` 读取 `ReadableStream`，按 `data: {...}\n\n` 解析 `metadata/text_delta/usage/done`；`Dialog.tsx:510-535` 将 `text_delta` 累加到 assistant 消息并在 `done` 后写入最终 response/tokens；`MessageRenderer.tsx:189-334` 渲染 tokens/context 等诊断信息。浏览器 smoke `b3-b4-b6-dialog-stream-smoke.mjs` mock 三段 `text_delta`，断言页面先后出现 `逐段`、`逐段流式`、最终 `逐段流式回答`，并显示 `10 tokens`，证明逐 token/分段渲染链路可见。验证：`B3_B4_B6_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/b3-b4-b6-dialog-stream-smoke.mjs` passed；`npm run build` passed；截图 `workspace_artifacts/screenshots/b3-b4-b6-dialog-stream-20260528.png`。Rollback：checkpoint `20260528-081657-b3-b4-b6-dialog-chat-stream-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-b3-b4-b6-20260528_082010.md`。残余风险：mock SSE 不覆盖真实网络中断/重连；真实 provider 错误仍依赖 `/api/chat/stream` 后端 error event 与现有前端错误分支。 |
| B5 | 成本档位统一 | low/medium/high/xhigh/max 五档 | Settings → API 凭证 → 默认档位 | `CredentialStrategyHint` enum 扩展 | settings-smartread-ui-followup tier slice | 是/P0 必做 | Claude | ✅ 已完成 (2026-05-26 commit b5b0ca3e + cc0da047)：`CredentialStrategyHint` 新增 LOW/MEDIUM/HIGH/XHIGH/MAX canonical 枚举 + DEFAULT/CHEAP/FAST/QUALITY legacy 枚举；`normalize_strategy_hint()` 映射 cheap→low, default→medium, fast→medium, quality→high, 未知→medium；`to_public()` 输出 canonical 值（存储保留原始，避免迁移）；13 测试覆盖 canonical/legacy/surface/defaults/case/whitespace/to_public 输出 (`tests/test_credentials_strategy_hint_mapping.py`)；代码：`literature_assistant/core/models/credentials.py:39-68` enum, `:173-217` normalize, `:356` to_public normalize；验证：`pytest tests/test_credentials_strategy_hint_mapping.py -v` 13/13 passed |
| B6 | 智能研读档位移除 | 提问界面不显示档位选择器 | Dialog 页无 `TierSelector` | 读取 Settings 默认档位 | settings-smartread-ui-followup tier slice | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`Dialog.tsx` 不再 import/render `TierSelector`，发送前在 `Dialog.tsx:440,452,489` 读取 `loadSmartReadCostTier('medium')` 并用 `backendTierForCostTier()` 映射后端 `tier`；`ChatInput.tsx:242-323` 只渲染附件、项目偏置 toggle、文本框和发送按钮，不显示 low/medium/high/xhigh/max 档位选择器。浏览器 smoke 在 localStorage 写入 `smart-read-cost-tier-v1=high` 后打开 `/dialog`，断言 `低/中/高/XHigh/Max` 档位按钮不存在，同时提交请求体 `tier === thorough`，证明 UI 已移除但仍读取 Settings 默认档位。验证：`B3_B4_B6_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/b3-b4-b6-dialog-stream-smoke.mjs` passed；`npm run build` passed。Rollback：checkpoint `20260528-081657-b3-b4-b6-dialog-chat-stream-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-b3-b4-b6-20260528_082010.md`。残余风险：该验收只覆盖 Dialog 主入口；Settings 内默认档位配置 UI 由 B5/I7/I8 相关行覆盖。 |
| B7 | ChatPipeline 统一 M-Slice 2 | `chat/pipeline.py` 统一 session/evidence 逻辑 | 无前端变化 | `build_chat_pipeline()` 单一入口 | fullstack-deduplication M-Slice 2 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口单测验收 (2026-05-28)：`chat/pipeline.py:137-194` 新增 `ChatPipelineFacade` 与 `build_chat_pipeline()` 稳定入口，聚合 mode resolution、turn planning、evidence record building，保持 `literature_qa`/`smart_read` 统一面；`intelligent_chat_router.py:41-66` 构造 `_CHAT_PIPELINE = build_chat_pipeline()`，`:672` evidence refs、`:1382` mode resolve、`:1474` 与 `:1775` turn plan 均通过该 facade，保留原 helper 兼容导出不破坏旧测试。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_smart_read_pipeline.py tests\test_intelligent_chat_router.py tests\test_chat_router_analysis_chain.py -q -o addopts=""` 50/50 passed（仅既有 `main_rag_workflow.py` utcnow deprecation warning）；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\chat\pipeline.py literature_assistant\core\routers\intelligent_chat_router.py tests\test_smart_read_pipeline.py` passed；`git diff --check -- literature_assistant\core\chat\pipeline.py literature_assistant\core\routers\intelligent_chat_router.py tests\test_smart_read_pipeline.py` passed。成熟方案核对：采用 facade/compatibility layer 成熟重构模式，先引入稳定入口并迁移高价值调用点，避免一次性重写整个 chat router。Rollback：checkpoint `20260528-114211-b7-chat-pipeline-entrypoint`，矩阵实体快照 `.rollback_snapshots/matrix-b7-chat-pipeline-entrypoint-20260528_114532.md`。残余风险：`chat.pipeline` 仍保留底层 helper 供旧调用和测试直接导入；`intelligent_chat_router.py` 在 dirty worktree 中已有大量历史改动，当前 slice 只新增 facade 使用点，未做全路由去重或 UI localStorage 迁移。 |
| B8 | localStorage 迁移 M-Slice 5 | 会话历史迁移到后端 DB | 无前端 UI 变化 (透明迁移) | `session_store` 表持久化 | fullstack-deduplication M-Slice 5 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端契约沿用 `intelligent_chat_router.py:1953-1996` 的 `/api/chat/sessions`、`DELETE /api/chat/sessions/{session_id}`、`POST /api/chat/resume` 和 JSON session store；前端 `SmartReadContext.tsx:489` 将 resume payload 转为 canonical `ChatMessageData`，`:648-676` 对带 `sessionId` 的本地 scope 在 Provider mount 后调用 `resumeChatSession({limit:100})` 并用后端 transcript 覆盖 stale local snapshot，失败时保留 localStorage fallback，`:767` provider-owned stream 发送下一轮时带回 `session_id`；`Dialog.tsx:399-403` 历史恢复写入 Provider 的 `sessionId`。验证：`npm run test -- src/contexts/SmartReadContext.test.tsx --run` 5/5 passed，覆盖 sessionId 持久化/续写与 mount hydrate；browser smoke `B8_FRONTEND_PORT=5326 node .\workspace_artifacts\runtime_state\b8-smartread-backend-session-browser-smoke.mjs` passed，断言 stale local snapshot 被 `/api/chat/resume` 返回 transcript 覆盖、历史恢复可见、follow-up `/api/chat/stream` 收到同一个 `session_b8_resume_001`；截图 `workspace_artifacts/screenshots/b8-smartread-backend-session-20260528.png`；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）。成熟方案核对：React 官方 `useEffect`/`useRef` 副作用与 StrictMode cleanup 模式、MDN Web Storage/localStorage fallback、Playwright 官方 `page.route/route.fulfill` network mocking。Rollback：checkpoint `20260528-122149-b8-smartread-backend-session-ui`，矩阵实体快照 `.rollback_snapshots/matrix-b8-smartread-backend-session-20260528_123727.md`。残余风险：浏览器验收使用 mock backend session store；真实多设备同步/DB 迁移仍取决于后端 store 从 JSON 切换到正式 DB 的后续存储实现，但 UI 权威恢复链路已不再只依赖 localStorage transcript。 |
| B9 | 长期历史、压缩、搜索与分叉 | 历史对话长期可迁移、可搜索、可分叉；长会话自动压缩且原文不丢失；多 Agent 会话可记录参与者 | Dialog/旧 IntelligentChat 历史抽屉支持全文搜索、从命中节点分叉、分支树缩进展示、agent 数提示、恢复/删除 | SQLite + JSONL 双写历史；FTS5/CJK fallback 搜索；fork 创建独立 `fork_session_id`；`/api/chat/context-compression` 配置自动压缩；`conversation_agents/agent_runs` 多 Agent 底座 | 2026-05-28 chat-history-and-compression plan | 是/P0 必做 | Claude→Codex | ✅ 已完成并前后端验证 (2026-05-28)：新增 `literature_assistant/core/chat/history_store.py`，提供 SQLite schema、JSONL transcript、`conversation_nodes`、`conversation_branches`、`compression_snapshots`、`conversation_agents`、`agent_runs`，搜索支持 FTS5 并对中文短文本 fallback 到 LIKE；`chat/pipeline.py` 增加非破坏性 `session["compression"]` 快照与历史摘要上下文拼装；`intelligent_chat_router.py` 增加 `POST /api/chat/history/import`、`GET /api/chat/history/search`、`POST /api/chat/history/conversations/{conversation_id}/fork`、`GET /api/chat/history/conversations/{conversation_id}/agents`，并让 fork 写入独立 JSON session `fork_session_id` 后再导入 SQLite；`/api/chat/sessions` summary 现在暴露 `fork` 元数据供 UI 画树。前端 `intelligentChatApi.ts` 增加 import/search/fork/agents 类型与方法；`Dialog.tsx` 历史抽屉支持搜索、命中恢复、节点分叉、删除、agent 数提示和主会话→fork 会话缩进树；`IntelligentChat.tsx` 旧入口保持搜索/分叉/树状展示 parity。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_chat_history_store.py tests\test_smart_read_pipeline.py tests\test_intelligent_chat_router.py -q -o addopts=""` 59/59 passed（仅既有 `datetime.utcnow` warning）；追加 focused `tests\test_smart_read_pipeline.py tests\test_intelligent_chat_router.py::test_chat_history_fork_and_agents_api` 22/22 passed，覆盖 fork metadata summary；`compileall -q literature_assistant\core\chat\history_store.py literature_assistant\core\chat\pipeline.py literature_assistant\core\routers\intelligent_chat_router.py` passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；`git diff --check` passed。成熟方案核对：参考 OpenAI Agents SDK Sessions/compaction、MCP 架构、OpenHanako 多 Agent session 思路，但采用文献助手本地 SQLite+JSONL 原生结构，不导入 Claude/Codex 历史。Rollback：`20260528-214827-chat-history-frontend-branch-ui`，前序修复回档 `20260528-213851-chat-history-dialog-repair`。残余风险：当前分支树是一层 source→fork 展示，尚未做完整图谱式分支管理、分支重命名或跨设备同步；多 Agent 已有历史 schema/API 底座，但未新增独立调度器。 |

---

## C. 学者工作台 (Scholar Workbench / Workbench Inspector)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| C1 | Workbench IA 重设计 | 三栏布局：文献列表 + PDF Reader + Inspector | Workbench 页显示三栏 | 无后端变化 | workbench-ia-redesign | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`WorkbenchShell` 三栏结构固定为 header + canvas + 360px inspector + drawer；`ResearchWorkbench.tsx:322-405` 将 PDF canvas、`ResearchWorkbenchInspector` 和 evidence drawer 组装成单对象工作台。验证：`npm run test -- src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 3/3 passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/c1-c3-workbench-inspector-smoke.mjs` passed，断言 workbench 头部、`aria-label="检视面板"`、360px 侧栏和证据抽屉存在；截图 `workspace_artifacts/screenshots/c1-c3-workbench-inspector-20260528.png`。成熟方案核对：Playwright 官方 `page.route` / locator / screenshot smoke 模式。Rollback：checkpoint `20260528-074807-c1-c3-workbench-inspector-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-c1-c3-20260528_075600.md`。残余风险：本次只验证单文献工作台的整体壳与可见布局，不覆盖更窄断点或长文档性能。 |
| C2 | Inspector 智读页 | 右侧 Inspector 嵌入智能研读对话 | Inspector 显示 `Conversation` | 复用 `/api/chat` | workbench-inspector phases | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`ResearchWorkbenchInspector.tsx:90-107` 智读 tab 复用 `Conversation`，并透传 `projectId/messages/onSend/selectedEvidenceId/projectReasoningBias`；`Conversation.tsx:65-120` 统一承载 `MessageRenderer + ChatInput`。验证：`npm run test -- src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 3/3 passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/c1-c3-workbench-inspector-smoke.mjs` passed，打开 `/workbench/paper/mat-c1-c3?page=1` 后默认显示智读空态、starter 按钮与输入框，截图 `workspace_artifacts/screenshots/c1-c3-workbench-inspector-20260528.png`。成熟方案核对：React `Conversation` / controlled input 组合模式。Rollback：checkpoint `20260528-074807-c1-c3-workbench-inspector-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-c1-c3-20260528_075600.md`。残余风险：本次未发真实 chat 请求，只验证智读壳、空态和输入入口。 |
| C3 | Inspector 多智能体页 | 右侧 Inspector 嵌入多智能体讨论 | Inspector 显示 Discussion 面板 | 复用 `/api/discussion` | workbench-inspector phases | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`ResearchWorkbenchInspector.tsx:108-136` 多智能体 tab 在 `inspector_embed_unified` 默认开启时内嵌 `DiscussionPanel`，回退时显示“多智能体讨论已切换到回退入口”；`DiscussionPanel.tsx` 继续复用既有讨论配置和 MCP/凭证接线。验证：`npm run test -- src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 3/3 passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/c1-c3-workbench-inspector-smoke.mjs` passed，点击多智能体 tab 后看到“讨论编排/角色配置”，且未触发回退入口；截图 `workspace_artifacts/screenshots/c1-c3-workbench-inspector-20260528.png`。成熟方案核对：Playwright 官方 `page.route` / locator / screenshot smoke 模式。Rollback：checkpoint `20260528-074807-c1-c3-workbench-inspector-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-c1-c3-20260528_075600.md`。残余风险：本次未实际发起讨论运行，只验证内嵌入口、默认 flag 和回退分支。 |
| C4 | Inspector 默认嵌入讨论 | 主线默认启用 Inspector 嵌入讨论 | Settings → 功能开关 默认 ON | `feature_flags.py` WORKBENCH_INSPECTOR_DEFAULT_EMBEDDED_DISCUSSION=True | experimental-mainline-promotion E2 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：现行 canonical flag 为 `inspector_embed_unified`，默认 `true`，覆盖矩阵旧名 `WORKBENCH_INSPECTOR_DEFAULT_EMBEDDED_DISCUSSION` 的产品意图；后端 `feature_flags.py:128-137` 声明默认开启并说明关闭后才回退；前端 `ResearchWorkbenchInspector.tsx:67-77` flag 缺失/请求失败都保持内嵌默认，`:115-123` 直接在 Workbench Inspector 多智能体 tab 渲染 `DiscussionPanel`，`:149-164` 仅显式关闭时显示跳转回退；Settings `Settings.tsx:2391-2394` 功能开关文案标明“工作台内嵌功能”。验证：`pytest tests/test_feature_flags.py -q` 10/10 passed；`npm run test -- src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 3/3 passed；`node ..\workspace_artifacts\runtime_state\c4-inspector-embedded-discussion-smoke.mjs` passed，浏览器打开 `/workbench/paper/mat-c4?page=1` 后点击 Inspector “多智能体”tab，默认 flag 下看到“讨论编排/角色配置”且无回退入口；同脚本显式 `current:false` 时看到“多智能体讨论已切换到回退入口”；`npm run build` passed；截图 `workspace_artifacts/screenshots/c4-inspector-embedded-discussion-20260527.png`。成熟方案核对：本 slice 为本地 UI/feature-flag 验收，无需额外外部方案。Rollback：checkpoint `20260527-215901-c4-inspector-embedded-discussion-default-smoke`；矩阵快照 `.rollback_snapshots/matrix-c4-20260527_220229`。残余风险：旧 flag 名未作为 env alias 保留；若外部部署仍设置旧 env var，需要后端另列兼容 alias slice。 |
| C5 | Inspector 窄栏布局优化 | 窄栏不挤压重叠，垂直滚动 | Inspector 宽度 < 400px 时布局正常 | 无后端变化 | settings-smartread-ui-followup discussion inspector slice | 是/P1 应做 | Codex(UI) | ✅ 已完成浏览器验收 (2026-05-27)：`WorkbenchShell.tsx:59-60` 固定右侧 Inspector 为 `w-[360px]` 且 `aria-label="检视面板"`，满足 `<400px` 窄栏；`ResearchWorkbenchInspector.tsx:117` 多智能体 tab 使用 `overflow-auto p-3`，`:122` 内嵌 `DiscussionPanel` 保持可滚动；验证：`npm run test -- src/components/workbench/ResearchWorkbenchInspector.test.tsx --run` 3/3 passed，Playwright smoke 打开 `/workbench/paper/mat-c5?page=1`、mock 项目/文献/注释/feature flags，量测 Inspector 宽度 360px，分别检查智读与多智能体 tab 无横向溢出，截图 `workspace_artifacts/screenshots/c5-inspector-narrow-layout-20260527.png`；回档：`20260527-211723-c5-inspector-narrow-layout-smoke` + 矩阵实体快照 `.rollback_snapshots/matrix-c5-20260527_212030`；剩余：本项只验证当前 360px shell；更窄移动断点或 DiscussionPanel 深层复杂内容需另列响应式压测 |
| C6 | Inspector 保存默认设置 | "保存为知识库讨论默认" 按钮 | Inspector 显示保存按钮 | 保存到 localStorage defaults | settings-smartread-ui-followup discussion inspector slice | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`DiscussionPanel.tsx:59-91` 使用 `discussion-panel-setup-v1` 持久化讨论问题、角色、轮次、自动停止、证据和 MCP 范围；`:769-782` “保存为知识库讨论默认”按钮写入当前配置并显示“已保存默认”；`:568-576` 修复刷新后后端 defaults 覆盖本地 `autoStop/minTurns` 的问题，只有没有本地保存值时才套用后端默认值。验证：`npm run test -- src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 2/2 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；浏览器 smoke `C6_FRONTEND_PORT=5322 node workspace_artifacts/runtime_state/c6-discussion-save-default-smoke.mjs` passed，填写讨论问题、轮次 5、自动停止、最小轮次 3 后保存，断言 localStorage 内容正确，刷新后表单恢复，截图 `workspace_artifacts/screenshots/c6-discussion-save-default-20260528.png`。成熟方案核对：MDN `localStorage.setItem/getItem` 持久化语义 + Playwright 官方 `page.evaluate`/locator/screenshot 浏览器验收模式。Rollback：checkpoint `20260528-063941-c6-discussion-save-default-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-c6-20260528_064922.md`。残余风险：这是本机 localStorage defaults，不是跨设备/多用户后端配置；如果要团队级默认值，需要后端 settings/profile API。 |
| C7 | PDF Reader L2 F0 | 加载 PDF.js 3.x | PDF Reader 渲染 PDF | `pdfjs-dist` 3.x 加载 | pdf-reader-l2 F0 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`PdfViewer.tsx:23-31` 支持从 `PdfTabsContext` 缓存读取 PDF bytes 并交给 react-pdf/PDF.js 渲染，`PdfViewer.tsx:427-431` 在文档页数就绪后按初始页跳转，`PdfViewer.tsx:637-640` 暴露页码跳转入口；`PdfTabsContext.tsx:99-229` 维护多标签 PDF view/cache，其中 `viewRevision` 让 view state 变更能广播到消费者。验证：`npm run test -- src/contexts/PdfTabsContext.test.tsx src/components/PdfViewer/PdfViewer.test.tsx --run` 9/9 passed；浏览器 smoke `C7_C9_C10_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/c7-c9-c10-pdf-reader-smoke.mjs` passed，断言 mock 3 页 PDF 首屏渲染第 1 页文本；`npm run build` passed；截图 `workspace_artifacts/screenshots/c7-c9-c10-pdf-reader-20260528.png`。成熟方案核对：React 官方 `useRef` 不触发重渲染、`useState` 触发渲染更新；Playwright 官方 `page.route` + `page.screenshot` 浏览器验收模式。Rollback：checkpoint `20260528-083407-c7-c9-c10-pdf-reader-matrix-update`，矩阵实体快照 `.rollback_snapshots/matrix-c7-c9-c10-20260528_083449`。残余风险：本次为前端 mock PDF 验收，真实扫描 PDF、超大文档性能与复杂字体渲染仍依赖 pdf.js 和后续性能测试。 |
| C8 | PDF Reader L2 F1 | 页面导航 (上/下页) | PDF Reader 显示翻页按钮 | 前端状态管理 | pdf-reader-l2 F1 | 是/P1 应做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-27)：`PdfViewer.tsx:3` 引入 `ChevronLeft/ChevronRight`，`:272-284` `goToPage()` 增加 jsdom/无 `scrollIntoView` 防守并新增 `canGoPrevious/canGoNext/jumpByPage`，`:505-535` 在 PDF Reader 工具栏页码输入两侧渲染“上一页/下一页”按钮，首尾页自动 disabled，`:811-815` 当前页按钮增加可访问名称；仍保留连续滚动、页码输入、outline/highlight 跳页。验证：`npm run test -- src/components/PdfViewer/PdfViewer.test.tsx --run` 4/4 passed，覆盖上一页/下一页边界 clamp；`node ..\workspace_artifacts\runtime_state\c8-pdf-reader-page-stepper-smoke.mjs` passed，浏览器打开 `/workbench/paper/mat-c8?page=1`，mock 两页 PDF，点击下一页/上一页并断言按钮 disabled 状态；`npm run build` passed；截图 `workspace_artifacts/screenshots/c8-pdf-reader-page-stepper-20260527.png`。成熟方案核对：react-pdf 官方 `Document`/`Page` 页码状态模式，沿用现有连续滚动 `goToPage` 而不替换渲染架构。Rollback：checkpoint `20260527-222518-c8-pdf-reader-page-stepper-buttons`；矩阵快照 `.rollback_snapshots/matrix-c8-20260527_223220`。残余风险：本 slice 不做虚拟滚动、文本搜索、打印/下载；长 PDF 性能与搜索仍对应 C15/C14。 |
| C9 | PDF Reader L2 F2 | 缩放控制 (放大/缩小) | PDF Reader 显示缩放按钮 | 前端 canvas 缩放 | pdf-reader-l2 F2 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`PdfViewer.tsx:31-35` 支持外部 zoom 状态，`PdfViewer.tsx:637-640` 工具栏与页跳转同屏，`PdfTabsContext.tsx:181-189` 修复 view 更新只写 ref 不重渲染的问题，改为更新 `viewRevision` 广播 zoom/page 变化；`PdfTabsContext.tsx:216-227` 将 `viewRevision` 纳入 context value 依赖，确保缩放按钮显示从 120%→140%→120% 实时刷新。验证：`npm run test -- src/contexts/PdfTabsContext.test.tsx src/components/PdfViewer/PdfViewer.test.tsx --run` 9/9 passed；浏览器 smoke `C7_C9_C10_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/c7-c9-c10-pdf-reader-smoke.mjs` passed，断言点击“放大/缩小”后比例文本实时变更且第 2 页仍可读；`npm run build` passed；截图 `workspace_artifacts/screenshots/c7-c9-c10-pdf-reader-20260528.png`。成熟方案核对：React 官方 `useState` 适合会影响渲染的值，`useRef` 适合不驱动渲染的缓存；Playwright 官方 screenshot smoke 模式。Rollback：checkpoint `20260528-083407-c7-c9-c10-pdf-reader-matrix-update`，矩阵实体快照 `.rollback_snapshots/matrix-c7-c9-c10-20260528_083449`。残余风险：未做移动端缩放手势、多标签大文档内存压力和 PDF canvas 性能基准，本次仅闭合核心按钮缩放契约。 |
| C10 | PDF Reader L2 F3 | 页面跳转输入框 | PDF Reader 显示页码输入 | 前端跳转逻辑 | pdf-reader-l2 F3 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`PdfViewer.tsx:337-347` `goToPage()` 对目标页做有限数值与上下界防守并滚动到页容器，`PdfViewer.tsx:542-568` 支持页码按钮/输入框提交跳转，`PdfViewer.tsx:960-1006` `PageJumpInput` 在确认或失焦时提交合法页码，`PdfTabsContext.tsx:181-189` 通过 `viewRevision` 保证 page view 更新能回传到工具栏。验证：`npm run test -- src/contexts/PdfTabsContext.test.tsx src/components/PdfViewer/PdfViewer.test.tsx --run` 9/9 passed；浏览器 smoke `C7_C9_C10_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/c7-c9-c10-pdf-reader-smoke.mjs` passed，断言页码输入跳到第 3 页、上一页到第 2 页、下一页回第 3 页；`npm run build` passed；截图 `workspace_artifacts/screenshots/c7-c9-c10-pdf-reader-20260528.png`。成熟方案核对：React 官方 state 更新驱动可见 UI；Playwright 官方 locator/assert/screenshot 验收模式。Rollback：checkpoint `20260528-083407-c7-c9-c10-pdf-reader-matrix-update`，矩阵实体快照 `.rollback_snapshots/matrix-c7-c9-c10-20260528_083449`。残余风险：本次验证的是正常页码和上下页按钮；连续滚动下的页码同步、非常大页数输入和键盘无障碍细节仍可后续专项验收。 |
| C11 | PDF Reader L2 F4 | 高亮 chunk bbox | PDF Reader 渲染高亮框 | 前端 canvas overlay | pdf-reader-l2 F4 | 是/P0 必做 | Codex(UI) | ✅ 前端 bbox overlay 已完成并浏览器验收 (2026-05-28)：`PdfViewer.tsx:610-620` 按页聚合 highlights，`:798-830` 在 PDF page wrapper 上渲染 `rects` 绝对定位 overlay；`PdfViewer.test.tsx` 新增 bbox highlight 覆盖；`ResearchWorkbench.tsx:38-58` 将 URL `bbox=` 转为归一化 rect 并与持久 annotation highlights 合并。验证同 A8：focused vitest 33/33 passed，`npm run build` passed，浏览器 smoke 和截图 `workspace_artifacts/screenshots/a8-c11-pdf-bbox-highlight-20260528.png`。成熟方案核对：react-pdf 官方 Page 分层渲染与项目 annotation `HighlightRect` 归一化坐标契约。Rollback：checkpoint `20260528-011117-20260528-a8-c11-pdf-bbox-highlight-ui`，矩阵快照 `.rollback_snapshots/matrix-a8-c11-20260528_015845`。残余风险：真实 chunk 精确框选仍依赖后端 locator/ingestion 产出归一化 bbox；若后端返回 PyMuPDF/PDF 原始 `[x0,y0,x1,y1]` 且大于 1，前端目前会拒绝渲染以避免错位。 |
| C12 | PDF Reader L2 F5 | 全屏模式 | PDF Reader 全屏按钮 | 前端全屏 API | pdf-reader-l2 F5 | 可延后/P2 | Codex(UI) | ✅ 已完成并本地浏览器验收 (2026-06-05)：`PdfViewer.tsx:173-212` 维护 `fullscreenAvailable/isFullscreen`，检测 `document.fullscreenEnabled`、`root.requestFullscreen`、`document.exitFullscreen` 并监听 `fullscreenchange/fullscreenerror` 同步浏览器/ESC 状态；`:404-412` 的 `handleToggleFullscreen()` 在 PdfViewer root 上请求或退出全屏并吞掉浏览器拒绝错误；`:731-738` 给 PdfViewer root 绑定 ref 并在全屏时撑满 viewport；`:850-860` 在工具栏新增“全屏阅读/退出全屏”图标按钮， unsupported browser 下禁用并给出 title。`PdfViewer.test.tsx:96-109` 为 jsdom 建立 Fullscreen API 边界，`:404-457` 覆盖 request/exit 状态切换；`workspace_artifacts/runtime_state/c12-pdf-fullscreen-smoke.mjs:138-148` 在浏览器 smoke 中 stub Fullscreen API，`:190-205` 断言“全屏阅读”→“退出全屏”→“全屏阅读”并记录 request/exit 事件。验证：`npm run test -- src/components/PdfViewer/PdfViewer.test.tsx --run` 12/12 passed；`C12_FRONTEND_PORT=5328 node workspace_artifacts\runtime_state\c12-pdf-fullscreen-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/c12-pdf-fullscreen-20260605.png`；`npm run build` passed，C12 引出的 chunk warning 已由 K3 bundle split 关闭。成熟方案核对：MDN Fullscreen API 与 `fullscreenchange`、React `useEffect` listener cleanup、本地 PDF Reader L2 plan/runbook。Rollback：checkpoint `20260605-011809-c12-pdf-fullscreen-preflight`，实体快照 `.rollback_snapshots/c12-pdf-fullscreen-preedit-20260605_011810`。残余风险：真实 OS/浏览器全屏过渡由平台接管，headless smoke 用 API stub 验证产品状态机。 |
| C13 | PDF Reader L2 F6 | 打印/下载 | PDF Reader 打印/下载按钮 | 浏览器打印 API | pdf-reader-l2 F6 | 是/P1 应做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-27)：`PdfViewer.tsx:3` 引入 `Download/Printer` 图标，`:247-258` 基于 `materialId` 生成安全 PDF 文件名，`:259-272` 用已加载 `pdfData` 创建 `application/pdf` Blob 并触发 `<a download>`，`:273-300` 打开 blob URL 打印窗口并调用 `print()`，弹窗被拦截时回退当前页面 `window.print()`，`:590-607` 工具栏渲染“下载 PDF/打印 PDF”按钮，PDF bytes 未就绪时 disabled；保留 `PdfReaderShell` 既有笔记 Markdown 导出。验证：`npm run test -- src/components/PdfViewer/PdfViewer.test.tsx --run` 6/6 passed，覆盖下载文件名/blob URL revoke 与打印窗口阻断回退；`node ..\workspace_artifacts\runtime_state\c13-pdf-reader-print-download-smoke.mjs` passed，浏览器打开 `/workbench/paper/mat-c13?page=1`，mock PDF 后断言两个按钮 enabled、下载事件文件名为 `mat-c13.pdf`、打印打开 blob URL 并触发 print stub；`npm run build` passed；截图 `workspace_artifacts/screenshots/c13-pdf-reader-print-download-20260527.png`，下载产物 `workspace_artifacts/runtime_state/c13-pdf-reader-download.pdf`。成熟方案核对：MDN `window.print()`、`URL.createObjectURL()`、`HTMLAnchorElement.download`/blob URL 下载模式。Rollback：checkpoint `20260527-223312-c13-pdf-reader-print-download-buttons`；矩阵快照 `.rollback_snapshots/matrix-c13-20260527_223908`。残余风险：真实系统打印对话框由浏览器/OS 接管，headless smoke 只能验证前端调用链；若用户需要打印当前注释/侧栏而非原始 PDF，需要另列 print stylesheet/合成导出 slice。 |
| C14 | PDF Reader L2 F7 | 搜索文本 | PDF Reader 搜索框 | PDF.js textContent API | pdf-reader-l2 F7 | 是/P1 应做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-27)：`PdfViewer.tsx:67-73` 新增页级搜索状态/结果模型，`:106-109` 维护搜索 query/status/results/active index，`:348-403` 基于 PDF.js `getPage(page).getTextContent()` 提取每页文本并按关键词匹配，命中后 `goToPage()` 跳转并 flash 页框，`:653-715` 工具栏新增“搜索 PDF 文本”输入、“搜索 PDF”提交、上/下一个搜索结果按钮和 `n/total` 状态；PDF 未加载完成前搜索按钮 disabled，避免早搜误报。`PdfViewer.test.tsx:180-206` 覆盖关键词命中第 2/3 页与上/下结果切换。验证：`npm run test -- src/components/PdfViewer/PdfViewer.test.tsx --run` 7/7 passed；`node ..\workspace_artifacts\runtime_state\c14-pdf-reader-text-search-smoke.mjs` passed，浏览器打开 `/workbench/paper/mat-c14?page=1`，mock 三页 PDF，等待真实 text layer 后搜索 `kinase`，断言跳到第 2 页 `1/2`，点击下一个跳到第 3 页 `2/2`；`npm run build` passed；截图 `workspace_artifacts/screenshots/c14-pdf-reader-text-search-20260527.png`。成熟方案核对：PDF.js 官方 `PDFDocumentProxy.getPage()` + `PDFPageProxy.getTextContent()` 文本抽取模式。Rollback：checkpoint `20260527-224730-c14-pdf-reader-text-search`；矩阵快照 `.rollback_snapshots/matrix-c14-20260527_231516`。残余风险：当前为页级全文搜索与跳页，不做词级坐标高亮、正则/大小写选项或跨页短语匹配；这些可另列高级搜索 slice。 |
| C15 | PDF Reader L2 F8 | 性能优化 (虚拟滚动) | 大 PDF 流畅滚动 | 前端虚拟列表 | pdf-reader-l2 F8 | 可延后/P2 | Codex(UI) | ✅ 已完成并本地浏览器验收 (2026-06-05)：`PdfViewer.tsx:94-96` 定义大文档 windowing 阈值、overscan 与默认占位高度，`:297-301` 从 `initialPage` 初始化可见窗口，`:746-780` 计算 bounded heavy page window 与 forced heavy pages，`:986-1048` 保留每页轻量 wrapper/anchor，但只为可见窗口、当前页、flash/deep-link 页和 highlight 页渲染重型 `react-pdf <Page />`，其余页使用稳定高度占位，避免大 PDF 一次性创建全部 canvas/text layer。`PdfViewer.test.tsx:8-79` 扩展 mock page count 与 rendered page 跟踪，`:336-355` 覆盖 80 页 PDF 下所有页 anchor 存在但重型 page 仅渲染 `[37,38,39,40,41,42,43,72]`。验证：`npm run test -- src/components/PdfViewer/PdfViewer.test.tsx --run` 13/13 passed；`npm run build` passed；临时 Vite `127.0.0.1:3100` 下 `C7_C9_C10_FRONTEND_PORT=3100 node workspace_artifacts\runtime_state\c7-c9-c10-pdf-reader-smoke.mjs` passed，覆盖渲染/缩放/页码跳转；`node workspace_artifacts\runtime_state\c14-pdf-reader-text-search-smoke.mjs` passed，覆盖搜索命中跳页；端口 3100 已按监听 PID 清理。成熟方案核对：web.dev long-list windowing/overscan、MDN IntersectionObserver、react-pdf `Document`/`Page` 渲染模型、本地 Direct PDF Reader Reliability 与 Zotero PDF Reader 参考研究。Rollback：checkpoint `20260605-041129-c15-pdf-virtualization-preflight`，实体快照 `.rollback_snapshots/c15-pdf-virtualization-preedit-20260605_041128`；文档收尾 checkpoint `20260605-124205-c15-docs-final-stop-audit-preedit`，实体快照 `.rollback_snapshots/c15-docs-final-preedit-20260605_124205`。残余风险：当前是重型 page/canvas windowing，不是完整 DOM wrapper 虚拟列表；为了保留页锚点、证据跳转、搜索和 overlay，轻量 page wrapper 仍按页存在。大量 highlight 页会被强制重型渲染，属于兼容性优先的性能折中。 |
| C16 | KG-1 Step 5 嵌入 | Workbench 嵌入知识图谱查看器 | Workbench 显示 KG viewer | `/api/kg/graph` 返回图数据 | kg-1-step-5-embed | 可延后/P2 | Claude→Codex | ✅ 已完成本地 API 兼容 alias (2026-06-05)：保留 canonical `GET /api/graph/payload` 和现有前端 `GraphPayloadViewer`/`graphApi.ts` 主调用，新增 `GET /api/kg/graph` 作为矩阵时代兼容入口，两个端点共用 `_graph_payload_for_query()` 并返回同一 `GraphPayloadV0` schema；`python_adapter_server.py` 注册 `kg_graph_router`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_graph_router.py tests\test_graph_payload.py -q -o addopts=""` 23/23 passed；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\routers\graph_router.py literature_assistant\core\python_adapter_server.py tests\test_graph_router.py` passed；运行时 route smoke 确认 `/api/graph/payload` 与 `/api/kg/graph` 均注册，`app.openapi()` 中 `/api/kg/graph` 200 schema 指向 `GraphPayloadV0`；`git diff --check` passed。成熟方案核对：FastAPI `APIRouter`/`include_router` 兼容路由模式、OpenAPI operation/schema 暴露、REST API backward-compatible alias/versioning 实践。Rollback：checkpoint `20260605-133142-c16-kg-graph-api-alias-preedit`，实体快照 `.rollback_snapshots/c16-kg-graph-api-alias-preedit-20260605_133142`。残余风险：未重写已大幅 dirty 的 generated OpenAPI 文件，避免 broad generated churn；前端仍优先使用 canonical `/api/graph/payload`。 |

---

## D. 多 Agent 讨论 (Discussion + Auto-stop + Citation)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| D1 | Runtime 凭证 Phase 6 | 讨论时动态选择 API 凭证 | Discussion 面板显示凭证选择器 | `runtime_credentials_store` 表 | runtime-credentials Phase 6 | 是/P0 必做 | Claude→Codex | ✅ 已完成并复核 (2026-05-27)：后端支持 `credential_id`、`strategy_hint`+`category` 动态采样；`DiscussionAgentConfig` 新增 `strategy_hint`/`category` 字段（与 `credential_id`/`llm` 互斥）；`_resolve_agent_endpoint` 优先级：llm > credential_id > strategy_hint > default；采样逻辑复用 `/api/credentials/sample`；`DiscussionAgentTrace` 不含 `api_key` 字段；前端已接入运行时凭证选择器：`DiscussionPanel.tsx:27` 引入 `CredentialPicker`，`:180` 构造讨论角色凭证需求，`:498-506` 将所选凭证写入 `agentConfig.credential_id` 并清空 inline llm/strategy/category，`:808-823` 渲染每个角色的本次 API 选择和清除入口；流式入口 `DiscussionContext.tsx:253` → `discussionApi.ts:244` `POST /api/discussion/runs/stream`。验证：后端队列复核 `pytest tests/test_credentials_strategy_hint_mapping.py tests/test_credentials_sampling.py tests/test_discussion_runtime_credentials.py tests/test_discussion_mcp_per_agent_scope.py tests/test_wiki_permissions.py tests/test_skill_export.py tests/test_writing_projects_api.py tests/test_writing_outline_api.py tests/test_writing_citations_api.py tests/test_writing_figures_api.py tests/test_writing_submission_export.py tests/test_evidence_refs_api.py tests/test_discussion_history.py tests/test_smart_read_pipeline.py tests/test_evolution_mempalace.py -q` 151/151 passed；`npm run build` passed；`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx --run` 10/10 passed；浏览器 smoke `node ../workspace_artifacts/runtime_state/d1-runtime-credentials-browser-smoke.mjs` 通过，mock 选中已保存凭证后拦截请求体断言 `agent_configs[0].credential_id=cred-discussion-fast` 且无 inline `api_key`，截图 `workspace_artifacts/screenshots/d1-runtime-credentials-discussion-20260527.png`。成熟方案核对：本 slice 为已实现 UI 的本地浏览器验收，无需额外外部方案。Rollback：checkpoint `20260527-212706-d1-runtime-credentials-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-d1-20260527_213127`。残余风险：真实凭证连通性仍依赖用户本地 credential store 与 provider 状态，本次未触发真实模型调用。 |
| D2 | Runtime 凭证 Phase 7 | 凭证采样端点 `/api/credentials/sample` | 无前端 UI (后端 API) | `/api/credentials/sample` 200 OK | runtime-credentials Phase 7 | 是/P1 应做 | Claude | ✅ 已完成 (2026-05-26 commit b5b0ca3e + cc0da047)：`POST /api/credentials/sample?category={generation|embedding|rerank}&strategy_hint={tier}` 按 category + strategy_hint 选择凭证；精确匹配 strategy_hint 优先（双向 normalize：查询参数和候选凭证都 normalize 后比较，确保 legacy stored credential 能被 canonical query 命中），回退到最高 priority (数值越小优先级越高)；返回 `RuntimeCredentialPublic` (masked api_key, canonical strategy_hint)；11 测试覆盖精确匹配/legacy 映射/priority 回退/category 过滤/无可用凭证 404/默认 category=generation/默认 strategy_hint=medium/api_key 掩码/legacy stored 匹配/legacy query 匹配/list 输出 canonical (`tests/test_credentials_sampling.py`)；代码：`literature_assistant/core/routers/credentials_router.py:208-261` sample_credential endpoint, `:252-256` 双向 normalize 匹配；验证：`pytest tests/test_credentials_sampling.py -v` 11/11 passed；依赖注入修复：router 所有端点使用 `Depends(get_credential_store)` 支持测试隔离 |
| D3 | Autostop 基础逻辑 | 讨论自动停止 (收敛/超时) | Discussion 面板显示停止原因 | `autostop_detector.py` 触发 | autostop-logic | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `discussion_orchestrator.py:849-850` 初始化 `stopped_early/stop_reason`，`:1057-1076` 在收敛 judge 达阈值时置 `stopped_early=True`、`stop_reason=converged`，`:1164-1187` 输出 convergence trace；前端 `DiscussionPanel.tsx:1270-1281` 显示“早停 · 收敛于第 N 轮 / 共 N 轮”和相似度。验证：浏览器 smoke `D3_D4_D6_D19_D23_FRONTEND_PORT=5325 node ..\workspace_artifacts\runtime_state\d3-d4-d6-d19-d23-discussion-browser-smoke.mjs` passed，mock SSE final result 断言早停提示和相似度可见；`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 15/15 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`。成熟方案核对：Playwright 官方 `page.route/route.fulfill` network mocking、MDN SSE `text/event-stream`/`data:` 格式、React conditional rendering。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵实体快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：本次为本地 mock SSE 验收，未触发真实 LLM convergence judge。 |
| D4 | Evidence trace G1 | 讨论生成证据包 | Discussion 显示证据引用 | `evidence_builder.py` 输出 | evidence-trace G1 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`DiscussionEvidencePackPayload` 经 `discussionApi.ts:121-129` 类型化传入前端；`DiscussionPanel.tsx:1296` 接入 `DiscussionEvidencePackSection`，`:1539-1570` 渲染证据池、`evidence_ids`、source 与 snippet 内容。验证：聚合 browser smoke `workspace_artifacts/runtime_state/d3-d4-d6-d19-d23-discussion-browser-smoke.mjs:249-357` mock evidence pack 三条 snippets，断言 `discussion-evidence-pack`、`evidence-snippet-E1/E2/E3` 和证据内容可见，截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；Vitest 15/15 passed；`npm run build` passed。成熟方案核对：Playwright route fixture + React list rendering。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：证据包持久化表仍属 D5，当前只验收 run payload 到 UI 的显示链路。 |
| D5 | Evidence trace G2 | 证据包持久化到 DB | 无前端 UI | `evidence_pack` 表 CRUD | evidence-trace G2 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：现行最小持久化契约采用 runtime-state JSON store，而非真实 DB 表；`evidence_router.py:288-374` 新增 `workspace_artifacts/runtime_state/discussion/evidence_packs.json` 读写层，按 `version + packs[]` 保存 `DiscussionEvidencePackPayload`，读取时跳过坏记录，写入用 tmp + `os.replace` 原子替换；`:395-438` 的 `POST/GET /api/discussions/{discussion_id}/evidence_pack` 现在会校验非空 `discussion_id/project_id/query`，保存后落盘，GET 会在进程缓存清空后从 runtime store 重新加载并返回同 discussion 最新 pack。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evidence_refs_api.py -q -o addopts=""` 18/18 passed，覆盖路由存在、保存/读取、清空 `_discussion_packs_store` 后从磁盘恢复、缺失 404；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\routers\evidence_router.py tests\test_evidence_refs_api.py` passed；`git diff --check -- literature_assistant\core\routers\evidence_router.py tests\test_evidence_refs_api.py` passed。成熟方案核对：沿用仓库现有 annotation/wiki/chunk-store 的 JSON `tmp + os.replace` 原子写入模式，保留 FastAPI `response_model` + Pydantic request validation。Rollback：checkpoint `20260528-112908-d5-discussion-evidence-pack-persistence-continue`，矩阵实体快照 `.rollback_snapshots/matrix-d5-discussion-evidence-pack-20260528_113129.md`。残余风险：矩阵原文写“DB 表 CRUD”；本 slice 闭合的是本地 runtime-state 持久化和 API CRUD 兼容，不引入 SQLite/关系表迁移，也未把 Discussion run 自动保存链路强制改为调用该独立 API。 |
| D6 | Evidence trace G3 | 前端渲染证据引用气泡 | Discussion 消息显示引用标记 | 前端解析 evidence refs | evidence-trace G3 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`DiscussionPanel.tsx:1469-1495` 将 evidence pack snippets 映射为 `EvidenceRefLike`，`:1497-1536` 的 `DiscussionCitedEvidencePills` 对有 `material_id` 的证据复用通用 `EvidencePill`，无 material 时保留证据池定位 fallback；`:1348` 在每个成功 agent trace 下渲染引用 pill。验证：聚合 browser smoke 断言 `cited-evidence-pills-proposer_1` 中显示 `fatigue-sn-curve.pdf` 和 `crack-growth-rate.pdf`；`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 15/15 passed；`npm run build` passed；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`。成熟方案核对：React conditional/list rendering + Playwright locator assertions。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：PDF 精确 bbox 跳转仍依赖上游 evidence snippet/locator 返回归一化 bbox。 |
| D7 | Evidence trace G4 | 引用点击跳转源文献 | 引用气泡点击 → PDF Reader | 前端路由 + locator API | evidence-trace G4 | 是/P0 必做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-27)：Discussion trace 的 `cited_evidence_ids` 已从仅滚动证据池升级为复用通用 `EvidencePill` 打开 PDF；`DiscussionPanel.tsx:28` 引入 `EvidencePill`，`:1126` 渲染 `DiscussionCitedEvidencePills`，`:1247-1272` 从 evidence pack `E1/E2` 映射 `material_id/chunk_id/page/source`，`:1275-1310` 有 material target 时跳 `/workbench/paper/{materialId}?page=&chunk=`，缺 material 时保留 `scrollToEvidence()` 证据池定位 fallback；回归 `DiscussionPanel.evidencePills.test.tsx:185-269` 覆盖 PDF 跳转与 fallback；验证：`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx --run` 10/10 passed，`npm run build` passed，Playwright browser smoke 注入 completed discussion session 后点击 `paper one · p.6`，URL 保留 `/workbench/paper/mat-1?page=6&chunk=chunk-1`，截图 `workspace_artifacts/screenshots/d7-discussion-evidence-jump-20260527.png`；回档：`20260527-202746-d7-discussion-evidence-jump-smoke` + 矩阵实体快照 `.rollback_snapshots/matrix-d7-20260527_205102`；剩余：精确 bbox 高亮仍依赖后端 locator 返回 bbox |
| D8 | Evidence trace G5 | 证据重叠检测 | 后端检测重复引用 | `citation_overlap_detector.py` | evidence-trace G5 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：现行后端实现位于 `evidence_router.py:329-344`，`DetectOverlapAnchor/DetectOverlapRequest` 为 `/api/citations/detect_overlap` 增加可选 `anchors` 输入并保留旧的 `project_id/draft_id/threshold` 兼容；`:347-371` 用规范化 token set 计算 Jaccard 相似度，`:364-371` 对同 material 同 chunk 复用给 1.0 overlap，`:388-436` 返回 `CitationOverlapPayload`，含 `anchor_id/material_id/chunk_id/overlap_score/overlapping_anchors/recommendation`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evidence_refs_api.py tests\test_discussion_convergence_citation.py tests\test_discussion_evidence_trace.py tests\test_discussion_evidence_trace_wiring.py -q -o addopts=""` 79/79 passed，覆盖端点存在、同 chunk 复用 1.0 overlap、文本 Jaccard 阈值、空 anchors 兼容空列表和既有 Discussion evidence trace；`compileall` passed；`npm run generate:openapi` passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）。成熟方案核对：采用 SciPy/集合论常见 Jaccard 相似度口径（实现为本地 set 逻辑，避免新增依赖）+ FastAPI `response_model`/Pydantic request model 模式。Rollback：checkpoint `20260528-105938-d8-discussion-citation-overlap-verification`，矩阵实体快照 `.rollback_snapshots/matrix-d8-citation-overlap-20260528_111315.md`。残余风险：该端点检测 exact chunk reuse 与文本 token overlap，不做 embedding 语义相似或跨语言同义判断；DiscussionPanel 当前 D9 警告仍基于 run payload 的 `cited_evidence_ids` 派生，尚未额外调用该端点。 |
| D9 | Evidence trace G6 | 前端显示重叠警告 | Discussion 显示"引用重叠"提示 | 前端接收 overlap 字段 | evidence-trace G6 | 是/P1 应做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-28)：前端 `DiscussionPanel.tsx:205-305` 新增 `buildCitationOverlapWarningSummary()`，忽略失败角色并按成功角色的引用集合计算重复证据与最大 pairwise Jaccard；`:307-355` 渲染 `role="alert"` 的“引用重叠”警告，展示重复 evidence ids、最高角色间重叠比例和参与角色；`:1122` 在讨论结果区接入。后端独立检测端点已在 D8 补齐为 `/api/citations/detect_overlap`，但 D9 当前浏览器链路仍采用 Discussion run 结果中的 `cited_evidence_ids` 做只读 UI 告警，不改变 run response 契约。验证：`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx --run` 13/13 passed；`npm run build` passed（保留既有 `annotationApi.ts` 动静态混合 import warning）；浏览器 smoke `D9_FRONTEND_PORT=5311 node ..\workspace_artifacts\runtime_state\d9-discussion-overlap-warning-smoke.mjs` passed，mock SSE 中支持方/批评方共同引用 `E1`，页面显示“引用重叠：1 条证据”和 33% 最高重叠，截图 `workspace_artifacts/screenshots/d9-discussion-overlap-warning-20260528.png`。成熟方案核对：React 官方 conditional rendering / list rendering 派生 UI 模式 + MDN `role="alert"` 可访问告警模式。Rollback：checkpoint `20260528-004741-20260528-d9-discussion-overlap-warning-ui`；矩阵快照 `.rollback_snapshots/matrix-d9-20260528_005618`。残余风险：前端告警仍无法发现语义相近但 ID 不同的 citation overlap；若需要把 D8 endpoint 接入 UI，应另列非阻塞增强 slice。 |
| D10 | History cap H0 | 讨论历史上限 100 轮 | 无前端 UI | `discussion_store` 限制 100 条 | history-cap H0 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口/store 验收 (2026-05-28)：`discussion_task_store.py:43` 定义 `DEFAULT_HISTORY_CAP = 100`，保留既有 `DEFAULT_CAPACITY = 100` active runs；`:78-91` 允许构造时注入 `history_cap` 并拒绝非正 ttl/capacity/history_cap；`:126-147` 写入 SSE event 与 `agent_done` live trace 时裁剪到最新 100 条，`:197-203` 让 resume `from_index` 按 `event_log_start_index` 计算裁剪后的 replay offset，`:233-249` 为 event log/live traces 提供有界窗口，`:252-266` snapshot 暴露 `event_log_start_index` 与实际 `history_cap`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_discussion_task_store.py tests\test_discussion_stream_endpoint.py -q -o addopts=""` 31/31 passed，覆盖默认 100、无效上限拒绝、小 cap 下 event_log/live_traces 只保留最新记录、resume replay 不破坏既有 stream endpoint；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed；`git diff --check -- literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed。成熟方案核对：Python 官方 `collections.deque(maxlen=...)` bounded-history 模式；本仓为兼容现有 list replay/from_index API，采用等价的 append 后裁剪 + 起始索引方案。Rollback：checkpoint `20260528-113212-d10-discussion-history-cap`，矩阵实体快照 `.rollback_snapshots/matrix-d10-discussion-history-cap-20260528_113556.md`。残余风险：该 slice 限制的是现有 Discussion stream task store 的 event replay/live trace 运行态历史，不新增 D12-D16 所需分页历史列表、归档表、搜索、导出或删除契约；已完成的 run 仍按既有 TTL/容量保留，非进程重启持久化属于 D17/后续 store slice。 |
| D11 | History cap H1 | 超限自动归档 | 无前端 UI | 旧记录移到 `archived_discussions` | history-cap H1 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并 store 验收 (2026-05-28)：现行最小归档契约为进程内 `DiscussionTaskStore` overflow archive，而非真实 `archived_discussions` DB 表；`discussion_task_store.py:44` 定义 `DEFAULT_ARCHIVE_CAPACITY = 100`，`:79-99` 支持 `archive_capacity` 并拒绝非正配置，`:107-114` 在 active run 容量满时先调用归档腾位，仍然只归档 terminal run，若全是 pending/running 则继续返回 `DiscussionTaskStoreFull`；`:220-226` 暴露 `list_archived()` 本地 store 查询，`:232-235` clear 同步清 archive，`:264-281` 将最旧 completed/cancelled/error snapshot 移到 bounded archive 并加 `archived/archived_at`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_discussion_task_store.py tests\test_discussion_stream_endpoint.py -q -o addopts=""` 35/35 passed，覆盖 overflow 自动归档最旧 terminal run、全 active 满载不丢工作、archive 自身上限、stream/resume 回归；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed；`git diff --check -- literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed。成熟方案核对：采用缓存 eviction/TTL store 常见的“容量满时优先逐出最旧 terminal item，保护 active item”策略，并保留 D10 的 bounded history replay 语义。Rollback：checkpoint `20260528-113720-d11-discussion-overflow-archive`，矩阵实体快照 `.rollback_snapshots/matrix-d11-discussion-overflow-archive-20260528_113855.md`。残余风险：该归档只在现有 task store 内可见，不新增 `/api/discussion/history`、分页历史列表、持久 DB 表或用户可见 archive UI；D12-D16 仍需后端历史列表/搜索/导出/删除契约后才能做前端验收。 |
| D12 | History cap H2 | 前端分页加载历史 | Discussion 历史列表分页 | `/api/discussion/history?page=N` | history-cap H2 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端在 `discussion_task_store.py` 增加 `list_runs(include_archived)`，按更新时间返回 run 快照；`models/discussion.py` 增加 `DiscussionRunHistoryItemPayload/DiscussionRunHistoryPagePayload`；`discussion_advanced_router.py` 增加 `GET /api/discussion/history?page=&page_size=&q=&state=&include_archived=`，用 response model 返回分页列表。前端 `discussionApi.ts` 增加 `listHistory()` 类型方法；`DiscussionPanel.tsx` 新增“讨论历史”面板，支持分页按钮与加载/空/错误状态。验证：`pytest tests/test_discussion_task_store.py tests/test_discussion_history.py -q -o addopts=""` 42/42 passed；`npm run test -- src/services/discussionApi.test.ts src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 4/4 passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/d12-d16-discussion-history-browser-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/d12-d16-discussion-history-20260528.png`。成熟方案核对：FastAPI `APIRouter` + `response_model` 分页契约、React Router 页面集成、Blob/object URL 下载模式。Rollback：实体快照 `.rollback_snapshots/matrix-d-history-20260528_132252`。 |
| D13 | History cap H3 | 归档历史只读查看 | Discussion 显示"归档"标签 | `/api/discussion/archived` | history-cap H3 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：`discussion_task_store.py` 增加 `archive(run_id)`，把 active run 快照移入 bounded archive 并持久化；`discussion_advanced_router.py` 增加 `GET /api/discussion/archived` 与 `PUT /api/discussion/runs/{run_id}/archive`；前端 `discussionApi.ts` 增加 `listArchived()`、`archiveRun()`，`DiscussionPanel.tsx` 新增“最近/归档”切换，归档记录显示 `Archive` 标签且不再展示归档按钮，作为只读历史查看。验证同 D12；浏览器 smoke 覆盖归档按钮、归档 tab、归档标签可见。Rollback：`.rollback_snapshots/matrix-d-history-20260528_132252`。 |
| D14 | History cap H4 | 归档搜索 | Discussion 历史搜索框 | 后端全文搜索 archived | history-cap H4 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：`discussion_advanced_router.py` 增加 `GET /api/discussion/history/search?q=&page=&page_size=&archived_only=`，并让 history/archived 列表也接受 `q`；搜索范围为 query 与 synthesis preview 的本地全文包含匹配，不引入外部索引；前端 `DiscussionPanel.tsx` 搜索框驱动最近/归档列表过滤，`discussionApi.ts` 增加 `searchHistory()`。验证同 D12；浏览器 smoke 覆盖搜索框输入后目标历史仍可见。残余风险：当前为 run summary 搜索，不搜索完整 agent answer 正文；如需全文正文搜索可在 SQLite 化后扩展。Rollback：`.rollback_snapshots/matrix-d-history-20260528_132252`。 |
| D15 | History cap H5 | 归档导出 | Discussion 导出按钮 | `/api/discussion/export` JSON | history-cap H5 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：采用 run-scoped 兼容契约 `POST /api/discussion/runs/{run_id}/export?format=json|markdown`，避免旧 session export 与高级 run store 混淆；`DiscussionRunExportPayload` 返回 `run_id/format/content/filename`；Markdown 导出包含 query、turns、agent answers、synthesis，JSON 导出为 store snapshot。前端 `discussionApi.ts` 增加 `exportRun()`；`DiscussionPanel.tsx` 导出按钮使用 Blob + object URL 下载 JSON，并及时 revoke URL。验证同 D12；浏览器 smoke 覆盖导出按钮和 export endpoint 调用。Rollback：`.rollback_snapshots/matrix-d-history-20260528_132252`。 |
| D16 | History cap H6 | 归档删除 (用户确认) | Discussion 删除按钮 + 确认弹窗 | `/api/discussion/delete` | history-cap H6 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：采用 run-scoped 删除契约 `DELETE /api/discussion/runs/{run_id}`，`discussion_task_store.py.delete()` 同时移除 active 与 archived 记录并返回布尔结果；`discussion_advanced_router.py` 返回 `{status:"deleted", run_id}` 或 404。前端 `discussionApi.ts` 增加 `deleteRun()`；`DiscussionPanel.tsx` 删除按钮先 `window.confirm()`，确认后删除并刷新当前历史列表。验证同 D12；浏览器 smoke 自动 accept confirm 并覆盖 delete endpoint 调用。Rollback：`.rollback_snapshots/matrix-d-history-20260528_132252`。 |
| D17 | Discussion 持久化 B1 | 讨论记录存入 DB | 无前端 UI 变化 | `discussion_store` 表持久化 | 0182-release B1 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并 store 验收 (2026-05-28)：现行最小持久化契约采用 runtime-state JSON store，而非真实 DB 表；`discussion_task_store.py:50` 定义 store schema version，`:90-108` 支持可选 `persistence_path` 并在构造时加载磁盘状态，`:134/:185/:201/:243/:249/:264` 在 register/event/terminal/delete/clear/sweep 变更后持久化，`:299-339` 从 JSON 加载 `entries/archived_entries` 并用 tmp + `os.replace` 原子写入，`:342-397` 序列化/校验 `_StoreEntry`，`:422-432` 让进程级 singleton 默认落到 `workspace_artifacts/runtime_state/discussion/task_store.json`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_discussion_task_store.py tests\test_discussion_stream_endpoint.py -q -o addopts=""` 37/37 passed，覆盖 task entries 持久化重载、bounded event log 恢复、overflow archive 持久化重载、stream/resume 回归；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed；`git diff --check -- literature_assistant\core\discussion_task_store.py tests\test_discussion_task_store.py` passed。成熟方案核对：沿用本仓 annotation/wiki/chunk-store 的 JSON `tmp + os.replace` 原子写入模式，保留 FastAPI stream 现有 replay API，不引入 DB migration。Rollback：checkpoint `20260528-113937-d17-discussion-task-store-persistence`，矩阵实体快照 `.rollback_snapshots/matrix-d17-discussion-task-store-persistence-20260528_114131.md`。残余风险：矩阵原期望 `discussion_store` DB 表；本 slice 闭合本地 runtime-state 持久化和进程重建恢复，不新增分页 history/archive/search/export/delete API，也不保证跨机器/多进程并发写一致性。 |
| D18 | Discussion 流式 DSE-1 | SSE 流式返回讨论消息 | Discussion 逐 token 显示 | `/api/discussion/stream` 200 OK | 0182-release DSE-1 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：后端保留 canonical `POST /api/discussion/runs/stream`，新增兼容别名 `POST /api/discussion/stream`，两者复用同一 `StreamingResponse(text/event-stream)` SSE 实现；定位 `discussion_advanced_router.py:349` `/runs/stream`，`:488` `/stream` alias，`discussionApi.ts:251` 前端当前调用 `/runs/stream`，`DiscussionContext.tsx:253` 消费 stream events 并回落到非流式；新增 `tests/test_discussion_stream_endpoint.py` alias 契约测试。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_discussion_stream_endpoint.py -q` 11/11 passed；`node ..\workspace_artifacts\runtime_state\d18-discussion-stream-smoke.mjs` passed，mock SSE `started/stage_progress/agent_done/synthesis_done/done` 并断言 Discussion 面板显示智能体输出与综合结论；`npm run build` passed；截图 `workspace_artifacts/screenshots/d18-discussion-stream-20260527.png`。成熟方案核对：FastAPI 官方 `StreamingResponse` 模式，按 media type 返回 `text/event-stream`；本 slice 采用兼容别名而非替换 canonical URL。Rollback：checkpoint `20260527-215400-d18-discussion-stream-alias-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-d18-20260527_215733`。残余风险：当前讨论流为 SSE 事件级增量（agent_done/synthesis_done），并非 provider token-by-token；如验收必须严格逐 token，需要后端 orchestrator/provider 层另列 slice。 |
| D19 | Discussion 流式 DSE-2 | 前端 EventSource 接收 | Discussion 组件订阅 SSE | 前端 EventSource 连接 | 0182-release DSE-2 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：前端实际使用 `fetch` 读取 SSE stream，不是浏览器 `EventSource`，契约闭合点为 `discussionApi.ts:245-253` `runDiscussionStream()` POST 到 `/api/discussion/runs/stream`，`:286-335` `consumeDiscussionSseStream()` 按 `data:` 行解析事件；`DiscussionContext.tsx:253` 订阅 stream 并把事件写入 session。验证：聚合 browser smoke mock `started/stage_progress/agent_done/turn_done/synthesis_done/done`，断言页面显示两个 agent 输出、综合结论和 completed 状态；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；Vitest 15/15 passed；`npm run build` passed。成熟方案核对：MDN SSE event stream format + Playwright network mocking。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：仍是事件级 SSE，不是 token-by-token 输出。 |
| D20 | Discussion 流式 DSE-3 | 多 Agent 并发流 | Discussion 显示多个 Agent 同时输出 | 后端 asyncio 并发 | 0182-release DSE-3 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `discussion_orchestrator.py:54` 引入 `arun_parallel_round`，`:920` 每轮并发运行 agent；前端 `DiscussionContext.tsx:160-206` 对每个 `agent_done` 追加 `liveTraces`，`DiscussionPanel.tsx:1298-1348` 按 turn/agent trace 渲染多角色结果。验证：聚合 browser smoke 的同一轮 SSE 连续发送支持方与批评方 `agent_done`，断言两段 agent answer 同屏可见并进入最终结果；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；Vitest 15/15 passed；`npm run build` passed。成熟方案核对：Playwright SSE fixture + React list rendering。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：browser smoke 证明 UI 消费多 agent 事件，后端真实并发性能/取消语义仍依赖后端 pytest/负载测试。 |
| D21 | Discussion 流式 DSE-4 | 流式错误处理 | Discussion 显示错误提示 | SSE error event | 0182-release DSE-4 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `discussion_advanced_router.py:422-447` 将异常序列化为 `{event:'error', status, error}` SSE；前端 `discussionApi.ts:407-411` 定义 error event，`DiscussionContext.tsx:288-305` 将 stream/fallback 错误写入 session error，`DiscussionPanel.tsx:1261-1265` 展示红色错误文本。验证：聚合 browser smoke error 分支 `workspace_artifacts/runtime_state/d3-d4-d6-d19-d23-discussion-browser-smoke.mjs:360-391` mock `event:error`，断言页面出现“出错”和 fixture 错误文案；错误态截图 `workspace_artifacts/screenshots/d21-discussion-stream-error-20260528.png`；主流程截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；Vitest 15/15 passed；`npm run build` passed。成熟方案核对：MDN SSE `data:` event payload + Playwright route fulfill。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：真实 provider 错误分类/中文化仍由后端 error mapper 决定，本次只验收前端显示 SSE error。 |
| D22 | Analysis Chain ACR-2 | Discussion 集成思维链 | Discussion 显示推理步骤 | `analysis_chain_rag_builder.py` | 0182-release ACR-2 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`discussion_orchestrator.py:1236-1263` 在 `analysis_chain_discussion` flag 开启时调用 `analysis_chain_rag_builder.build_analysis_chain()`；`analysis_chain_rag_builder.py:184-207` 提供同步 6 字段 AnalysisChain 构造；前端 `discussionApi.ts:131-138` 类型化 `AnalysisChainPayload`，`:152` 挂到 `DiscussionAgentTrace.analysis_chain`，`DiscussionPanel.tsx:1344-1345` 传给 `AnalysisChainPanel`。验证：聚合 browser smoke 在 agent trace 中注入 observation/mechanism/evidence/boundary/counter_evidence/next_action 六字段，断言折叠状态下内容不存在、展开后六字段内容可见；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；Vitest 15/15 passed；`npm run build` passed。成熟方案核对：React conditional rendering + Playwright locator state assertions。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：浏览器验收使用 mock payload；真实 `analysis_chain_discussion` flag 和 LLM/deterministic 生成质量未做真实模型 smoke。 |
| D23 | Analysis Chain ACR-3 | 前端渲染思维链 UI | Discussion 显示折叠/展开推理 | 前端解析 chain steps | 0182-release ACR-3 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`AnalysisChainPanel.tsx:35-84` 渲染可折叠 6 字段面板，空字段不渲染；`DiscussionPanel.tsx:788` 现为 `min-h-0` flex column，`:1270` 结果区取消零高内部滚动容器，修复常规 1440x1020 桌面高度下 AnalysisChain 按钮被外层 `overflow-hidden` 裁剪导致不可点击的问题；`:1344-1345` 在 Discussion agent trace 下接入。验证：聚合 browser smoke 在 1440x1020 viewport 下点击“AI 推理过程”，断言 `aria-expanded` 从 `false` 到 `true` 且六字段全部可见；截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png`；`npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 15/15 passed；`npm run build` passed。成熟方案核对：React conditional rendering + Playwright pointer-reachable locator checks。Rollback：checkpoint `20260528-091742-d3-d4-d6-d19-d23-discussion-browser-smoke-resume`，矩阵快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-20260528_092730.md`。残余风险：未做移动 viewport 截图；本次只闭合桌面 Discussion browser acceptance。 |

复验记录 (2026-05-28 10:38)：D3/D4/D6/D19-D23 聚合 browser smoke 已在当前 worktree 用临时 Vite `D3_D4_D6_D19_D23_FRONTEND_PORT=5325 node ..\workspace_artifacts\runtime_state\d3-d4-d6-d19-d23-discussion-browser-smoke.mjs` 重新通过，刷新截图 `workspace_artifacts/screenshots/d3-d4-d6-d19-d23-discussion-browser-smoke-20260528.png` 与 `workspace_artifacts/screenshots/d21-discussion-stream-error-20260528.png`；focused Vitest `npm run test -- src/components/DiscussionPanel/DiscussionPanel.evidencePills.test.tsx src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 15/15 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）。矩阵实体快照 `.rollback_snapshots/matrix-d3-d4-d6-d19-d23-reverify-20260528_103957.md`。

---

## E. 灵感工坊 (Inspiration / Evidence-refs / Causal-DAG)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| E1 | Inspiration 重写 B1 | 新架构：IRAC/FinCoT 框架 | 无前端 UI 变化 | `inspiration_generator.py` 重构 | inspiration-rewrite B1 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口单测验收 (2026-05-28)：现行主实现仍在 `inspiration_router.py`，已支持 `GenerateSparksRequest.frame` 的 `auto/irac/fincot`、模板文件 `inspiration_irac.txt`/`inspiration_fincot.txt`、`_select_inspiration_frame()` 自动选 FinCoT 机制类 query 和 `_build_inspiration_prompt()` JSON-only prompt；本 slice 新增 `inspiration_generator.py:28-70` 作为历史矩阵点名的稳定 facade，`InspirationGenerator.select_frame/build_prompt/generate_local` 复用 active router/engine，不复制生成逻辑，`build_inspiration_generator()` 提供兼容入口。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_generator.py tests\test_inspiration_prompt_templates.py tests\test_inspiration_router.py -q -o addopts=""` 33/33 passed，覆盖 IRAC/FinCoT 模板存在与 schema anchor、prompt 构造、facade frame selection、本地 engine delegate、router JSON schema fallback；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\inspiration_generator.py tests\test_inspiration_generator.py tests\test_inspiration_router.py` passed；`git diff --check -- literature_assistant\core\inspiration_generator.py tests\test_inspiration_generator.py tests\test_inspiration_router.py` passed。顺手修复测试隔离：`tests/test_inspiration_router.py:360-373` 将 env fallback 用例与本地 `chat_store` runtime override 隔离，避免用户配置污染测试。成熟方案核对：采用 facade/compatibility layer 模式闭合历史入口名，同时保持 active router 为单一生成实现；LLM 输出继续用 JSON schema/json_object 结构化输出模式。Rollback：checkpoint `20260528-114709-e1-inspiration-framework-contract`，矩阵实体快照 `.rollback_snapshots/matrix-e1-inspiration-framework-20260528_115017.md`。残余风险：未做真实 LLM inspiration smoke；`inspiration_generator.py` 是兼容 facade，不是把 router 代码迁入新模块的大重构。 |
| E2 | Inspiration 重写 B2 | 证据引用集成 | Inspiration 显示引用标记 | `evidence_refs` 字段输出 | inspiration-rewrite B2 | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`types/writing.ts:106-130` 定义 `SparkEvidenceRef` 与 `InspirationSpark.evidence_refs`，`inspirationService.ts:162-180` 将 `/inspiration/generate` 返回的 spark 列表交给 UI，`InspirationPanel.tsx:85-95` 发起生成请求，`:419-423` 在写作启发 dock 的 spark 卡片下挂载 `SparkEvidencePills`。验证：浏览器 smoke `node ..\workspace_artifacts\runtime_state\e2-e8-e9-inspiration-evidence-browser-smoke.mjs` passed，mock `/inspiration/generate` 返回带 `material_id/chunk_id/page/bbox/text` 的 `evidence_refs`，断言灵感卡片显示引用 pill；截图 `workspace_artifacts/screenshots/e2-e8-e9-inspiration-evidence-browser-smoke-20260528.png`；`npm run test -- src/components/inspiration/SparkEvidencePills.test.tsx src/services/inspirationService.test.ts --run` 14/14 passed；`npm run build` passed。成熟方案核对：本 slice 为本地 UI 验收，沿用 Playwright 官方 `page.route/route.fulfill` mock + screenshot 模式和 React 条件渲染，不需额外外部方案。Rollback：checkpoint `20260528-094854-e2-e8-e9-inspiration-evidence-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-e2-e8-e9-20260528_095840.md`。残余风险：验收入口使用当前活动 `/writing/draft` 右侧“启发”dock；旧 `/inspiration` 路由仍按 B1/B2 决策重定向到 `/dialog`。 |
| E3 | Inspiration 重写 B3 | 前端渲染 IRAC 结构 | Inspiration 显示 Issue/Rule/Application/Conclusion | 前端解析 IRAC 字段 | inspiration-rewrite B3 | 可延后/P2 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-06-05)：新增 `InspirationAnalysisChain.tsx` 渲染 `analysis_chain` 的 Issue/Rule/Application/Boundary/Counter/Conclusion 六段，显示 `confidence_reason` 与高时效 `temporal_sensitivity`；`inspirationDisplay.ts:173-208` 运行时防守解析旧 localStorage/API 中缺失或非法的结构化字段，旧 spark 继续只显示 `content`；`InspirationPanel.tsx:573` 在 `/writing/draft` → “启发联想”卡片展开区展示结构化 IRAC，`:523-531` 同步补 `aria-expanded/aria-controls` 键盘可用 disclosure；`Inspiration.tsx:150` 在旧独立 Inspiration drawer 中展示同一组件。验证：`npm run test -- src/services/inspirationService.test.ts src/pages/Inspiration.test.tsx src/components/writing/InspirationPanel.test.tsx --run` 10/10 passed；`npm run build` passed；浏览器 smoke `E3_FRONTEND_PORT=5329 node workspace_artifacts\runtime_state\e3-inspiration-irac-display-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/e3-inspiration-irac-display-20260605.png`。成熟方案核对：React 官方条件/列表渲染、WAI-ARIA disclosure pattern、本地 Inspiration rewrite plan、MinerU structured-output、PaperQA2 evidence context、Academic Research Skills typed handoff、CaseStrainer citation/evidence guardrail reference-study。Rollback：checkpoint `20260605-013050-e3-inspiration-irac-display-preflight`，实体快照 `.rollback_snapshots/e3-irac-display-preedit-20260605_013102`。残余风险：本 slice 不改后端生成质量或字段填充率；真实 LLM 若未返回 `analysis_chain`，UI 仍按旧 `content` 兜底。 |
| E4 | Inspiration 重写 B4 | FinCoT 推理链 | Inspiration 显示推理步骤 | `fincot_chain` 字段输出 | inspiration-rewrite B4 | 可延后/P2 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-06-05)：`writing.ts:129-130` 前端类型兼容可选 `fincot_chain` 与 `frame`；`inspirationDisplay.ts:62-67` 定义 FinCoT 阶段标签 Phenomenon/Driver/Indicator/Risk/Counter/Validation，`:202-230` 优先读取 `fincot_chain`，缺失时保留 E3 `analysis_chain`/IRAC 兜底；`InspirationAnalysisChain.tsx:84-96` 按 display title 渲染 `FinCoT 推理链` 或 `IRAC 分析链`；`InspirationPanel.test.tsx:304-347` 与 `Inspiration.test.tsx:191-230` 覆盖 FinCoT payload。验证：`npm run test -- src/services/inspirationService.test.ts src/pages/Inspiration.test.tsx src/components/writing/InspirationPanel.test.tsx --run` 12/12 passed；`npm run build` passed；浏览器 smoke `E4_FRONTEND_PORT=5330 node workspace_artifacts/runtime_state\e4-inspiration-fincot-display-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/e4-inspiration-fincot-display-20260605.png`。成熟方案核对：FinRobot/Financial CoT 公开论文证据、React 条件/列表渲染、WAI-ARIA disclosure pattern、本地 Inspiration rewrite plan FinCoT sections、PaperQA2 evidence context、Academic Research Skills typed handoff。Rollback：checkpoint `20260605-014135-e4-inspiration-fincot-display-preflight`，实体快照 `.rollback_snapshots/e4-fincot-display-preedit-20260605_014149`。残余风险：当前后端公开响应仍主要使用 `analysis_chain`；本 slice 补齐前端 `fincot_chain` 兼容显示，不新增后端字段填充率或真实 LLM FinCoT 质量保证。 |
| E5 | Inspiration 重写 B5 | 持久化到 DB | 无前端 UI 变化 | `inspiration_store` 表 CRUD | inspiration-rewrite B5 | 可延后/P2 | Claude(后端/接口) | ✅ 已完成并接口/持久化验收 (2026-06-05)：新增 `literature_assistant/core/inspiration_store.py`，默认落到 `workspace_artifacts/runtime_state/inspiration_store.sqlite3`，建立 `inspiration_store` SQLite 表与 `project_id/source/updated_at` 索引，提供 `InspirationStore.create/get/list/update/delete`，持久保存 `SparkResponse` JSON、query、notes、tags、metadata、created_at/updated_at/version；`inspiration_router.py` 新增 `POST/GET /inspiration/store`、`GET/PUT/DELETE /inspiration/store/{saved_id}`，用 Pydantic 边界模型校验 spark、分页、source、tags，并保留 `/inspiration/generate` 与 Evolution capture 语义不变。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_store.py -q -o addopts=""` 3/3 passed，覆盖表存在、跨实例恢复、列表过滤、更新版本、删除和 API 400 错误 envelope；`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_store.py tests\test_inspiration_router.py tests\test_inspiration_evidence_refs.py tests\test_inspiration_evolution_capture.py -q -o addopts=""` 65/65 passed；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\inspiration_store.py literature_assistant\core\routers\inspiration_router.py tests\test_inspiration_store.py` passed。成熟方案核对：FastAPI `APIRouter` + Pydantic response-model 边界、Python `sqlite3` transaction/context manager、SQLite runtime-state local table pattern、本地 PaperQA2 evidence context 与 MemPalace scoped memory reference-study。Rollback：checkpoints `20260605-014915-e5-inspiration-persistence-crud-preflight`、`20260605-015410-e5-inspiration-store-crud-continue-preedit`，实体快照 `.rollback_snapshots/e5-inspiration-persistence-preedit-20260605_014929`、`.rollback_snapshots/e5-inspiration-store-crud-continue-preedit-20260605_015410`。残余风险：本 slice 只闭合后端/local API CRUD；未新增前端“保存灵感”按钮、OpenAPI 生成物或数据库迁移工具，跨进程写并发按 SQLite/WAL 本地能力处理。 |
| E6 | Evidence refs E0 | 后端 API `/api/evidence_refs` | 无前端 UI (后端 API) | `/api/evidence_refs` 200 OK | evidence-refs E0 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-27)：`evidence_router.py:289-305` 提供 `GET /api/inspiration/evidence_refs`，复用 canonical `get_evidence_refs()` 的 `source_labels`、分页过滤；`evidence_router.py:38-75` 为独立 `/api/evidence_refs` 主链路；`python_adapter_server.py:706` 注册后应用可访问。验证：`pytest tests/test_evidence_refs_api.py -q` 10/10 passed，覆盖通过 `/api/evidence_refs` 创建 `spark` evidence ref，再由 `/api/inspiration/evidence_refs?source_labels=spark` 取回。成熟方案核对：FastAPI 官方 `TestClient` 路由测试模式。Rollback：checkpoint `20260527-234703-20260527-a4-a5-e6-evidence-refs-api-verification`，矩阵快照 `.rollback_snapshots/matrix-a4-a5-e6-20260527_235508`。残余风险：Inspiration UI 尚未接这个独立 alias；目前是后端/API 契约闭合。 |
| E7 | Evidence refs E1 | 前端调用 evidence refs | Inspiration 加载引用数据 | 前端调 `/api/evidence_refs` | evidence-refs E1 | 是/P1 应做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-28)：现行可见入口为 `/writing/draft` → 右侧写作 dock “启发”，旧 `/inspiration` 路由当前重定向到 `/dialog`，因此本 slice 在活动写作入口闭合。前端 `inspirationService.ts:190` 新增 `listEvidenceRefs()`，请求 `GET /api/inspiration/evidence_refs?page=1&page_size=12` 并用防守型 parser 解析 `refs/total/filtered_by_labels`；`InspirationPanel.tsx:89-109` 加载独立证据索引，`:187-306` 渲染“独立证据索引”列表、loading/error/empty 状态、页码/score/source_labels；`StatusBar.tsx:15-20` 修复浏览器 smoke 中 mocked budget 缺少有限 `cost_usd` 导致 ErrorBoundary 的无关崩溃。验证：`npm run test -- src/services/inspirationService.test.ts src/components/writing/InspirationPanel.test.tsx src/components/writing/StatusBar.test.tsx --run` 5/5 passed；`npm run build` passed（保留既有 `annotationApi.ts` 动静态混合 import warning）；浏览器 smoke `node ..\workspace_artifacts\runtime_state\e7-e10-inspiration-evidence-filter-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/e7-e10-inspiration-evidence-filter-20260528.png`。成熟方案核对：React controlled form/input 官方模式 + MDN `URLSearchParams.append()` 重复 query 参数模式。Rollback：checkpoint `20260527-235818-20260528-e7-e10-inspiration-evidence-filter-ui`；矩阵快照 `.rollback_snapshots/matrix-e7-e10-20260528_004451`。残余风险：独立 evidence refs 后端仍是进程内 store；UI 仅加载前 12 条，未做完整分页。 |
| E8 | Evidence refs E2 | 引用气泡渲染 | Inspiration 显示引用标记 | 前端渲染 refs | evidence-refs E2 | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`SparkEvidencePills.tsx:33-53` 对每个 spark `evidence_refs` 渲染通用 `EvidencePill`，保留 `material_id/chunk_id/page/bbox/text` 且空数组不渲染；`EvidencePill.tsx:99-171` 负责友好标签、图标、tooltip 与无 raw id 展示。验证：浏览器 smoke `node ..\workspace_artifacts\runtime_state\e2-e8-e9-inspiration-evidence-browser-smoke.mjs` passed，断言 `E2 E8 E9 anchored evidence...` 引用 pill 在灵感结果中可见；截图 `workspace_artifacts/screenshots/e2-e8-e9-inspiration-evidence-browser-smoke-20260528.png`；Vitest 14/14 passed；`npm run build` passed。成熟方案核对：Playwright locator 可见性断言 + 现有通用 EvidencePill 组件复用。Rollback：checkpoint `20260528-094854-e2-e8-e9-inspiration-evidence-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-e2-e8-e9-20260528_095840.md`。残余风险：真实后端若返回空 `evidence_refs`，UI 按契约不展示引用 pill，未合成伪证据。 |
| E9 | Evidence refs E3 | 引用点击跳转 | 引用气泡点击 → PDF Reader | 前端路由 + locator API | evidence-refs E3 | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`EvidencePill.tsx:115-141` 根据 `page/chunk/bbox` 构造 `/workbench/paper/:materialId?page=&chunk=&bbox=`，缺页码时通过 `resourcesApi.locateChunk()` 升级 locator；`ResearchWorkbench.tsx:78-88,153-165,356-364` 解析 deep-link 并生成 PDF overlay highlight。验证：浏览器 smoke `node ..\workspace_artifacts\runtime_state\e2-e8-e9-inspiration-evidence-browser-smoke.mjs` passed，点击 Inspiration 引用 pill 后 URL 进入 `/workbench/paper/mat-e2-e8-e9?page=2&chunk=chunk-e2-e8-e9&bbox=0.2,0.22,0.36,0.12`，PDF 第 2 页文本与蓝色 bbox overlay 可见；截图 `workspace_artifacts/screenshots/e2-e8-e9-inspiration-evidence-browser-smoke-20260528.png`；Vitest 14/14 passed；`npm run build` passed。成熟方案核对：React Router deep-link navigation + Playwright route/screenshot browser smoke。Rollback：checkpoint `20260528-094854-e2-e8-e9-inspiration-evidence-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-e2-e8-e9-20260528_095840.md`。残余风险：真实 PDF 高亮仍依赖后端/提取链路提供归一化 bbox；没有 bbox 时仍退化为 page/chunk 跳转。 |
| E10 | Evidence refs E4 | 引用过滤 (by source_labels) | Inspiration 显示过滤器 | 后端 source_labels 过滤 | evidence-refs E4 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `evidence_router.py:289-305` 的 `/api/inspiration/evidence_refs` 已复用 canonical evidence refs `source_labels` 过滤；前端 `inspirationService.ts:190-203` 通过 `URLSearchParams.append("source_labels", label)` 发送重复 `source_labels` 参数，`InspirationPanel.tsx:38-45` 将逗号/中文逗号/换行输入解析为去重标签，`:151-160` 应用/重置过滤，`:225-238` 展示已应用标签。浏览器 smoke 在写作启发 dock 输入 `spark, method` 后断言请求包含重复 `source_labels` 且只显示匹配证据，截图 `workspace_artifacts/screenshots/e7-e10-inspiration-evidence-filter-20260528.png`。验证：`npm run test -- src/services/inspirationService.test.ts src/components/writing/InspirationPanel.test.tsx src/components/writing/StatusBar.test.tsx --run` 5/5 passed；`npm run build` passed；`node ..\workspace_artifacts\runtime_state\e7-e10-inspiration-evidence-filter-smoke.mjs` passed。成熟方案核对：React controlled form/input 官方模式 + MDN `URLSearchParams.append()` 重复 query 参数模式。Rollback：checkpoint `20260527-235818-20260528-e7-e10-inspiration-evidence-filter-ui`；矩阵快照 `.rollback_snapshots/matrix-e7-e10-20260528_004451`。残余风险：真实 evidence volume 的跨页过滤/排序体验还未做；本次不改变旧 `/inspiration` 兼容重定向。 |
| E11 | Evidence refs E5 | 引用排序 (by relevance) | Inspiration 引用按相关性排序 | 后端 relevance score | evidence-refs E5 | 是/P1 应做 | 维护 | ✅ 已完成并测试验收 (2026-05-28)：`inspiration_router.py:167-246` 的 `build_spark_evidence_refs()` 从 chunk `score` 读取 relevance 分数，跳过缺 `material_id` 的伪引用，按 `score` 降序排序并将无分数 chunk 放到最后，默认最多 3 条、最大 cap 10 条；`inspiration_router.py:914-922` 的 `_local_spark_to_response()` 将 engine `evidence_chunks` 转成 `SparkResponse.evidence_refs`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_evidence_refs.py -q` 31/31 passed，覆盖 score 降序、无分数置后、去重、cap、空输入不伪造引用和 response 边界转换。成熟方案核对：本 slice 仅做既有纯函数排序/边界测试验收，沿用项目 Pydantic schema + pytest characterization tests，无需额外外部方案。Rollback：checkpoint `20260528-101029-e11-inspiration-evidence-relevance-sort-verifica`，矩阵实体快照 `.rollback_snapshots/matrix-e11-20260528_101110.md`。残余风险：当前排序使用 chunk 层数值 `score`，未额外接真实 reranker/provider relevance；真实检索质量由上游 chunk scoring/rerank slice 负责。 |
| E12 | Evidence refs E6 | 引用导出 | Inspiration 导出按钮 | `/api/evidence_refs/export` | evidence-refs E6 | 可延后/P2 | Claude→Codex | ✅ 已完成并本地浏览器验收 (2026-06-05)：后端 `evidence_router.py:57-72` 定义 JSON/CSV export 版本和稳定 CSV 字段，`:229-263` 复用 material/source_labels 过滤与稳定排序，`:277-324` 构造 JSON envelope 和 CSV 行，`:408-444` 新增 `GET /api/evidence_refs/export?format=json|csv`，返回下载响应并保留 `source_labels/page/bbox/bbox_unit` 等 PDF anchor 字段；服务层 `inspirationService.ts:258-278` 新增 `exportEvidenceRefs()`，用 `URLSearchParams` 传 `format/material_id/source_labels` 并解析 attachment filename；可达写作入口 `InspirationPanel.tsx:249-268` 按当前 source-label 过滤条件导出，`:321-343` 在“独立证据索引”渲染导出按钮；旧独立页 `Inspiration.tsx:442-455`、`:507-515` 也保留同一导出动作。测试覆盖：`tests/test_evidence_refs_api.py:167-269` 覆盖 JSON 导出过滤、CSV 稳定列和非法格式 422；`inspirationService.test.ts:81-90` 覆盖 query contract；`InspirationPanel.test.tsx:201-231` 覆盖 active source-label 导出；`Inspiration.test.tsx:127-146` 覆盖页面按钮。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evidence_refs_api.py -q -o addopts=""` 28/28 passed；`compileall` passed；`npm run test -- src/services/inspirationService.test.ts src/pages/Inspiration.test.tsx src/components/writing/InspirationPanel.test.tsx --run` 8/8 passed；`npm run build` passed；浏览器 smoke `E12_FRONTEND_PORT=5327 node workspace_artifacts\runtime_state\e12-inspiration-evidence-export-smoke.mjs` passed，断言 `/writing/draft` → “启发联想” → `method,spark` 过滤 → `GET /api/evidence_refs/export?format=json&source_labels=method&source_labels=spark`，截图 `workspace_artifacts/screenshots/e12-inspiration-evidence-export-20260605.png`。成熟方案核对：FastAPI/Starlette Response 下载响应、Python `csv.DictWriter` 稳定表头、MDN Blob URL/anchor download、本地 S5/S6/TheKnowledge/CaseStrainer source-anchor reference-study。Rollback：checkpoints `20260605-005028-e12-evidence-refs-export-preflight`、`20260605-005702-e12-inspiration-export-ui-preflight`、`20260605-011334-e12-evidence-export-docs-final-preedit`；实体快照 `.rollback_snapshots/e12-plan-preedit-20260605_005055`、`.rollback_snapshots/e12-ui-preedit-20260605_005702`、`.rollback_snapshots/e12-docs-final-preedit-20260605_011334`。残余风险：导出仍复用当前 evidence refs runtime store，未做 DB migration；旧 `/inspiration` 路由按 B1 重定向，实际可见闭环在 `/writing/draft` 写作入口。 |
| E13 | Causal DAG P3 | 因果图生成 | Inspiration 显示因果图 | `causal_dag_builder.py` | causal-dag P3 | 可延后/P2 | Claude→Codex | ✅ 已完成 reduced local contract slice (2026-06-05)：未新增 `causal_dag_builder.py`、LLM/provider 抽取、NER/RE、cache/backfill 或新依赖；改为在现有 Inspiration response 上新增向后兼容可选字段 `SparkResponse.causal_dag: GraphPayloadV0 | None`，复用仓内 `literature_assistant.core.graph_payload.GraphPayloadV0` 版本化节点/边 contract，并用 `tests/test_inspiration_evidence_refs.py:109-132` 覆盖该字段可序列化且不破坏无 DAG spark。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_evidence_refs.py -q -o addopts=""` 32/32 passed；`compileall` for `inspiration_router.py`、`graph_payload.py`、test file passed；`npm run build` passed。成熟方案核对：Microsoft GraphRAG entity/relationship/key-claim graph dataflow、NetworkX DAG validation pattern、现有 GraphPayload v0 contract。Rollback：checkpoint `20260605-124854-e13-e14-causal-dag-reduced-preflight`，实体快照 `.rollback_snapshots/e13-e14-causal-dag-preedit-20260605_124854`。残余风险：真实因果抽取、DAG 质量评估、provider-backed NER/RE、缓存/回填和单独 builder 命名仍属后续显式授权 slice。 |
| E14 | Causal DAG P3 前端 | 前端渲染 DAG (D3.js / Cytoscape) | Inspiration 显示交互式图 | 前端图渲染库 | causal-dag P3 | 可延后/P2 | Codex(UI) | ✅ 已完成 reduced display slice (2026-06-05)：未引入 D3/Cytoscape 或新 graph renderer；`frontend/src/types/writing.ts:139` 为 `InspirationSpark` 增加可选 `causal_dag?: GraphPayloadV0 | null`，`InspirationGraphSection.tsx:43-90` 优先渲染第一个非空 causal DAG payload，并在无 DAG 时保留现有 evidence_refs fallback 图；`GraphPayloadViewer` 继续作为统一交互图 viewer，支持 `projectId` 透传。验证：`npm run test -- src/components/inspiration/InspirationGraphSection.test.tsx --run` 2/2 passed，覆盖 explicit `causal_dag` 优先与 evidence fallback；`npm run build` passed。成熟方案核对：React Flow nodes/edges 受控 graph 渲染模式、现有 `GraphPayloadViewer`/`payloadToRf` contract。Rollback：checkpoint `20260605-124854-e13-e14-causal-dag-reduced-preflight`，实体快照 `.rollback_snapshots/e13-e14-causal-dag-preedit-20260605_124854`。残余风险：当前为只读 GraphPayload 展示，不做 DAG 编辑、大图性能预算、真实抽取质量验收或 provider smoke。 |
| E15 | IRAC P0 | IRAC 框架生成 | Inspiration 显示 IRAC 结构 | `irac_generator.py` | irac P0 | 可延后/P2 | Claude→Codex | ✅ 已完成并兼容接口验收 (2026-06-05)：新增 `literature_assistant/core/irac_generator.py`，以 `IracInspirationGenerator`、`build_irac_generator()`、`build_irac_prompt()` 和 `coerce_irac_request()` 闭合历史点名入口，强制 `frame="irac"`，复用现有 `inspiration_generator.py` facade 与 active `inspiration_router` prompt/template/local engine，不复制生成逻辑；前端 IRAC 展示已由 E3 验收。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_generator.py tests\test_inspiration_prompt_templates.py tests\test_inspiration_router.py -q -o addopts=""` 36/36 passed；`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_store.py tests\test_inspiration_generator.py tests\test_inspiration_prompt_templates.py tests\test_inspiration_router.py tests\test_inspiration_evidence_refs.py tests\test_inspiration_evolution_capture.py -q -o addopts=""` 82/82 passed；compileall for generator files passed。成熟方案核对：compatibility facade / branch-by-abstraction style wrapper、Pydantic request boundary、pytest focused characterization tests、本地 Inspiration rewrite IRAC sections。Rollback：checkpoint `20260605-020624-e15-e16-inspiration-generator-facades-preedit`，实体快照 `.rollback_snapshots/e15-e16-generator-facades-preedit-20260605_020508`。残余风险：这是稳定命名/compat facade，不是新的 LLM 质量提升；真实 provider IRAC 输出质量仍依赖现有 prompt/router链路。 |
| E16 | FinCoT P1 | FinCoT 推理链生成 | Inspiration 显示推理步骤 | `fincot_generator.py` | fincot P1 | 可延后/P2 | Claude→Codex | ✅ 已完成并兼容接口验收 (2026-06-05)：新增 `literature_assistant/core/fincot_generator.py`，以 `FinCoTInspirationGenerator`、`build_fincot_generator()`、`build_fincot_prompt()` 和 `coerce_fincot_request()` 闭合历史点名入口，强制 `frame="fincot"`，复用现有 `inspiration_generator.py` facade 与 active `inspiration_router` prompt/template/local engine，不复制生成逻辑；前端 FinCoT 兼容显示已由 E4 验收。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_generator.py tests\test_inspiration_prompt_templates.py tests\test_inspiration_router.py -q -o addopts=""` 36/36 passed；`.\.venv-1\Scripts\python.exe -m pytest tests\test_inspiration_store.py tests\test_inspiration_generator.py tests\test_inspiration_prompt_templates.py tests\test_inspiration_router.py tests\test_inspiration_evidence_refs.py tests\test_inspiration_evolution_capture.py -q -o addopts=""` 82/82 passed；compileall for generator files passed。成熟方案核对：compatibility facade / branch-by-abstraction style wrapper、Pydantic request boundary、pytest focused characterization tests、本地 Inspiration rewrite FinCoT sections 与 FinRobot/Financial CoT evidence。Rollback：checkpoint `20260605-020624-e15-e16-inspiration-generator-facades-preedit`，实体快照 `.rollback_snapshots/e15-e16-generator-facades-preedit-20260605_020508`。残余风险：这是稳定命名/compat facade，不新增后端 `fincot_chain` materialization；真实 provider FinCoT 输出质量仍依赖现有 prompt/router链路。 |

---

## F. 演化层 (Evolution / Memory Palace / Curator)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| F1 | Evolution S0 | 候选池初始化 | 无前端 UI | `candidate_store` 表创建 | evolution S0 | 是/P1 应做 | 维护 | ✅ 已完成并测试验收 (2026-05-28)：`evolution/store.py:61-145` 的 `EvolutionCandidateStore` 初始化时创建 `candidates` SQLite 表和 workspace/status/project/memory_type 索引，默认路径为 `workspace_artifacts/runtime_state/evolution_candidates.sqlite3`；`:75-91` 打开连接时启用 WAL、busy_timeout 和 synchronous=NORMAL。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_candidates.py tests\test_evolution_candidate_idempotency.py tests\test_evolution_store_concurrency.py tests\test_evolution_bug_sweep.py -q` 30/30 passed，覆盖 store 初始化、索引/读写、并发 writer、WAL/busy_timeout 探针和坏 `evidence_refs` 单行不毒化列表；成熟方案核对：本 slice 为 SQLite store 既有实现的 focused 测试验收，沿用本仓“每 public method 新连接 + WAL + online backup”模式，无需额外外部方案。Rollback：checkpoint `20260528-101459-f1-f5-evolution-candidate-store-verification`，矩阵实体快照 `.rollback_snapshots/matrix-f1-f5-20260528_101535.md`。残余风险：生产级 schema migration/versioning 仍未单独建 migration runner；当前验证是现有 schema 初始化和兼容读取。 |
| F2 | Evolution S1 | 候选生成逻辑 | 无前端 UI | `evolution_agent.py` 生成候选 | evolution S1 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口单测验收 (2026-05-28)：active 生成主线仍是 `EvolutionService.capture()` 与 inspiration/discussion/rag/runtime/skill capture extractors；本 slice 新增 `evolution_agent.py:25-109` 作为历史入口名兼容 facade，`EvolutionCandidateInput` 定义候选输入，`EvolutionAgent.generate_candidate()` 复用 authoritative service path，保留 secret scan、dedupe hash、PENDING/BLOCKED 状态和 SQLite store 写入，`build_evolution_agent()` 提供稳定入口。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_agent.py tests\test_evolution_candidates.py tests\test_evolution_router.py -q -o addopts=""` 31/31 passed，覆盖 facade 生成 pending candidate、service dedupe merge、secret scan blocked、router/list/transition 回归；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\evolution_agent.py tests\test_evolution_agent.py` passed；`git diff --check -- literature_assistant\core\evolution_agent.py tests\test_evolution_agent.py` passed。成熟方案核对：采用 facade/compatibility layer 模式闭合历史文件名，避免绕过现有 service/store 安全边界。Rollback：checkpoint `20260528-115107-f2-evolution-agent-facade`，矩阵实体快照 `.rollback_snapshots/matrix-f2-evolution-agent-20260528_115306.md`。残余风险：未把所有 capture sites 迁移到 `EvolutionAgent` facade；它是兼容入口，真实生产 capture 仍直接调用 `EvolutionService.capture()` 或 source-specific extractors。 |
| F3 | Evolution S2 | 候选评分 | 无前端 UI | `candidate_scorer.py` | evolution S2 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：当前实现将候选评分契约收敛为 `ExperienceCandidate.confidence`，`evolution_agent.py:20-49` 的 `EvolutionCandidateInput.confidence` 要求 `[0,1]`，`:59-78` 通过 `EvolutionService.capture()` 保留 secret scan、dedupe 与状态机；`evolution/store.py` candidates 表原生持久化 `confidence`，router capture 继续返回该字段。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_agent.py tests\test_evolution_candidates.py tests\test_evolution_router.py -q -o addopts=""` 36/36 passed；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用现有 facade/compatibility-layer 模式，排序参数使用 SQL allowlist 而非拼接用户输入。Rollback：checkpoint `20260528-115349-f3-f4-evolution-score-sort`，矩阵实体快照 `.rollback_snapshots/matrix-f3-f4-evolution-score-sort-20260528_115822.md`。残余风险：未新增独立 `candidate_scorer.py` 文件；矩阵原名按现行模型解释为 `confidence` 评分字段。 |
| F4 | Evolution S3 | 候选排序 | 无前端 UI | 按 score 排序 | evolution S3 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：`evolution/store.py:298-321` 为 `EvolutionCandidateStore.list()` 增加 `sort_by`，`_candidate_order_by()` 只允许 `updated_at/created_at/confidence`，其中 `confidence` 使用 `confidence DESC, updated_at DESC, candidate_id ASC`；`evolution/service.py:369-382`、`routers/evolution_router.py:83-103` 和 `evolution_agent.py:82-92` 透传排序参数，默认仍为 `updated_at` 以保持兼容。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_agent.py tests\test_evolution_candidates.py tests\test_evolution_router.py -q -o addopts=""` 36/36 passed，覆盖 store、facade、`GET /evolution/candidates?sort_by=confidence` 和非法 sort 422/ValueError；`compileall` passed；`git diff --check` passed。成熟方案核对：SQL ORDER BY allowlist 模式，避免 raw query 字段进入 SQL。Rollback：checkpoint `20260528-115349-f3-f4-evolution-score-sort`，矩阵实体快照 `.rollback_snapshots/matrix-f3-f4-evolution-score-sort-20260528_115822.md`。残余风险：排序 API 尚未接前端 UI 控件；当前为后端/API 能力闭合。 |
| F5 | Evolution S4 | 候选持久化 | 无前端 UI | `candidate_store` 表写入 | evolution S4 | 是/P1 应做 | 维护 | ✅ 已完成并测试验收 (2026-05-28)：`evolution/store.py:146-225` 的 `insert_or_merge()` 对新候选 insert、按 `dedupe_hash` 合并重复候选并拒绝 blocked-secret payload 覆盖已有 clean 行，`:527-569` 通过 `_insert_row()`/`_update_row()` 写入 SQLite，`:335-384` 提供 list/count 分页一致性；`evolution/service.py:83-151` 通过 `EvolutionService.capture()` 将 capture args 写入 store。验证：同 F1 focused pytest 30/30 passed，覆盖候选写入、重复 capture 幂等 merge、并发相同 dedupe_hash 不产生可见重复、secret merge bypass 防护、列表/count 分页和损坏 JSON 容错；成熟方案核对：本 slice 为现有 SQLite 持久化路径的 focused 测试验收，沿用项目 dedupe_hash idempotency + state-machine gated transition 模式。Rollback：checkpoint `20260528-101459-f1-f5-evolution-candidate-store-verification`，矩阵实体快照 `.rollback_snapshots/matrix-f1-f5-20260528_101535.md`。残余风险：真实 capture 来源质量、候选评分/排序和 MemPalace 晋升分别属于 F2/F3/F4/F8，不在本次 store 持久化验收内。 |
| F6 | Evolution S5 | Review UI | Evolution 页显示候选列表 | `/api/evolution/candidates` | evolution S5 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：前端 `EvolutionInbox.tsx:51-100` 读入状态 / 列表 / 审计，`:169-184` 切换状态或记忆类型时重置分页，`:344-372` 渲染候选卡列表与空态，`CandidateList.tsx:1-83` 三态列表，`CandidateCard.tsx:1-153` 以中文字段显示来源/类型/风险/证据状态并隐藏 raw JSON，`AuditPanel.tsx:1-140` 显示 `/evolution/audit` 汇总。后端 `evolution_router.py:53-88` `/evolution/status`、`:80-108` `/evolution/candidates`、`:276-330` `/evolution/audit`；`frontend/src/services/evolutionApi.ts:32-71` 使用 canonical `/evolution/*` 前缀。验证：`npm run test -- src/pages/EvolutionInbox.test.tsx src/services/evolutionApi.test.tsx --run` 31/31 passed；`tests/test_evolution_e2e_smoke.py -q` 2 passed；浏览器 smoke `node ..\workspace_artifacts\runtime_state\f6-f7-evolution-review-smoke.mjs` 通过，mock `/evolution/status`、`/evolution/candidates`、`/evolution/audit`，断言候选列表、审计面板、详情抽屉和状态按钮可见；`npm run build` passed；截图 `workspace_artifacts/screenshots/f6-f7-evolution-review-20260527.png`。成熟方案核对：本 slice 为本地 UI/浏览器验收，按 Playwright 官方 mock API + locator 断言模式实现，无需额外产品方案。Rollback：checkpoint `20260527-234143-20260527-f6-f7-evolution-review-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-f6-f7-20260527_234143`. 残余风险：矩阵旧文本写 `/api/evolution/*`，但现行 canonical 路由是 `/evolution/*`；本次只验证前端和后端现行契约，不新增兼容 alias。 |
| F7 | Evolution S6 | 用户审核 (批准/拒绝) | Evolution 页显示批准/拒绝按钮 | `/api/evolution/approve` | evolution S6 | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：`EvolutionInbox.tsx:118-170` 的保存 / 忽略 / 稍后 / 撤销 走 `acceptCandidate()`、`rejectCandidate()`、`snoozeCandidate()`、`rollbackCandidate()`，`CandidateCard.tsx:112-151` 保持动作区稳定高度并暴露状态化按钮文案；后端 `evolution_router.py:124-191` 以及 `frontend/src/services/evolutionApi.ts:74-121` 使用 `/evolution/candidates/{candidate_id}/accept|reject|snooze|rollback`。验证：`npm run test -- src/pages/EvolutionInbox.test.tsx src/services/evolutionApi.test.tsx --run` 31/31 passed；`tests/test_evolution_e2e_smoke.py -q` 2 passed；浏览器 smoke `node ..\workspace_artifacts\runtime_state\f6-f7-evolution-review-smoke.mjs` 通过，点击“保存/撤销/忽略/稍后”分别断言请求发往 `/evolution/candidates/{id}/accept|rollback|reject|snooze`；`npm run build` passed；截图 `workspace_artifacts/screenshots/f6-f7-evolution-review-20260527.png`。成熟方案核对：本 slice 为本地 UI/浏览器验收，按 Playwright 官方 mock API + locator 断言模式实现，无需额外产品方案。Rollback：checkpoint `20260527-234143-20260527-f6-f7-evolution-review-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-f6-f7-20260527_234143`. 残余风险：矩阵旧文本写 `/api/evolution/approve`，但现行 canonical 交互是四个 action endpoints；本次不做新增 alias。 |
| F8 | Evolution S7 | 批准后晋升到 MemPalace | 无前端 UI | 候选 → `memory_palace` 表 | evolution S7 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：批准后晋升链路已按现行 MemPalace adapter 架构闭合；`evolution/promotion.py:85-193` 的 `EvolutionPromoter._promote_to_memory()` 检查 adapter 启用状态后调用 `MempalaceMemoryAdapter.add_memory()`，写入内容、metadata、source_file、added_by，并返回 drawer `rollback_ref`；`evolution/service.py:241-334` 只允许 `ACCEPTED` 候选晋升，成功后转为 `PROMOTED_TO_MEMORY` 并记录 `promoted_at/rollback_ref`，重复 promote 幂等返回已有 ref；`routers/evolution_router.py:196-235` 暴露 `POST /evolution/candidates/{candidate_id}/promote`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_promotion.py tests\test_evolution_e2e_smoke.py tests\test_evolution_release_hardening.py -q -o addopts=""` 31/31 passed，覆盖 kill switch、happy path、adapter disabled/error、idempotency、rollback、router 409/404/200；`compileall` passed；`git diff --check` passed（仅既有 CRLF warning）。成熟方案核对：沿用 adapter/facade 模式和 kill switch，避免直接耦合外部 MemPalace 存储内部表。Rollback：矩阵实体快照 `.rollback_snapshots/matrix-f8-evolution-mempalace-promotion-20260528_121243.md`；本项为证据口径更新，无新增代码 checkpoint。残余风险：矩阵原写 `memory_palace` 表，但本仓权威边界是外部 MemPalace adapter/chromadb；未在本仓新增表 CRUD，F11 仍保留存储表形态缺口。 |
| F9 | Evolution S8 | Curator 定期清理 | 无前端 UI | `curator.py` 定时任务 | evolution S8 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口/单元验收 (2026-05-28)：保留 `POST /evolution/curate/run` 手动清理，新增轻量 asyncio 定时调度器 `evolution/scheduler.py`；`EvolutionCuratorScheduler.start()` 受 `evolution.curator_enabled` kill switch 保护，默认关闭，开启后按 `curator_interval_seconds()` 周期调用同一 `EvolutionCurator.run()`，`stop()` 在 lifespan shutdown 取消任务；`config.py:14-83` 增加 `curator_interval_seconds` 配置读取且下限 60s；`python_adapter_server.py:169-227` 在 FastAPI lifespan 中启动/停止 scheduler；`evolution/__init__.py` 导出 scheduler API。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_scheduler.py tests\test_evolution_curator.py tests\test_evolution_router.py -q -o addopts=""` 27 passed, 1 skipped（既有 SQLite dedupe collision 条件跳过）；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用 Python 官方 `asyncio.create_task`/取消任务模式与 FastAPI lifespan 启停资源模式；未引入 APScheduler 等新依赖，避免本地桌面应用多进程调度副作用。Rollback：checkpoint `20260528-120141-f9-evolution-curator-scheduler`，矩阵实体快照 `.rollback_snapshots/matrix-f9-evolution-curator-scheduler-20260528_120507.md`。残余风险：定时器仍默认关闭，需配置 `evolution.curator_enabled=true` 才会改写候选状态；未新增 UI 状态面板。 |
| F10 | Evolution S8.1 优先级队列 | 高优先级候选优先展示 | Evolution 页默认请求 `sort_by=confidence`，高置信候选先显示 | `GET /evolution/candidates?sort_by=confidence`，store 使用 allowlisted `ORDER BY confidence DESC, updated_at DESC, candidate_id ASC` | post-s8.1-priority-queue | 可延后/P2 | Claude→Codex | ✅ 已完成并后端/前端验收 (2026-06-05)：未新增单独 `priority_queue` 字段，改用现有 `confidence` 作为可验证优先级排序键；`evolution_router.py:79-92` 用 `Literal["updated_at","created_at","confidence"]` 校验 `sort_by` 并传给 service，`evolution/service.py:363-379` 透传到 store，`evolution/store.py:581-589` 仅允许内部 allowlist ORDER BY，拒绝未知排序；`evolutionApi.ts:52-73` 增加 typed `sortBy` 并序列化为 `sort_by`，`EvolutionInbox.tsx:165-170` 默认请求 `sortBy: "confidence"`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_evolution_candidates.py tests\test_evolution_router.py -q -o addopts=""` 32/32 passed；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\evolution\store.py literature_assistant\core\evolution\service.py literature_assistant\core\routers\evolution_router.py tests\test_evolution_candidates.py tests\test_evolution_router.py` passed；`npm run test -- src/services/evolutionApi.test.ts --run` 17/17 passed；`npm run build` passed；`F10_FRONTEND_PORT=5331 node workspace_artifacts\runtime_state\f10-evolution-priority-sort-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/f10-evolution-priority-sort-20260605.png`。成熟方案核对：SQLite 官方 ORDER BY/排序约束、FastAPI 查询参数校验、pytest focused tests、本地 Evolution S8/S8.1 runbook 与 MemPalace/agent memory reference studies。Rollback：checkpoints `20260605-021141-f10-evolution-priority-queue-reboundary-prefligh`、`20260605-021416-f10-evolution-priority-queue-frontend-preedit`；实体快照 `.rollback_snapshots/f10-evolution-priority-queue-reboundary-preedit-20260605_021156`、`.rollback_snapshots/f10-evolution-priority-frontend-preedit-20260605_021427`。残余风险：没有新增用户可切换的排序控件；当前产品行为是 Evolution 收件箱默认优先级排序，其他调用方仍可显式请求 `updated_at` 或 `created_at`。 |
| F11 | MemPalace 存储 | 长期记忆存储 | 无前端 UI | `memory_palace` 表 CRUD | memory-palace | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并后端 CRUD 验收 (2026-05-28)：在不复制外部 MemPalace 私有 schema 的前提下，`layers/m_layer_mempalace_memory.py` 增加仓内公共 `MemoryRecord` 视图与 `list_memories/get_memory/delete_memory`，底层仍使用 Chroma `PersistentClient` + collection `add/get/delete`，保留 `content_hash` 幂等写入；`routers/memory_router.py` 新增 `/api/memory_palace/memories` POST/GET、`/api/memory_palace/memories/{memory_id}` GET/DELETE，形成 repo-owned 长期记忆 CRUD alias，同时保留 `/memory/search` 与 `/api/memory_palace/search`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_memory_router.py tests\test_evolution_promotion.py tests\legacy_root\test_mempalace_integration.py -q -o addopts=""` 28/28 passed；`.\.venv-1\Scripts\python.exe -m pytest tests\test_settings_router.py tests\test_memory_router.py tests\test_evaluate_tolf_bilingual_qrels.py -q -o addopts=""` 10/10 passed；`compileall` passed；`git diff --check` passed（仅既有 CRLF warning）。成熟方案核对：Chroma 官方 collection `add/get/delete` 模式 + FastAPI response model CRUD。Rollback：`.rollback_snapshots/matrix-f11-memory-crud-20260528_182045`。残余风险：该 CRUD 是 adapter 公共边界，不新增 SQL `memory_palace` 表；真实持久化仍依赖 MemPalace/Chroma 启用与本机依赖。 |
| F12 | MemPalace 检索 | 记忆检索 API | 无前端 UI (后端 API) | `/api/memory_palace/search` | memory-palace | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：保留既有 `POST /memory/search`，新增兼容 alias `POST /api/memory_palace/search`；`routers/memory_router.py:14-51` 将搜索实现收敛到 `_search_memory_impl()` 并由 legacy/alias 两个 endpoint 复用，继续透传 `query/wing/room/limit` 到共享 MemPalace adapter；`python_adapter_server.py:606-681` 注册 `memory_compat_router`，`:594` health endpoint 自描述也列出新 alias。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_memory_router.py tests\test_evolution_promotion.py tests\test_evolution_mempalace.py -q -o addopts=""` 38/38 passed，覆盖 alias 与 legacy 返回一致、adapter 参数传递、ValueError 映射 400；`compileall` passed；`git diff --check` passed（仅既有 CRLF warning）。成熟方案核对：FastAPI 官方 `APIRouter` 兼容路由模式 + 共享 handler，避免复制业务逻辑；保留旧路径向后兼容。Rollback：checkpoint `20260528-115906-f12-memory-palace-search-alias`，矩阵实体快照 `.rollback_snapshots/matrix-f12-memory-palace-search-alias-20260528_120058.md`。残余风险：本 slice 只闭合检索 API 契约；真实 MemPalace 存储仍依赖外部 adapter/chromadb 与配置开关，F11 表 CRUD 不在本次范围。 |
| F13 | MemPalace 前端展示 | Evolution 页显示 MemPalace 内容 | Evolution 页新增“长期记忆”面板，展示 MemPalace 记忆列表 | 前端调 `/api/memory_palace/memories`，后端 CRUD alias 已有测试 | memory-palace | 可延后/P2 | Codex(UI) | ✅ 已完成并前端验收 (2026-06-05)：`memory_router.py:141-155` 已有 `GET /api/memory_palace/memories`，`tests/test_memory_router.py:193-214` 覆盖 CRUD/list；新增 `frontend/src/services/memoryPalaceApi.ts` typed list client，`frontend/src/components/evolution/MemoryPalacePanel.tsx` 在 Evolution 页面展示长期记忆的 wing/room/source/text 摘要、loading/empty/error/refresh 状态，`EvolutionInbox.tsx:406` 挂载于收纳总览下方；不新增删除、发布、同步或凭据行为。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_memory_router.py -q -o addopts=""` 4/4 passed；`npm run test -- src/services/memoryPalaceApi.test.ts src/services/evolutionApi.test.ts --run` 19/19 passed；`npm run build` passed；`F13_FRONTEND_PORT=5332 node workspace_artifacts\runtime_state\f13-mempalace-display-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/f13-mempalace-display-20260605.png`。成熟方案核对：React 官方列表 key / 条件渲染 / Effect 数据读取模式、MDN fetch/JSON response 基础模式、本地 MemPalace long-term memory reference study 与 GenericAgent memory/tool-loop study。Rollback：checkpoint `20260605-022250-f13-mempalace-frontend-display-preflight`；实体快照 `.rollback_snapshots/f13-mempalace-frontend-display-preedit-20260605_022303`。残余风险：该面板是只读最近记忆列表，不提供搜索、删除、回滚或按项目过滤；browser smoke 使用 mock API，不连真实 Chroma/MemPalace。 |

---

## G. LLM-Wiki (知识库 / Compiler / Doctor / Graph)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| G1 | Wave 0 基础架构 | Wiki 页面存储 | 无前端 UI | `wiki_pages` 表创建 | llm-wiki Wave 0 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并存储/接口验收 (2026-05-28)：当前 Wiki 基础架构采用文件页 + SQLite source/chunk registry，而非 `wiki_pages` 表；`wiki/page_store.py:96-131` 提供 `WikiPageStore`，支持根目录防逃逸、atomic markdown 写入、读取和按 kind 列表；`wiki/source_registry.py:12-35` 建立 `wiki_sources/wiki_chunks` 表，`:101-229` 提供 source upsert、chunk register、hash immutability 和查询；`wiki/service.py:126-274` 在该存储上提供页面 create/update/delete/版本记录；`wiki_router.py` 暴露 `/api/wiki/pages` CRUD/list/read/status/compile/search 等接口。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_page_store.py tests\wiki\test_source_registry.py tests\test_wiki_crud.py -q -o addopts=""` 83/83 passed，覆盖文件页安全写入/读取、registry schema 与 CRUD、WikiService CRUD；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用项目既有文件页 frontmatter + registry DB 架构，保留 markdown 可审计页面和 source/chunk 结构化索引；不新增与当前架构重复的 `wiki_pages` 表。Rollback：矩阵实体快照 `.rollback_snapshots/matrix-g1-wiki-page-storage-current-architecture-20260528_121732.md`；本项为证据口径更新，无新增代码 checkpoint。残余风险：矩阵原始表名不再作为实现目标；若未来产品决定统一 DB 页面表，需要另列迁移和兼容读取 slice。 |
| G2 | Wave 1 页面 CRUD | 创建/读取/更新/删除页面 | Wiki 页显示页面列表 | `/api/wiki/pages` CRUD | llm-wiki Wave 1 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成：`POST /api/wiki/pages` 创建页面；`PUT /api/wiki/pages/{slug}` 更新页面；`DELETE /api/wiki/pages/{slug}` 删除页面；`GET /api/wiki/pages` 列表和 `GET /api/wiki/pages/{page_path:path}` 读取已存在；`WikiService.create_page/update_page/delete_page` 方法；`WikiPageCreateRequest/WikiPageUpdateRequest/WikiPageMutationResponse` 模型；16 个测试全通过 (tests/test_wiki_crud.py)；create 自动生成 stable_slug，update 支持部分更新，delete 物理删除文件 |
| G3 | Wave 2 Markdown 渲染 | 页面内容 Markdown 渲染 | Wiki 页渲染 Markdown | 前端 Markdown 库 | llm-wiki Wave 2 | 是/P0 必做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-28)：`WikiPagePreviewPanel.tsx:2-3` 使用 `react-markdown` + `remark-gfm`，`:165-184` 将 wiki body 渲染为 Markdown prose，并保留 frontmatter/空态；浏览器 smoke 打开 `/wiki?page=claims/markdown-links.md`，mock `/api/wiki/pages/{path}` 返回含 H1、加粗、GFM table、task list、inline code 的正文，断言 heading/table cell/task text/code 均按预览区渲染。验证：`npm run build` passed（保留既有 `annotationApi.ts` 动静态混合 import warning）；`G3_G4_FRONTEND_PORT=5312 node ..\workspace_artifacts\runtime_state\g3-g4-wiki-markdown-links-smoke.mjs` passed；截图 `workspace_artifacts/screenshots/g3-g4-wiki-markdown-links-20260528.png`。成熟方案核对：本 slice 为已有 Wiki UI 的浏览器验收，沿用 `react-markdown` + `remark-gfm` 官方插件模式，无需额外产品方案。Rollback：checkpoint `20260528-005723-20260528-g3-g4-wiki-markdown-links-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-g3-g4-20260528_010033`。残余风险：本次验证 Markdown/GFM 渲染，不覆盖 Mermaid/数学公式/大型页面虚拟滚动。 |
| G4 | Wave 3 页面链接 | Wiki 内部链接 `[[page]]` | Wiki 页显示可点击链接 | 前端解析 `[[]]` 语法 | llm-wiki Wave 3 | 是/P0 必做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-28)：`WikiPagePreviewPanel.tsx:49-58` 将相对 Wiki target 补 `.md` 并按当前页面父目录解析，`:61-65` `renderableWikiMarkdown()` 将 `[[target]]`、`[[target|label]]` 转为 Markdown 链接，`:170-179` 自定义 link renderer 使用前端路由链接；浏览器 smoke 中正文 `[[sources/paper|Source paper]]` 渲染为可点击链接，断言 `href=/wiki?page=sources%2Fpaper.md`，点击后 URL 变为 `page=sources%2Fpaper.md`。验证：`npm run build` passed；`G3_G4_FRONTEND_PORT=5312 node ..\workspace_artifacts\runtime_state\g3-g4-wiki-markdown-links-smoke.mjs` passed；截图 `workspace_artifacts/screenshots/g3-g4-wiki-markdown-links-20260528.png`。成熟方案核对：本 slice 为已有 Wiki UI 的浏览器验收，沿用 React Router search-param 导航与 Markdown link renderer 模式。Rollback：checkpoint `20260528-005723-20260528-g3-g4-wiki-markdown-links-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-g3-g4-20260528_010033`。残余风险：当前只处理同库页面链接，不做不存在页面提示、反向链接或锚点段落定位。 |
| G5 | Wave 4 搜索 | Wiki 全文搜索 | Wiki 页显示搜索框 | `/api/wiki/search` | llm-wiki Wave 4 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：后端 `POST /api/wiki/search` 为 canonical 端点，`POST /api/wiki/query` 保留 legacy 别名；前端 `WikiWorkbench.tsx:83-86` 维护搜索 query/result/loading/error，`:210-226` 通过 `searchWiki()` 调用 `/api/wiki/search`，`:319-327` 挂载 `WikiSearchPanel`，`:535-625` 渲染 Wiki 检索输入框、搜索按钮、结果计数、证据页面卡片与点击后 `?page=` 预览跳转；服务层 `wikiApi.ts:441-450` 严格解析 `WikiSearchModel`，`:474-481` `POST /api/wiki/search` 请求 `{query}`。验证：`npm run test -- src/services/wikiApi.test.ts --run` 11/11 passed；浏览器 smoke `node ..\workspace_artifacts\runtime_state\g5-wiki-search-smoke.mjs` 通过，mock `/api/wiki/search` 并断言请求体 `{query:'porosity fatigue'}`、结果卡片、点击结果后 `/wiki?page=claims%2Fporosity-fatigue.md` 预览；`npm run build` passed；截图 `workspace_artifacts/screenshots/g5-wiki-search-20260527.png`。成熟方案核对：本 slice 为本地 UI/浏览器验收，按 Playwright 官方 `page.route`/locator/screenshot 模式实现 smoke，无需额外产品方案。Rollback：checkpoint `20260527-232347-20260527-g5-wiki-search-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-g5-20260527_232346`。残余风险：本次验证搜索 UI 与 canonical API 契约，不评估真实检索质量、排序召回或大索引性能。 |
| G6 | Wave 5 版本历史 | 页面版本控制 | Wiki 页显示历史按钮 | `wiki_versions` 表 | llm-wiki Wave 5 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：Wiki 文件页架构下新增本地 sidecar 版本历史，未强行引入 `wiki_versions` 表；`wiki/service.py:43-57` 提供 `list_page_versions()`，`:124-173` 将版本元数据原子写入 `wiki_root/.versions/{slug}.json`，`:184-274` 在 create/update/delete/update_extra 时追加 `create/update/delete/update_extra` 快照，记录 `version/action/status/title/body_hash/created_at_iso/updated_at_iso/recorded_at_iso`；`wiki_router.py:130-144` 定义版本响应模型，`:584-604` 新增 `GET /api/wiki/pages/{slug}/versions` 并复用现有 ACL 读权限。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_wiki_router.py tests\test_wiki_permissions.py tests\test_wiki_crud.py -q -o addopts=""` 57/57 passed，覆盖 create/update/delete 版本记录、版本 API、权限过滤和 OpenAPI 注册；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用项目现有 JSON sidecar + tmp file + `os.replace` 原子写模式，与 WikiPageStore 文件页结构兼容；版本 API 只暴露元数据和 body hash，不泄漏历史正文。Rollback：checkpoint `20260528-120746-g6-wiki-page-version-history`，矩阵实体快照 `.rollback_snapshots/matrix-g6-wiki-page-version-history-20260528_120936.md`。残余风险：前端历史按钮/查看器尚未接入；若未来切换 DB 存储，可把 sidecar 迁移为 `wiki_versions` 表。 |
| G7 | Wave 6 Compiler | 自动生成 Wiki 页面 | 无前端 UI | `wiki_compiler.py` | llm-wiki Wave 6 | 是/P0 必做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：`wiki/compiler.py` 既有确定性写入能力已通过 router 暴露为受控写入；`wiki_router.py:104-108` 为 `WikiCompileRequest` 增加 `allow_write` 显式确认，`:403-437` 保持 `dry_run=true` 默认，`dry_run=false` 且未传 `allow_write=true` 时 400 拒绝，确认后调用 `WikiCompiler.compile_source/compile_project(dry_run=False)` 写入 WikiPageStore；`:290-294` 新增 `_written_paths_for_compile()` 返回实际已写入路径，避免调用者靠文件系统推断。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_wiki_router.py tests\wiki\test_compiler.py -q -o addopts=""` 38/38 passed，覆盖 dry-run、无确认拒绝、确认后写入 `sources/write-paper.md`、OpenAPI `allow_write` 字段；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用 FastAPI 显式请求字段 + response_model 模式，写入操作采用确认字段防误触；不引入真实 LLM 调用，继续复用 deterministic compiler 和现有预算 guard。Rollback：checkpoint `20260528-121019-g7-wiki-compiler-write-confirmation`，矩阵实体快照 `.rollback_snapshots/matrix-g7-wiki-compiler-write-confirmation-20260528_121148.md`。残余风险：当前写入路径是确定性 source/paper 页面生成，不是完整 LLM 自动概念/claim 批量抽取；前端触发写入按钮仍需 UI slice。 |
| G8 | Wave 7 Doctor | Wiki 一致性检查 | Wiki 页显示检查报告 | `wiki_doctor.py` | llm-wiki Wave 7 | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`wikiApi.ts:484` 读取 `GET /api/wiki/doctor` 并经 `parseWikiDoctor()` 防守解析，`WikiWorkbench.tsx:68-70,126,348-353` 维护 doctor 状态/错误并挂载 `DoctorReportPanel`，`DoctorReportPanel.tsx:77-160` 渲染健康诊断报告、集成警告、总览计数、分组检查、可执行动作与空/错误/加载态；浏览器 smoke `workspace_artifacts/runtime_state/g8-wiki-doctor-smoke.mjs:159-196` mock `/api/wiki/doctor`，断言 “Wiki 集成尚未启用。”、总览、提醒、图谱索引、修复命令、可自动修复和 OK 检查均可见。验证：`G8_FRONTEND_PORT=5323 node workspace_artifacts/runtime_state/g8-wiki-doctor-smoke.mjs` passed；截图 `workspace_artifacts/screenshots/g8-wiki-doctor-report-20260528.png`。成熟方案核对：本 slice 为已有 Wiki Doctor UI 的本地浏览器验收，无需额外外部方案，沿用 Playwright `page.route`/locator/screenshot smoke 模式。Rollback：checkpoint `20260528-065137-g8-wiki-doctor-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-g8-doc-update-20260528_065954.md`。残余风险：本次验证 UI 对 doctor payload 的呈现与警告，不评估真实 Wiki 诊断算法准确率或自动修复效果。 |
| G9 | Wave 8 Graph 基础 | 页面关系图数据 | 无前端 UI | `/api/wiki/graph` 返回图数据 | llm-wiki Wave 8 | 是/P0 必做 | 维护 | ✅ 已完成并接口验收 (2026-05-28)：`wiki_router.py:859-865` 提供 `GET /api/wiki/graph`，启用时对当前用户授权后的 page store 调 `build_wiki_graph()` 并返回 `WikiGraphResponse(enabled, graph)`；`wiki/graph.py` 提供 wikilink/frontmatter relation 抽取、`WikiGraphStore` JSON/SQLite 持久化、backlinks、blast radius 和 deterministic export。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_graph.py tests\wiki\test_graph_extended.py tests\wiki\test_wiki_router.py::test_doctor_and_graph_contract_when_enabled tests\test_wiki_permissions.py::TestWikiPermissionEnforcement::test_graph_filters_unreadable_nodes -q` 41/41 passed，覆盖 wikilink 忽略 code、frontmatter supports/contradicts、graph JSON/SQLite 持久化、空图/大图生命周期、`/api/wiki/graph` 返回 `node_count=2`、按 user_id 过滤不可读节点；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\wiki literature_assistant\core\routers\wiki_router.py` passed。成熟方案核对：本 slice 为已有 Wiki graph API 的本地接口/单元验收，沿用项目 graph snapshot + SQLite persistence + FastAPI TestClient 模式，无需额外外部方案。Rollback：checkpoint `20260528-101331-g9-wiki-graph-api-verification`，矩阵实体快照 `.rollback_snapshots/matrix-g9-20260528_101435.md`。残余风险：Graph UI 交互和大规模图性能属于 G10/G15 后续体验/性能 slice；本次只闭合 graph 数据 API 与权限过滤。 |
| G10 | Wave 9 Graph 前端 | 前端渲染知识图谱 | Wiki 页显示交互式图 | 前端图渲染库 | llm-wiki Wave 9 | 是/P0 必做 | Codex(UI) | ✅ 已完成并浏览器验收 (2026-05-27)：Wiki 页 `GraphDebugPanel` 已从 payload/debug 展示升级为 React Flow 交互图；`GraphDebugPanel.tsx:10` 引入 `@xyflow/react`，`:101` `graphToFlow()` 将 `/api/wiki/graph` structured graph 转为 nodes/edges 并经 dagre 布局，`:236` 稳定容器 `data-testid="wiki-interactive-graph"` 渲染拖拽/缩放图谱，`:133` `WikiGraphNodeDetail` 点击节点显示页面、类型、frontmatter id 和 metadata；保留 graph stats、节点/关系概览和刷新入口；验证：官方 React Flow 容器尺寸/fitView/Controls/Background 模式已核对，`npm run test -- src/components/wiki/GraphDebugPanel.test.tsx --run` 3/3 passed，`npm run build` passed，Playwright smoke 打开 `/wiki`、mock `/api/wiki/graph`、点击 `First claim` 并断言 `claim-frontmatter-1` 详情，截图 `workspace_artifacts/screenshots/g10-wiki-interactive-graph-20260527.png`；回档：`20260527-205436-g10-wiki-interactive-graph-ui` + 矩阵实体快照 `.rollback_snapshots/matrix-g10-20260527_210850`；剩余：当前为只读图谱浏览，不做图编辑；超大图谱后续需要过滤/聚合/性能预算 |
| G11 | Wave 10 标签系统 | 页面标签 | Wiki 页显示标签 | `wiki_tags` 表 | llm-wiki Wave 10 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并前端/接口验收 (2026-05-28)：前端标签展示已浏览器验收：`WikiPagePreviewPanel.tsx:79-97` 从 frontmatter 的 `tags/labels/categories/category` 兼容提取标签并去重，`:201-236` 用摘要与标签胶囊展示，浏览器 smoke `G11_FRONTEND_PORT=5321 node workspace_artifacts/runtime_state/g11-wiki-tags-metadata-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/g11-wiki-tags-metadata-20260528.png`。本轮补齐后端只读标签索引：`wiki_router.py:54-64` 定义 `WikiTagPayload/WikiTagsResponse`，`:317-344` 从 frontmatter `tags/labels/category/categories` 派生去重 tag key，`:540-558` 新增 `GET /api/wiki/tags`，基于授权可读页面生成 tag→pages 索引并尊重 ACL。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_wiki_router.py tests\test_wiki_permissions.py tests\test_wiki_crud.py -q -o addopts=""` 61/61 passed，覆盖 tags/labels/category 派生、权限过滤和 OpenAPI 注册；`compileall` passed；`git diff --check` passed；此前前端 `npm run test -- src/components/wiki/WikiPagePreviewPanel.test.tsx --run` 1/1 passed、`npm run build` passed。成熟方案核对：沿用 frontmatter 作为 Wiki 文件页元数据源和 FastAPI response_model；不引入未使用表或迁移。Rollback：checkpoints `20260528-062439-g11-wiki-tags-metadata-ui`、`20260528-121401-g11-wiki-tags-index-api`，矩阵实体快照 `.rollback_snapshots/matrix-g11-wiki-tags-index-api-20260528_121544.md`。残余风险：当前为只读派生标签索引，不是独立 `wiki_tags` 表 CRUD；若未来需要全局标签管理/重命名，再另列持久表 slice。 |
| G12 | Wave 11 分类 | 页面分类/目录 | Wiki 页显示目录树 | `wiki_categories` 表 | llm-wiki Wave 11 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并接口验收 (2026-05-28)：新增只读分类树 API `GET /api/wiki/categories`，从当前文件页存储和 frontmatter 派生目录结构，不强行引入不存在的 `wiki_categories` 表；`wiki_router.py:43-52` 定义 `WikiCategoryNodePayload/WikiCategoriesResponse`，`:294-338` 从 `category/categories` 或页面 `kind` 生成安全 key 与层级树，`:490-507` 在授权可读页面集合上返回分类节点和页面摘要，沿用现有 wiki ACL 过滤。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\wiki\test_wiki_router.py tests\test_wiki_permissions.py tests\test_wiki_crud.py -q -o addopts=""` 54/54 passed，覆盖 frontmatter 分类、kind fallback、权限过滤和 OpenAPI 注册；`compileall` passed；`git diff --check` passed。成熟方案核对：沿用现有 WikiPageStore/frontmatter 模式和 FastAPI response_model；分类树采用只读派生视图，避免在当前文件页架构中新增未使用的表。Rollback：checkpoint `20260528-120541-g12-wiki-categories-tree-api`，矩阵实体快照 `.rollback_snapshots/matrix-g12-wiki-categories-tree-api-20260528_120705.md`。残余风险：前端目录树控件尚未接入；若未来需要跨项目独立分类管理，再另列 `wiki_categories` 持久表/CRUD slice。 |
| G13 | Wave 12 模板 | Wiki 页面模板 | Wiki 页显示模板选择器 | `wiki_templates` 表 | llm-wiki Wave 12 | 可延后/P2 | Claude(后端/接口) | ❓ 未定位 |
| G14 | Wave 13 权限 | 页面访问控制 | Wiki 页显示权限设置 | `wiki_permissions` 表 | llm-wiki Wave 13 | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成并复核修正 (2026-05-27)：默认权限从“无权限=公开”改为“无权限=local-user 私有”；`wiki/permissions.py:17` local owner、`:32` `WikiPagePermissions`、`:114` `can_read`、`:150` `can_write` fail-closed；`wiki_router.py:425` `_authorized_page_store` 统一过滤，已接入 status/pages/read/create/update/delete/search/export/graph/doctor (`:513`, `:597`, `:682`, `:701`, `:741`, `:788`, `:825`, `:860`)；`wiki/service.py:166` 新建页默认 private，`:259` 修复 live service 导入路径；OpenAPI 已刷新 (`frontend/src/generated/openapi.ts:1671` permissions, `:1766` search, `:1566` export)；验证：`compileall` pass，`pytest tests/test_wiki_permissions.py tests/test_wiki_crud.py tests/test_wiki_export.py tests/wiki/test_wiki_router.py -q` 60/60 passed，`npm run generate:openapi` passed，`npm run build` passed；回档：`.rollback_snapshots/g14-wiki-permissions-20260527_073200`、`.rollback_snapshots/g14-openapi-20260527_073920` |
| G15 | Wave 14 导出 | Wiki 导出 (Markdown/PDF) | Wiki 页显示导出按钮 | `/api/wiki/export` | llm-wiki Wave 14 | 是/P1 应做 | Claude→Codex | ✅ Markdown 导出已完成并浏览器验收 (2026-05-27)：后端 `POST /api/wiki/export` 调用 `export_wiki_markdown` 将 Wiki 页面打包为 Markdown zip，默认输出 `workspace_artifacts/wiki_exports/wiki_export_{timestamp}.zip`，`WikiExportResponse` 返回 `success/page_count/output_path/errors`；前端 `WikiWorkbench.tsx:87-90` 维护 export 状态，`:228-239` 调用 `exportWikiMarkdown()`，`:303-310` 将导出动作接入 Wiki 编译知识层卡片，`:470-481` 渲染“导出 Markdown”按钮，`:490-503` 展示页数和高级导出位置；服务层 `wikiApi.ts:451-459` 解析 `WikiExportModel`，`:513-519` `POST /api/wiki/export`。验证：`pytest tests/test_wiki_export.py -q` 8 passed；浏览器 smoke `node ..\workspace_artifacts\runtime_state\g15-wiki-export-smoke.mjs` 通过，mock `/api/wiki/export` 后点击“导出 Markdown”，断言 `POST` 被触发、显示“已导出 3 页。”与 `workspace_artifacts/wiki_exports/wiki_export_20260527_g15.zip`；`npm run build` passed；截图 `workspace_artifacts/screenshots/g15-wiki-export-20260527.png`。成熟方案核对：本 slice 为本地 UI/浏览器验收，按 Playwright 官方 `page.route`/locator/screenshot 模式实现 smoke，无需额外产品方案。Rollback：checkpoint `20260527-233256-20260527-g15-wiki-export-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-g15-20260527_233255`。残余风险：矩阵标题包含 PDF，但当前 canonical endpoint 和 UI 明确为 Markdown zip 导出；PDF 导出仍需后续后端格式能力与前端格式选择 slice，不能因 Markdown 完成而声称 PDF 已实现。 |
| G16 | Wave 15 导入 | Wiki 导入 (Markdown/Notion) | Wiki 页显示导入按钮 | `/api/wiki/import` | llm-wiki Wave 15 | 可延后/P2 | Claude(后端/接口) | ✅ 后端本地 Markdown 导入已完成并验收 (2026-06-05)：按当前 dirty worktree 安全边界把 G16 收窄为 backend-only Markdown 本地导入，不接 Notion、外部 URL、压缩包、前端按钮、graph/index rebuild、部署、上传、发布、凭据或真实外部资源；`wiki_router.py:239-276` 新增 `WikiImportRequest/Item/Response`，`:377-467` 增加导入路径白名单、`.md` 限制、`github/`/`.git`/`.rollback_snapshots` 保护、大小/数量 guard、标题提取、auto marker 清理和 source provenance，`:686-810` 新增 `POST /api/wiki/import`，支持 `dry_run` 默认 true、`overwrite` 默认 false、默认 private ACL owner、创建/更新/跳过/错误逐项返回。验证：`.\\.venv-1\\Scripts\\python.exe -m pytest tests\\test_wiki_crud.py tests\\test_wiki_permissions.py -q -o addopts=\"\"` 43/43 passed；`compileall` passed；`git diff --check` passed with existing CRLF warning only。测试覆盖：dry-run plan、apply create、existing skip、protected `github/` path、non-md rejection、真实 `WikiService` isolated store 落盘并写入 private ACL/provenance。成熟/官方方案核对：FastAPI Pydantic request body/response model、Python `Path.resolve()` + `relative_to()` containment、CommonMark Markdown plain-text input；本地参考：llmwiki-cli local Markdown tooling、llm-wiki compiler provenance/review、Obsidian local edit protection、WikiLoom ingest provenance、OpenKB compiled wiki/pageindex、S7 wiki graph evidence sidecar。Rollback：checkpoint `20260605-040314-g16-wiki-import-mini-plan-preedit`，实体快照 `.rollback_snapshots/g16-wiki-import-mini-plan-preedit-20260605_040313`。残余风险：前端 Wiki 导入按钮和 Notion/import connector 未做，需在 Wiki UI dirty work 可集成后另开 UI slice。 |

---

## H. 写作系统 (Writing Project / Outline / Citations / Figures)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| H1 | Writing Overview 页 | 写作项目总览 | Writing 页显示项目列表 | `/api/writing/projects` | git status (WritingOverview.tsx) | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：后端 canonical `/api/writing/projects` 已存在并 alias 到 resources 项目层；前端项目 CRUD 已切到 canonical API，`writingBackend.ts:79-128` 的 create/get/list/status/delete 均使用 `/api/writing/projects`，`WritingOverview.tsx:18` 读取 `activeProjectId`，`:28-39` 拉取当前项目统计，`:67-83` 显示项目激活状态与刷新，`:90-115` 展示总字数/引用文献/章节/审校轮次；统计端点仍使用现有 `/resources/project/{project_id}/stats` (`writingBackend.ts:595-599`)；验证：`pytest tests/test_writing_projects_api.py -q` 6 passed，`npm run build` passed，Playwright smoke 打开 `/writing`、mock `/api/writing/projects` + stats，断言无 `/resources/projects` 列表请求、项目选择器激活 `project-h1`、统计数字渲染，截图 `workspace_artifacts/screenshots/h1-writing-overview-20260527.png`；回档：`20260527-210953-h1-writing-overview-contract-smoke` + 矩阵实体快照 `.rollback_snapshots/matrix-h1-20260527_211635`；剩余：项目 stats/detail 仍沿用 resources 层端点，后续可按 API 统一策略补 `/api/writing/projects/{id}/stats` alias |
| H2 | Outline Manager 页 | 大纲管理 | Writing 页显示大纲编辑器 | `/api/writing/outline` CRUD | git status (OutlineManager.tsx) | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：`OutlineManager.tsx:173` 从 `/api/writing/outline` 读取 section-backed outline，`:68-80` 章节标题/说明为受控输入，`:278-291` 上下移动和保存时扁平化为 `OutlineItemPayload[]`，`:332-337` 新增“保存大纲”，`:261` 删除走 canonical `deleteOutlineItem()`；`writingBackend.ts:222-263` 封装 `getOutline()`/`updateOutline()`/`deleteOutlineItem()`；后端 `writing_router.py:575-602` `PUT /api/writing/outline` 保存 title/order/description，`Body(default_factory=list)` 修正请求体与可变默认值，`:605-612` `DELETE /api/writing/outline/{item_id}` 删除 section-backed item；`tests/test_writing_outline_api.py` 新增真实 update/delete 行为测试。验证：`pytest tests/test_writing_outline_api.py -q` 9/9 passed；`node ..\workspace_artifacts\runtime_state\h2-outline-manager-crud-smoke.mjs` passed，浏览器打开 `/writing/outline` 后 mock 读取两条大纲、编辑“方法设计”为“证据与方法”、填写说明、上移、保存并断言 PUT body 顺序和字段，再删除“研究背景”并断言 DELETE `/api/writing/outline/sec-h2-background`；`npm run build` passed；截图 `workspace_artifacts/screenshots/h2-outline-manager-crud-20260527.png`。成熟方案核对：React 官方 controlled input + immutable array update 模式；本 slice 用现有按钮式重排而非引入拖拽库。Rollback：checkpoint `20260527-220357-h2-outline-manager-crud-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-h2-20260527_221914`。残余风险：没有真正拖拽交互，当前为上移/下移按钮；层级 parent_id 保留现有结构，暂未提供新增子章节/跨层级调整 UI。 |
| H3 | Sources Citations 页 | 文献引用管理 | Writing 页显示引用列表 | `/api/writing/citations` CRUD | git status (SourcesCitations.tsx) | 是/P0 必做 | Claude→Codex | ✅ 已完成并前端/后端契约验收 (2026-05-28)：前端保持 2026-05-27 已验收链路，`SourcesCitations.tsx:73-133` 从 `/api/writing/citations/sources` + materials/chunks/drafts 拉取真实来源并汇总 chunk/page/bbox，`:181-205` 提供 AI 推荐与 citation anchor 复制，`:282-283` 明确 citation anchor 不等于 Word 字段式交叉引用，`:384-399` 通过 `buildWorkbenchPath()` 将来源跳转 PDF Reader；后端 `writing_router.py:687-816` 提供 citation sources/update/suggest，`writing_resources.py:31-133,600,647,2178-2213` 负责 citation anchor 序列化与恢复。本次补齐结构化 bibliography/export 契约：`models/resources.py:164-184` 新增 `ProjectExportBibliographyEntryPayload`，`ProjectExportPayload.bibliography_entries` 纳入 OpenAPI；`resources_router/_export_helpers.py:80-121` 从 project materials 生成稳定 `citation_key/material_id/ordinal/title/type/authors/year/venue/doi/url/summary/display_text`，`:254-259` 把 bibliography entries 写入 academic export，`:361-369`、`:477-484`、`:566-569` 让 Markdown/LaTeX/DOCX 参考文献区共用结构化 display text，file export payload 也返回 `bibliography_entries`。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_resources_export_contract.py tests\test_writing_submission_export.py tests\test_writing_citations_api.py -q -o addopts=""` 18/18 passed；`compileall` passed；`npm run generate:openapi` passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；既有浏览器截图 `workspace_artifacts/screenshots/h3-sources-citations-20260527.png` 继续证明 UI。成熟方案核对：参考 CSL/DOI 常见 bibliography 字段做最小结构化 schema，沿用 FastAPI response model + pytest/TestClient + 现有 exporter 模式，不引入 citeproc 或 Word field-code 依赖。Rollback：checkpoint `20260528-102056-h3-bibliography-export-contract`，矩阵实体快照 `.rollback_snapshots/matrix-h3-bibliography-20260528_103015.md`。残余风险：仍不是 Word 插件式字段交叉引用，也不是完整 GB/T 7714/APA/MLA citeproc 排版；当前是基于现有材料元数据的确定性 bibliography/export 契约。 |
| H4 | Figures Tables 页 | 图表管理 | Writing 页显示图表列表 | `/api/writing/figures` CRUD | git status (FiguresTables.tsx) | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：`FiguresTables.tsx:109-136` 从 `/api/writing/figures` + `/api/writing/figures/candidates` 并发加载真实资产与文本派生候选，`:171-216` 允许候选登记为资产、刷新/删除资产，`:247-300` 提供视图切换、筛选和上传入口，`:511-608` 以编号、题注状态、文献名、定位状态和资产预览渲染，不显示 `material/chunk/bbox/asset_path` 原始上下文；`writingBackend.ts:555-584` 封装 list/create/update/delete figure asset，`:551-559` 和 `:564-584` 使用 canonical `/api/writing/figures` 路由；后端 `writing_router.py:841-934` 提供 `GET/POST/PUT/DELETE /api/writing/figures` 与 `/api/writing/figures/candidates` alias，`writing_resources.py:347-398`、`:2079-2157` 提供 SQLite 持久化 figure asset CRUD，`models/resources.py:380-427` 定义 figure asset/request 模型。验证：`.\\.venv-1\\Scripts\\python.exe -m pytest tests\\test_writing_figures_api.py -q` 9/9 passed；`npm run generate:openapi` passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/h4-figure-assets-crud-smoke.mjs` passed，断言项目激活后能加载真实资产与候选、候选登记会发起 `POST /api/writing/figures` 并保留 `material_id/source_page/bbox`，删除会命中 `DELETE /api/writing/figures/{asset_id}`；截图 `workspace_artifacts/screenshots/h4-figure-assets-crud-20260528.png`。成熟方案核对：FastAPI 官方 `response_model` / `APIRouter` 路由模式、Pydantic `BaseModel`/`Field` 约束、React 受控输入与列表过滤模式。Rollback：checkpoint `20260528-072643-h4-figure-assets-verification`；矩阵实体快照 `.rollback_snapshots/matrix-h4-figure-assets-20260528_073550`。残余风险：真实图表裁剪/自动编号绑定/从 PDF locator 直接生成资产仍是后续 slice；本页目前是资产登记与候选管理，不是完整的图表提取流水线。 |
| H5 | Reviewer Submission 页 | 审稿人提交 | Writing 页显示提交表单 | `/api/writing/submit` | git status (ReviewerSubmission.tsx) | 是/P0 必做 | Claude→Codex | ✅ 已实现并浏览器验收 (2026-05-27)：前端 `ReviewerSubmission.tsx:92-116` 调用 `submitForReview()` 并提交 project/reviewer/message/include_drafts/include_materials，`:139-156` 顶部“准备投稿/刷新”动作，`:203-267` 显示审稿人邮箱、提交说明、包含草稿/来源和提交成功回执；服务层 `writingBackend.ts:578-585` 调 `POST /api/writing/submit`；类型 `resources.ts:206-211` 使用 OpenAPI `SubmitForReviewRequest/SubmissionResponsePayload`；后端 `writing_router.py:917-984` 生成 `workspace_artifacts/generated/output/writing_submissions/{project_id}/{submission_id}/submission_manifest.json` 和 `README.md`，返回 `submission_id/status/submitted_at/reviewer_email/package_path`；回归 `tests/test_writing_submission_export.py:30-105` 验证真实项目、章节、草稿、来源和本地 package 文件；验证：`pytest tests/test_writing_submission_export.py -q` 7 passed，`compileall` pass，`npm run build` passed；浏览器在 `http://127.0.0.1:5196/writing/reviewer` 对临时项目 `proj_dc632abbb62a` 点击“准备投稿”生成 `sub_5ea53601743a` 和本地包路径，截图 `workspace_artifacts/screenshots/h5-reviewer-submit-20260527.png`；说明：当前是本地审稿包交付，不发送外部邮件、不对接真实期刊投稿系统 |
| H6 | 引用跳转 PDF | Citations 点击 → PDF Reader | Citations 列表项点击跳转 | 前端路由 + locator API | chunk-page-locator L4 | 是/P0 必做 | Codex(UI) | ✅ 写作来源引用跳转 PDF Reader 已完成并浏览器验收 (2026-05-28)：与 A11 同一链路，`SourcesCitations.tsx:88-103` 从 `/resources/chunks` 汇总首个 `chunk_id/page/bbox`，`:384-388` 每条真实来源提供 PDF Reader 内部链接，`:626-645` 生成 `/workbench/paper/{materialId}?page=&chunk=&bbox=`；`ResearchWorkbench.tsx:38-58` 解析 bbox，`:153-164` 生成“当前跳转证据位置”高亮。验证：`npm run build` passed；`A11_H6_FRONTEND_PORT=5315 node workspace_artifacts/runtime_state/a11-h6-writing-sources-pdf-bbox-smoke.mjs` passed，断言来源页链接携带 `page/chunk/bbox`，点击后进入 PDF 第 2 页并显示蓝色 overlay；截图 `workspace_artifacts/screenshots/a11-h6-writing-source-pdf-bbox-20260528.png`。成熟方案核对：React Router `<Link>` + `URLSearchParams`；沿用 A8/C11 PDF.js overlay。Rollback：checkpoints `20260528-020032-20260528-a11-h6-writing-source-pdf-bbox-jump`、`20260528-021801-a11-h6-writing-sources-pdf-bbox-smoke`，矩阵快照 `.rollback_snapshots/matrix-a11-h6-20260528_023500`、`.rollback_snapshots/matrix-h6-sync-20260528_023607`。残余风险：这仍是 citation anchor/source manager 到 PDF 的跳转，不是 Word 字段式交叉引用；真实高亮精度依赖后端返回归一化 bbox。 |
| H7 | 大纲自动生成 | AI 生成大纲 | Outline 页显示"生成"按钮 | `/api/writing/outline/generate` | writing-outline-generation | 是/P1 应做 | Claude→Codex | ✅ 已实现并浏览器验收 (2026-05-27)：前端 `OutlineManager.tsx:156-184` 生成请求与状态处理，`:216-286` 生成大纲面板/主题/重点/字数/写入按钮，`:372-417` 将 `OutlineItemPayload` 渲染为层级树；服务层 `writingBackend.ts:231-240` `POST /api/writing/outline/generate`；类型 `resources.ts:65-72` 复用 OpenAPI `OutlinePayload/GenerateOutlineRequest`；后端 `writing_router.py:175` AI JSON 解析，`:202` 无 AI/解析失败 fallback，`:277` 持久化为 section，`:435-478` 生成入口；回归 `tests/test_writing_outline_api.py:53-133` 验证生成非空、二级 parent_id、GET 读回；验证：`pytest tests/test_writing_outline_api.py -q` 8 passed，`compileall` pass，`npm run build` passed，浏览器在 `http://127.0.0.1:5194/writing/outline` 生成并显示 5 个章节，截图 `workspace_artifacts/screenshots/h7-outline-generate-20260527.png`；说明：无真实 AI 凭证时走确定性 fallback，配置 AI 后会解析模型 JSON 输出 |
| H8 | 引用自动补全 | AI 推荐引用 | Citations 页显示推荐列表 | `/api/writing/citations/suggest` | writing-citation-suggest | 是/P1 应做 | Claude→Codex | ✅ 已实现并浏览器验收 (2026-05-27)：前端 `SourcesCitations.tsx:177-195` 调用 `/api/writing/citations/suggest`，`:210-219` 推荐卡复制 `[^cite:{material_id}]`，`:311-321` 推荐面板接入 attempted/loading/copy 状态，`:423-500` 显示推荐标题、相关度、excerpt、rationale、无结果提示和“引用/已复制”动作；后端 `writing_router.py:392-438` 检索结果转推荐，`:440-510` material 元数据 fallback，`:757-816` suggest endpoint 校验项目/草稿/上下文、优先 chunk search、失败或无索引时 fallback；回归 `tests/test_writing_citations_api.py:47-118` 验证无 chunk index 时按 material 标题/摘要/focus_points 推荐；验证：`pytest tests/test_writing_citations_api.py -q` 7 passed，`compileall` pass，`npm run build` passed，浏览器在 `http://127.0.0.1:5195/writing/sources` 对临时项目返回 `Porosity defect fatigue mechanism`、87% 匹配、`元数据匹配` 理由，点击“引用”后显示“已复制”，截图 `workspace_artifacts/screenshots/h8-citation-suggest-20260527.png`；说明：这不是自动插入 Word 交叉引用，当前复制 citation anchor token，参考文献目录生成仍归 H3/H10 后续 |
| H9 | 图表自动生成 | AI 生成图表 | Figures 页显示"生成"按钮 | `/api/writing/figures/generate` | writing-figure-generation | 可延后/P2 | Claude→Codex | ✅ 已完成并本地验收 (2026-06-05)：按当前本地边界把“自动生成”收窄为确定性本地图表资产生成，不调用图片模型、真实 AI、生产/付费资源或新凭据；`models/resources.py:538-566` 新增 `GenerateFigureAssetsRequest/Response`，`models/__init__.py` 导出模型；`writing_router.py:1126-1193` 新增 `POST /api/writing/figures/generate`，从已有 chunk-backed pixel candidates 生成持久 `FigureAssetPayload`，保留 `material_id/source_page/bbox/asset_path/format`，并跳过已登记 `asset_path` 防重复；`writingBackend.ts:168-184,637-645` 增加 typed service；`FiguresTables.tsx:19,113,287-313,382-393` 增加“生成”按钮、loading/error guard、生成后插入已保存图表并移除同 asset_path 候选。验证：`.\\.venv-1\\Scripts\\python.exe -m pytest tests\\test_writing_figures_api.py -q -o addopts=\"\"` 16/16 passed；`compileall` passed；`npm run test -- src/services/writingBackend.test.ts src/pages/writing/FiguresTables.test.tsx --run` 2/2 passed；`npm run build` passed；browser smoke `H9_FRONTEND_PORT=5333 node workspace_artifacts\\runtime_state\\h9-writing-figure-generate-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/h9-writing-figure-generate-20260605.png`；`git diff --check` passed with existing CRLF warnings only。成熟/官方方案核对：FastAPI response model/request-body pattern、Pydantic BaseModel/Field validation、React immutable array state updates、Nature Research Figure Guide 的图表规格与来源/可编辑性原则；本地参考：Nature Skills 图表契约研究、S17 source-folder figure/table candidate smoke。Rollback：checkpoint `20260605-023051-h9-writing-figure-generation-audit-preflight`、`20260605-023432-h9-writing-figure-generate-local-preedit`；实体快照 `.rollback_snapshots/h9-writing-figure-generation-audit-preedit-20260605_023105`、`.rollback_snapshots/h9-writing-figure-generate-local-preedit-20260605_023432`。残余风险：这不是真实 AI 图片生成或投稿级绘图脚本；它只把已由切块/PDF 图表加载产生的像素候选登记成写作图表资产，复杂数据绘图、统计 QA、source-data manifest 和期刊 profile 仍需后续专门 slice。 |
| H10 | 写作项目导出 | 导出 Word/LaTeX/PDF | Writing 页显示导出按钮 | `/api/writing/export` | writing-export | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `models/resources.py:172-187` 扩展 `ProjectExportPayload`，新增 `content_base64/media_type/file_path` 以承载生成文件；`resources_router/_export_helpers.py:21-307` 将 export format 扩展为 `markdown/json/word/latex/pdf`，复用同一学术导出内容生成 Markdown，新增 LaTeX 转义与 `.tex` 内容、`python-docx` `.docx` 生成、PyMuPDF 文本 `.pdf` 生成，并把文件写入 `workspace_artifacts/generated/output/writing_exports/{project_id}/` 后返回 base64 下载载荷；`resources_router/endpoints_export_stats.py:39-99` 让 `/resources/project/{id}/export` 支持五格式；`writing_router.py:991-1009` 让 canonical `POST /api/writing/export` 委托同一 exporter，不再对 Word/LaTeX/PDF 返回 501；`frontend/src/services/writingBackend.ts:60-146,667-683` 增加五格式 union、base64→Blob 下载 helper 与 canonical POST；`ExportPreviewModal.tsx:21-154` 和 `ReferenceDrawer.tsx:101-290` 增加“导出格式”选择器，可获取/预览/下载 Markdown、JSON、Word、LaTeX、PDF，二进制格式显示生成文件提示；OpenAPI `frontend/openapi/modular-pipeline-openapi.json` 与 `frontend/src/generated/openapi.ts` 已同步。验证：`pytest tests/test_writing_submission_export.py tests/test_resources_export_contract.py -q -o addopts=""` 11/11 passed，覆盖 Word/LaTeX/PDF 非 501、LaTeX 特殊字符转义、DOCX `PK` 头、PDF `%PDF` 头和本地文件路径存在；`compileall` passed；`npm run generate:openapi` passed；`npm run test -- src/components/writing/ReferenceDrawer.test.tsx --run` 2/2 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；浏览器 smoke `H10_FRONTEND_PORT=5316 node workspace_artifacts/runtime_state/h10-writing-export-ui-smoke.mjs` passed，断言默认 markdown 请求体含 `include_evidence/include_citations`，并切换 `word/latex/pdf` 三种格式各触发 `/api/writing/export` 请求，UI 显示下载 WORD/LATEX/PDF；截图 `workspace_artifacts/screenshots/h10-writing-export-docx-latex-pdf-20260528.png`。成熟方案核对：python-docx 官方 `Document.add_paragraph/add_table/save` 模式、PyMuPDF 官方 `new_page/insert_text/save` 生成 PDF 模式、Overleaf/LaTeX 特殊字符转义规则、MDN Blob/Object URL 下载模式。Rollback：checkpoints `20260528-054827-h10-writing-export-docx-latex-pdf-contract`、`20260528-055324-h10-writing-export-docx-latex-pdf-implementation`，矩阵实体快照 `.rollback_snapshots/matrix-h10-docx-latex-pdf-20260528_061832`。残余风险：当前 Word/PDF 是基于现有草稿/证据/引用链的本地学术导出文件，不是期刊模板排版引擎；PDF 为文本 PDF，未做复杂分页、图表浮动、公式排版或 Word 字段式交叉引用；参考文献仍由当前 materials 列表渲染，不自动生成 Word bibliography 目录。 |

---

## I. 设置与凭证 (Settings / Credentials / Sampling)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| I1 | Settings 统一 API 配置 | 单一入口配置所有 API | Settings 页显示 API 配置区 | `/api/settings` CRUD | settings-unified-api-config | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：新增 `routers/settings_router.py` 和 `/api/settings` GET/PUT/DELETE 聚合契约，统一返回 chat/embedding/rerank masked config、Credentials 计数和 feature flags；PUT 只写既有 runtime stores，DELETE 只清 API overrides，不删除凭证，避免绕过敏感字段边界。`python_adapter_server.py` 已注册 settings router；OpenAPI 已重生成 `frontend/openapi/modular-pipeline-openapi.json` 与 `frontend/src/generated/openapi.ts`。前端新增 `services/settingsApi.ts`，`Settings.tsx` 增加首屏“API 配置”单一入口，展示生成/向量化/rerank 状态并跳转到原专用表单，保留既有 Chat、语义路由、Credentials、功能开关分区。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_settings_router.py tests\test_credentials_router.py -q -o addopts=""` 26/26 passed；`npm run test -- src/services/settingsApi.test.ts --run` passed；`npm run build` passed（仅既有 `annotationApi.ts` dynamic/static import warning）；browser smoke `node workspace_artifacts/runtime_state/i1-settings-unified-api-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/i1-settings-unified-api-20260528.png`；`compileall` passed；`git diff --check` passed。成熟方案核对：FastAPI `APIRouter`/`response_model` 聚合契约，Settings 单入口 + 分组编辑 UX。Rollback：`.rollback_snapshots/matrix-i1-unified-settings-20260528_172033`。 |
| I2 | Settings 清理 | 移除废弃配置项 | Settings 页无废弃项 | 后端移除废弃字段 | settings-cleanup | 是/P1 应做 | Codex(UI) | ✅ 2026-05-27 Codex：旧 Settings 深链不再回到废弃独立页，`embedding`/`rerank` 规范化到“语义路由”，`sampling` 规范化到“聊天与生成”并显示内嵌任务采样；定位 `frontend/src/pages/settingsSections.ts:48-52` legacy section normalize，`:70-77` `resolveInitialSection()`，`frontend/src/pages/Settings.tsx:733` Chat 内嵌 `SectionSampling`、`:1207` `section-semantic-routing`、`:1422` `section-chat-sampling`、`:2547` URL 参数归一化、`:2596-2599` legacy map 到当前 surface，`frontend/src/pages/settingsSections.test.ts:42-101` 覆盖 embedding/rerank/sampling 深链。验证：`node ../workspace_artifacts/runtime_state/i2-settings-cleanup-smoke.mjs` 打开 `?section=embedding`/`rerank` 断言进入“语义路由”，打开 `?section=sampling` 断言进入“聊天与生成”且只显示“任务采样参数”而非 standalone“采样策略”，截图 `workspace_artifacts/screenshots/i2-settings-cleanup-20260527.png`；`npm run test -- src/pages/settingsSections.test.ts --run` 15/15 passed；`npm run build` passed。成熟方案核对：MDN `URLSearchParams.get()`/React Router search-param pattern，保留 URL 兼容入口但归一化到当前 UI surface。Rollback：checkpoint `20260527-214204-i2-settings-cleanup-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-i2-20260527_214552`。残余风险：后端仍存在 embedding/rerank runtime config API，这是当前语义路由的必要配置面；本 slice 只证明 Settings UI 不再暴露废弃独立分区入口。 |
| I3 | Credentials 统一 | 凭证管理统一入口 | Settings → API 凭证 | `/api/credentials` CRUD | credentials-unification | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`Settings.tsx:2606-2608` 通过懒加载挂载 `CredentialsSectionLazy`，`CredentialsSection.tsx:153-229` 统一拉取 `/api/credentials` 列表并在空态时提示新增，`:231-352` 支持创建/编辑/删除/测试连接，`:524-578` 列表项展示 provider/model/category/strategy/masked key 且进入详情页，`:568-578` 在详情区显示当前凭证状态 badge；`credentialsApi.ts:153-250` 封装 canonical list/create/update/delete/test/apply API。验证：`npm run test -- src/services/credentialsApi.test.ts --run` 13/13 passed；`npm run build` passed；浏览器 smoke `I3_I4_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/i3-i4-credentials-manager-smoke.mjs` passed，断言 `/settings?section=credentials` 渲染 API 凭证列表、详情页、masked key、测试连接按钮，并且未渲染 raw `localStorage` API key；截图 `workspace_artifacts/screenshots/i3-i4-credentials-manager-20260528.png`。成熟方案核对：Playwright 官方 `page.route`/locator/screenshot smoke 模式；凭证 UI 按 OWASP secrets 最小暴露原则只显示 masked key，不回显明文。Rollback：checkpoint `20260528-083725-i3-i4-credentials-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-i3-i4-20260528_084338`。残余风险：测试连接依赖后端 `/api/credentials/{id}/test` 的真实判定与上游连通性，本次只验 UI 契约和请求形状。 |
| I4 | Credentials 列表/详情 | 列表进入详情子界面 | Credentials 列表 → 详情页 | 前端组件内状态 | settings-smartread-ui-followup credentials slice | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`CredentialsSection.tsx:226-229` 点击列表项进入详情，`:396-415` 选中项和状态 badge 由 `editingId` 驱动，`:421-594` 详情页展示 provider/model/base_url/档位/API Key/信任来源，并在 `mode === 'edit'` 时保留空密钥输入以避免回显；`credentialsApi.ts:160-250` 复用统一 `/api/credentials` CRUD/test。验证：同 I3 的 focused test/build/browser smoke；浏览器断言详情页出现 `返回 API 列表`、`OpenAI / gpt-4.1-mini` 标题、空 API Key 字段和“已信任”状态 badge，返回列表后触发 `POST /api/credentials/cred-i3-openai-fast/test`。成熟方案核对：受控详情面板 + 列表/详情切换模式；敏感值不回显，只显示 masked key。Rollback：checkpoint `20260528-083725-i3-i4-credentials-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-i3-i4-20260528_084338`. 残余风险：对“测试连接”的成功/失败提示仍依赖后端和上游 API 状态，本次只闭合前端交互与安全展示。 |
| I5 | Credentials 采样 B8 | 采样端点 `/api/credentials/sample` | 无前端 UI (后端 API) | `/api/credentials/sample` 200 OK | 0182-release B8 | 是/P0 必做 | Claude | ✅ 已完成 (2026-05-26 commit b5b0ca3e + cc0da047)：与 D2 同一实现。`POST /api/credentials/sample?category={generation|embedding|rerank}&strategy_hint={tier}` 按 category + strategy_hint 选择凭证；精确匹配优先（双向 normalize 确保 legacy stored credential 能被 canonical query 命中），回退到最高 priority；返回 `RuntimeCredentialPublic` (masked api_key, canonical strategy_hint)；11 测试覆盖精确匹配/legacy 映射/priority 回退/category 过滤/无可用凭证 404/默认值/掩码/legacy 兼容 (`tests/test_credentials_sampling.py`)；代码：`literature_assistant/core/routers/credentials_router.py:208-261` sample_credential endpoint；验证：`pytest tests/test_credentials_sampling.py -v` 11/11 passed |
| I6 | Credentials 档位配置 | 默认成本档位配置 | Credentials 详情显示档位选择器 | `default_cost_tier` 字段 | settings-smartread-ui-followup tier slice | 是/P0 必做 | Claude→Codex | ✅ 已完成 (2026-05-27)：现行契约采用后端 `strategy_hint` 作为 default cost tier，不再新增重复 `default_cost_tier`；后端 B5 已统一 low/medium/high/xhigh/max 并兼容 legacy；前端 `credentialsApi.ts:19` 补齐 canonical type，`CredentialsSection.tsx:51` 表单持有 `strategy_hint`，`:101` 五档选项，`:234` 编辑时 legacy→canonical，`:261` 创建提交档位，`:273` 更新提交档位，`:786` 详情“调用档位”选择器，`:1008` 列表显示档位；验证：`npm run test -- src/services/credentialsApi.test.ts --run` 13/13 passed，`npm run build` passed，浏览器验收 `/settings` → API 凭证 → 新增：桌面/390px 移动快照均显示“调用档位”且不重叠；截图：`workspace_artifacts/screenshots/i6-credentials-tier-mobile.png`；回档：`.rollback_snapshots/i6-credentials-tier-ui-20260527_074148` |
| I7 | Credentials 模型路由偏好 | Claude Max / Codex XHigh / Codex Fast | Credentials 详情显示路由说明 | `CredentialStrategyHint` enum | settings-smartread-ui-followup tier slice | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `models/credentials.py:39-68` 保留 canonical `low/medium/high/xhigh/max`、legacy `default/cheap/fast/quality` 和 surface hint `discussion/embedding/rerank`；前端 `credentialsApi.ts:19-31` 同步类型，`CredentialsSection.tsx:101-107` 在调用档位下拉中显示“低/中/高/XHigh/Max”及“Codex 路由优先”“Claude Max 路由优先”，`:137-141` 将 legacy `fast` 映射为“中”、`quality` 映射为“高”，`:786-803` 在详情表单下方解释低/中/高控制成本与上下文预算、XHigh/Max 用于高预算模型路由。验证：`npm run test -- src/components/settings/CredentialsSection.test.tsx src/services/credentialsApi.test.ts --run` 15/15 passed；`npm run build` passed；浏览器 smoke `I7_I8_FRONTEND_PORT=5324 node workspace_artifacts/runtime_state/i7-i8-settings-routing-smoke.mjs` passed，断言 Settings → API 凭证列表显示 legacy 档位映射、详情新增页显示 XHigh/Codex 与 Max/Claude Max 路由说明；截图 `workspace_artifacts/screenshots/i7-i8-settings-routing-20260528.png`。成熟方案核对：HTML 原生 `select/option` + label/value 表单模式；凭证信息仍只显示 masked key，符合 secrets 最小暴露原则。Rollback：checkpoint `20260528-085156-i7-i8-settings-routing-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-i7-i8-20260528_090346`。残余风险：路由偏好只是选择/采样 hint，不强制验证真实 Claude/Codex 账号或模型可用性；真实调用仍依赖后端凭证采样与 provider 连通性。 |
| I8 | Settings 语义路由说明 | 同时覆盖 embedding + rerank | Settings → 语义路由 说明文案 | 无后端变化 | settings-smartread-ui-followup copy slice | 是/P0 必做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`Settings.tsx:1202-1260` 的 `SectionSemanticRouting` 明确“语义路由 = Embedding 召回 + Rerank 精排”，说明 Embedding 决定候选池、Rerank 决定候选证据排序，并保留“本地 embedding 与 rerank 怎么接？”details，分别指向 OpenAI 兼容 `/v1` embedding 服务和 Cohere 兼容 `/rerank` rerank 端点；同页继续挂载 `EmbeddingCard` 与 `RerankCard`。验证：同 I7 focused tests/build/browser smoke；浏览器断言 `/settings?section=semantic-routing` 可见 embedding/rerank 边界说明、Top-K 配置、details 展开后的本地服务接法，截图 `workspace_artifacts/screenshots/i7-i8-settings-routing-20260528.png`。成熟方案核对：使用原生 `<details>/<summary>` 渐进披露长说明，避免把实现细节放到主界面；Playwright `page.route`/locator/screenshot smoke 模式。Rollback：checkpoint `20260528-085156-i7-i8-settings-routing-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-i7-i8-20260528_090346`。残余风险：语义路由说明不验证真实 embedding/rerank 质量；端点连通性仍由各自配置卡的测试按钮和后端 router 负责。 |
| I9 | Settings 功能开关说明 | 不暴露实现路径 | Settings → 功能开关 用户友好文案 | 无后端变化 | settings-smartread-ui-followup copy slice | 是/P1 应做 | Codex(UI) | ✅ 2026-05-27 Codex：功能开关列表改为用户语义标签/说明，隐藏 raw flag name/env var/实现缩写；定位 `frontend/src/pages/Settings.tsx:2362` `FEATURE_FLAG_DISPLAY_COPY`、`:2433` `sourceLabel`、`:2464` 渲染映射标签，`frontend/src/locales/zh.json:637` 将 env 来源文案改为“系统设定”。验证：`node ../workspace_artifacts/runtime_state/i9-settings-feature-flags-copy-smoke.mjs` 断言 `/settings?section=experimental` 中无 `tolf_context`/`analysis_chain`/env var/`TOLF`/`RAG`/`LLM`/`Inspector`/“环境变量”泄露并截图 `workspace_artifacts/screenshots/i9-settings-feature-flags-copy-20260527.png`；`npm run build` 通过。成熟方案核对：Microsoft/GOV.UK plain-language UI copy 原则，使用用户可理解对象名，避免实现术语。Rollback：checkpoint `20260527-212159-i9-settings-feature-flags-copy-smoke`；矩阵快照 `.rollback_snapshots/matrix-i9-20260527_212627`。残余风险：新增后端 flag 若未加入映射仍会回退显示后端 label，需后续配置治理。 |
| I10 | Settings 输出路径 | 默认安装目录内输出 | Settings → 输出路径 显示默认值 | `project_paths.py` 默认路径 | settings-smartread-ui-followup copy slice | 是/P1 应做 | Codex(UI) | ✅ 2026-05-27 Codex：Settings 工作区分区展示默认输出目录，且后端路径锚点统一到应用数据根下的 generated/output；定位 `frontend/src/services/settingsStore.ts:75` 默认 `./workspace_artifacts/generated/output`，`frontend/src/pages/Settings.tsx:958` `section-workspace`、`:967` 输入框值、`:973` 用户可见说明，`literature_assistant/core/project_paths.py:20-52` 安装态/开发态 user data root 与 `WORKSPACE_GENERATED_ROOT`，`:71-73` `WORKSPACE_OUTPUT_ROOT`。验证：`node ../workspace_artifacts/runtime_state/i10-settings-output-path-smoke.mjs` 打开 `/settings?section=workspace`，断言“输出与工作区”、输入框默认值、`workspace_artifacts/generated/output` 可见，且无旧 root `./output` 文案，截图 `workspace_artifacts/screenshots/i10-settings-output-path-20260527.png`；`npm run build` 通过。成熟方案核对：Windows app data 与 XDG-style app data/state 约定均支持使用应用专用数据根集中存放运行输出。Rollback：checkpoint `20260527-213213-i10-settings-output-path-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-i10-20260527_213352`。残余风险：Settings 仍是本地 localStorage 配置面，不是统一 `/api/settings` CRUD；真实安装路径显示为相对说明，未从后端实时读取绝对 resolved path。 |

---

## J. 扩展生态 (MCP / Skill 安装与运行)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| J1 | MCP v0.3 支持 | 支持 MCP v0.3 协议 | 无前端 UI | `mcp_client.py` v0.3 | mcp-v0.3-support | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成：mcp SDK 1.27.0 (protocol 2025-11-25) > v0.3；ClientSession/StdioServerParameters/stdio_client 已集成 |
| J2 | MCP v0.4 支持 | 支持 MCP v0.4 协议 | 无前端 UI | `mcp_client.py` v0.4 | mcp-v0.4-support | 是/P0 必做 | 维护 | ✅ 已完成并接口/安全测试验收 (2026-05-28)：`models/mcp.py:31-35` 定义 `McpTransport.STREAMABLE_HTTP`，`models/mcp.py:92-119` 定义 `McpStreamableHttpConfig`，`models/mcp.py:222-244` 强制 `streamable_http` 必须提供 `http` 且不得混用 `stdio`；`mcp_runtime/client_manager.py:106-152` 保留 `LITERATURE_ENABLE_MCP_STREAMABLE_HTTP=1` 执行闸门，`:204-246` 使用 MCP SDK `streamablehttp_client`、header refs 解析与 safe httpx factory；`mcp_runtime/security_policy.py:326-421` 对 `streamable_http` URL 做 scheme/host/private range/DNS rebinding guard；`routers/mcp_router.py:497-509` 暴露 pending tool approval 决策入口。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_mcp_server_store.py tests\test_mcp_fingerprint_v2.py tests\test_credential_binding_index.py tests\test_mcp_phase5_hardening.py tests\test_mcp_safe_transport.py tests\test_mcp_pending_calls.py tests\test_mcp_pending_call_http_integration.py tests\fixtures\mcp\test_phase0_smoke.py -q` 107/107 passed，覆盖 streamable_http 配置存储/指纹、凭证绑定、私网/回环/DNS 防护、safe transport、pending call 审批和 MCP SDK import smoke。成熟方案核对：沿用官方 MCP SDK `streamablehttp_client`/`ClientSession` per-operation lifecycle、FastAPI TestClient/pytest characterization、本仓 safe transport 与 pending approval 模式；本 slice 只做本地协议兼容验证，无需额外外部网络调用。Rollback：checkpoint `20260528-101635-j2-mcp-v04-streamable-http-verification`，矩阵实体快照 `.rollback_snapshots/matrix-j2-20260528_101833.md`。残余风险：未连接真实外部 streamable_http MCP server 做协议互操作；执行仍被 feature flag 关闭，真实 server 验收需要用户配置端点和授权网络/凭证。 |
| J3 | MCP Vision 辅助 | Vision 模型辅助 MCP 工具 | 无前端 UI | `mcp_vision_aux.py` | mcp-vision-aux | 可延后/P2 | Claude(后端/接口) | ✅ 本地 Vision auxiliary 包与 SmartRead 契约已验证 (2026-06-05)：当前实现位于 `extension_packages/mcp/lit_mcp_vision_auxiliary/`，不是旧占位名 `mcp_vision_aux.py`；`literature-mcp.json` 暴露本地 stdio 启动 `python -m lit_mcp_vision_auxiliary.server`、config fields、credential slot 和 expected tools `analyze_images_batch`、`vision.describe_image`、`vision.extract_text`；`server.py:41-45` 定义批量/兼容工具名，`:302-416` 分别构造 OpenAI-compatible、Anthropic、Gemini vision 请求，`:418-434` 做 provider 分派，`:449-508` 让 SmartRead 批量工具返回可解析 JSON 错误/notes，`:517-603` 的 `list_tools()` 暴露 MCP tool schemas。host 集成证据：`tests/test_intelligent_chat_router.py` 覆盖 enabled `vision-auxiliary` 时图片 notes 注入、错误降级、chat path 支持图片时跳过；`tests/test_mcp_has_enabled_server.py` 覆盖只有 `enabled_for_session` 才视为启用。验证：`pytest` focused Vision package tests 7/7 passed，覆盖 missing image 文案、manifest expected tool、OpenAI-compatible/Gemini/Anthropic mock shape、配置错误 JSON、provider HTTP error redaction；`pytest tests\test_mcp_has_enabled_server.py tests\test_intelligent_chat_router.py -q -o addopts=""` 47 passed；`pytest tests\test_mcp_package_scanner.py tests\test_mcp_installer_router.py tests\test_skill_required_credentials.py -q -o addopts=""` 104 passed, 1 skipped；`python -m json.tool extension_packages\mcp\lit_mcp_vision_auxiliary\literature-mcp.json` passed；`compileall` for package `server.py/__init__.py` passed；`git diff --check` passed with only the existing matrix CRLF warning。成熟方案核对：MCP tools/list + tools/call schema、MCP security best practices、local S8 MCP/Skill extension runtime study。Rollback：checkpoint `20260605-124547-j3-vision-auxiliary-local-verification-preflight`，实体快照 `.rollback_snapshots/j3-vision-auxiliary-preedit-20260605_124547`。残余风险：本次没有发布、上传、远程安装、probe 真实 provider、写真实凭据或部署；真实视觉调用仍依赖用户在凭证中心绑定可用 provider。 |
| J4 | MCP Tool-use UX | 工具使用体验优化 | 前端显示工具调用状态 | 后端返回工具调用详情 | mcp-tool-use-ux | 是/P1 应做 | Claude→Codex | ✅ 已实现并浏览器验收 (2026-05-27)：后端 `GET /api/mcp/pending-calls` + `POST /api/mcp/pending-calls/{id}/decide` + audit；前端 `App.tsx:11/94` 全局挂载 `McpPendingCallPoller`，`McpPendingCallPoller.tsx:67-134` 轮询 pending call 并提交 approve/reject，`McpToolApprovalModal.tsx:43-129` 展示 server/tool/capability/risk、args preview、会话内记住选择，`mcpApi.ts:247-267` 封装 pending/decide API。验证：`npm run test -- src/services/mcpApi.test.ts src/components/mcp/McpPendingCallPoller.test.tsx src/components/mcp/McpToolApprovalModal.test.tsx --run` 28/28 passed；`npm run build` passed；浏览器 smoke `node ../workspace_artifacts/runtime_state/j4-mcp-tool-approval-smoke.mjs` 通过，mock pending `write_note` 弹窗展示 `write` capability 与 args preview，勾选“本次会话内记住”后断言 POST `/api/mcp/pending-calls/tool-call-j4-write-note/decide` body 为 `{decision:'approve',remember_for_run:true}`，截图 `workspace_artifacts/screenshots/j4-mcp-tool-approval-20260527.png`。成熟方案核对：本 slice 为已实现 pending-call UI 的本地浏览器验收，无需额外外部方案；沿用既有 MCP pending approval ADR/组件契约。Rollback：checkpoint `20260527-213714-j4-mcp-tool-approval-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-j4-20260527_213838`。残余风险：真实工具执行结果/audit 依赖后端 MCP server 与工具运行时，本次只验证审批 UI 与 decision 请求。 |
| J5 | MCP 本地安装器 A | 扫描本地 MCP 包 | 无前端 UI | `mcp_scanner.py` | mcp-local-installer A | 是/P1 应做 | 维护 | ✅ 已完成并接口验收 (2026-05-28)：现行 scanner 为 `mcp_runtime/package_scanner.py:100-904` 的 `McpPackageScanner`，优先读取 `literature-mcp.json`/`lit-mcp.json`，fallback 到 `mcp.json`、`server.json`、`package.json`、`pyproject.toml`，解析 launch candidates、config fields、required credentials、expected tools 与 warning；`mcp_installer_router.py:152-169` 提供 `POST /api/mcp/installations/scan` 并把 scan result 注册到 `McpScanRegistry`；`models/mcp_installation.py:221-260` 定义 scan request/response schema。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_mcp_package_scanner.py tests\test_mcp_installer_router.py -q` 66/66 passed，覆盖远程 URL/git URL 拒绝、manifest 优先级、shell metachar/坏 args/未知 transport 拦截、config/credential 字段解析、scan_id 稳定性、router scan 注册和 remote URL 400 `scan_rejected`；`.\.venv-1\Scripts\python.exe -m compileall -q literature_assistant\core\mcp_runtime literature_assistant\core\routers\mcp_installer_router.py literature_assistant\core\models\mcp_installation.py` passed。成熟方案核对：本 slice 为本地接口/扫描器验收，沿用项目 scanner allowlist + argv 数组启动候选 + TestClient router tests；不执行包代码、不访问网络。Rollback：checkpoint `20260528-101148-j5-mcp-local-installer-scan-api-verification`，矩阵实体快照 `.rollback_snapshots/matrix-j5-20260528_101223.md`。残余风险：真实第三方包兼容性取决于 manifest 质量；scan 阶段只读声明文件，真实进程探测由 J7/J6 信任确认链路处理。 |
| J6 | MCP 本地安装器 B | 前端配置 UI | Settings → MCP 安装 | 前端 MCP 配置表单 | mcp-local-installer B | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`McpServersSection.tsx:59-121` 在 Settings → MCP 服务器下提供“本地安装”tab，`McpLocalInstallView.tsx:8-60` 提供本地路径入口和安全提示，`McpInstallWizard.tsx:65-172` 串联 source/candidate/config/credentials/review/done 步骤，`:247-253` 调 `scanLocalMcpPackage()`，`:472-480` 用 `CredentialPicker` 绑定 required credentials，`:661-670` 安装提交 `config_values/credential_bindings/trust_to_probe`；`CredentialPicker.tsx:46-179` 只列 enabled RuntimeCredentials 并按 provider hint 分组；`mcpInstallApi.ts:192-224` 封装 scan/install API。验证：browser smoke `J6_FRONTEND_PORT=5327 node ..\workspace_artifacts\runtime_state\j6-mcp-local-installer-config-smoke.mjs` passed，mock scan 返回 config field + `QWEN_API_KEY` credential requirement，断言用户完成本地路径扫描、配置、DashScope 凭证绑定、信任确认和安装完成；拦截 install body 确认只提交 `{QWEN_API_KEY:'cred-j6-dashscope'}` credential_id 引用，不泄漏 masked/raw key；截图 `workspace_artifacts/screenshots/j6-mcp-local-installer-config-20260528.png`；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）。成熟方案核对：本 slice 为现有 MCP installer UI 的本地 browser mock 验收，沿用 Playwright `page.route/route.fulfill` 与显式信任确认模式，无真实包执行/真实凭证编辑。Rollback：checkpoint `20260528-100001-j6-mcp-local-installer-config-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-j6-20260528_100930.md`。残余风险：真实扫描、路径 normalize、包进程探测和安装目录写入由后端 installer 负责；本次只验收前端配置 UI 和请求契约。 |
| J7 | MCP 本地安装器 C | 安装/探测/信任确认 | Settings → MCP 显示信任确认弹窗 | `/api/mcp/install` | mcp-local-installer C | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成：实际端点 `POST /api/mcp/installations/install` (scan/preview/install 三阶段)；McpTemplateInstaller stdio 安装 + 信任确认 |
| J8 | MCP Per-agent scope | 每个 Agent 独立 MCP 配置 | Discussion 面板支持角色级 MCP 覆盖 | `mcp_config` 按 agent_id 隔离 | mcp-per-agent-scope | 是/P1 应做 | Claude→Codex | ✅ 已完成并复核 (2026-05-27)：后端 `McpScopeType` (surface/agent) + `DiscussionMcpOverrides.per_agent/per_role` + discussion agent_id 隔离逻辑；前端 `discussionApi.ts:93-97` 暴露 `scope_type/per_agent/per_role` 类型，`DiscussionPanel.tsx:124-144` 清洗 per-agent scope，`:544-548` 发送 `scope_type: agent` 和 `per_agent`，`:932-964` 在全局 MCP 下方渲染“按角色隔离 MCP”覆盖区。验证：`pytest tests/test_discussion_mcp_per_agent_scope.py -q` passed（含在 151/151 后端队列复核内）；`npm run test -- src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 1/1 passed；`npm run build` passed；浏览器 smoke `node ../workspace_artifacts/runtime_state/j8-discussion-per-agent-mcp-smoke.mjs` 通过，mock 两个 enabled MCP server，选择全局 `mcp-global-files` 与角色 `proposer_1` 覆盖 `mcp-agent-notes`，拦截请求体断言 `mcp_overrides={scope_type:'agent',server_ids:['mcp-global-files'],per_agent:{proposer_1:['mcp-agent-notes']}}` 且没有 `per_role`，截图 `workspace_artifacts/screenshots/j8-discussion-per-agent-mcp-20260527.png`。成熟方案核对：本 slice 为已实现 UI 的本地浏览器验收，无需额外外部方案；MCP 权限隔离沿用既有 run-level/agent-level scope 设计。Rollback：checkpoint `20260527-213420-j8-discussion-per-agent-mcp-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-j8-20260527_213631`。残余风险：真实 MCP tool execution/audit 依赖后端 MCP server 可用性，本次只验证前端隔离配置和提交契约。 |
| J9 | Skill 安全评估 | Skill 安装前安全检查 | Settings → Skill 显示安全评分 | `skill_security_assessor.py` | skill-security-assessment | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`skillApi.ts:176-179` 调用 `GET /skills/{skill_id}/security`；`SkillManager.tsx:268-285` 通过“查看安全策略”按钮加载 assessment，`:823-824` 提供可访问按钮名，`:913-951` 渲染风险等级、运行门控、是否可执行、启用是否需审批、拦截操作、沙箱控制、block/approval reason。验证：浏览器 smoke `J9_J12_FRONTEND_PORT=5325 node ..\workspace_artifacts\runtime_state\j9-j12-skill-security-approval-smoke.mjs` passed，mock 高风险 imported skill 后断言未受信任/脚本拦截/高风险标记、安全策略面板、`风险：high`、`blocked_until_approval`、`script.execute, network`、沙箱控制和 block reason 可见；截图 `workspace_artifacts/screenshots/j9-skill-security-assessment-20260528.png`；`npm run test -- src/services/skillApi.test.ts --run` 5/5 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）。成熟方案核对：本 slice 为已实现 Skill 管理 UI 的本地 browser mock 验收，复用 Playwright `page.route`/screenshot 模式，无需额外外部方案。Rollback：checkpoint `20260528-093726-j9-j12-skill-security-approval-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-j9-j12-20260528_094349.md`。残余风险：真实安全评分规则和沙箱执行拦截仍由后端 assessor/skill runtime 负责，本次只验收前端显示与 API 调用契约。 |
| J10 | Skill 沙箱 | Skill 运行沙箱隔离 | 无前端 UI | `skill_sandbox.py` | skill-sandbox | 是/P1 应做 | Claude(后端/接口) | ✅ 已完成：skill_executor.py `_execute_scripted` subprocess 隔离 + timeout + SKILL_SANDBOX=1 env + 输出截断；safe_to_execute 安全策略阻断 |
| J11 | Skill 导入/导出 | Skill 包导入导出 | Settings → Skill 导入/导出按钮 | `/api/skill/import` `/api/skill/export` | skill-import-export | 是/P1 应做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-27)：`POST /skills/import` 已存在；`POST /skills/{skill_id}/export` 端点；`WritingSkillService.export_user_skill` 打包 skill 目录为 zip，默认输出 `workspace_artifacts/skill_exports/{skill_id}.zip`；拒绝导出 builtin skill；前端 `Settings.tsx:61`/`:2601` 懒加载 SkillManager，`skillApi.ts:182` `importSkill()`、`:272` `exportSkill()`，`SkillManager.tsx:370-383` 用户 skill 导出并展示导出路径/安全边界，`:838` 仅非 builtin 渲染导出按钮，`zh.json:566-570` 导出文案。验证：后端 `pytest tests/test_skill_export.py -q` passed（含在 151/151 后端队列复核内）；`npm run test -- src/services/skillApi.test.ts --run` 5/5 passed；`npm run build` passed；浏览器 smoke `node ../workspace_artifacts/runtime_state/j11-skill-import-export-smoke.mjs` 通过，mock Settings → Skill 管理页验证导入表单、用户 skill 导出按钮、builtin skill 无导出按钮，点击导出后断言 `POST /skills/skill.acceptance-export/export` 且展示 `workspace_artifacts/skill_exports/skill.acceptance-export.zip` 与“不暴露 API Key 或运行时凭证”，截图 `workspace_artifacts/screenshots/j11-skill-import-export-20260527.png`。成熟方案核对：本 slice 为已实现 UI 的本地浏览器验收，无需额外外部方案；沿用既有 Skill 沙箱/导出后端契约。Rollback：checkpoint `20260527-213911-j11-skill-import-export-browser-smoke`；矩阵快照 `.rollback_snapshots/matrix-j11-20260527_214110`。残余风险：真实导入/导出文件系统结果仍依赖用户本地 skill 包路径与后端权限，本次不读写真实包。 |
| J12 | Skill 审批流程 | Skill 安装需审批 | Settings → Skill 显示审批按钮 | `skill_approval_workflow.py` | skill-approval | 是/P1 应做 | 维护 | ✅ 已完成并浏览器验收 (2026-05-28)：`skillApi.ts:232-246` 封装 `GET /skills/approvals/pending` 与 `POST /skills/approvals/{request_id}/decide`；`SkillManager.tsx:288-300` 记录批准/拒绝/稍后结果，`:566-570` 渲染审批 tab，`:978-1054` 显示待审批 capability、reason、时间和三种决策按钮。验证：同一 browser smoke `workspace_artifacts/runtime_state/j9-j12-skill-security-approval-smoke.mjs:147-213` mock pending approval，点击“审批”tab 后断言待审批 Skill 与 reason 可见，点击“批准”后断言请求体 `decision=approved` 且页面显示“审批已记录：批准”；截图 `workspace_artifacts/screenshots/j12-skill-approval-flow-20260528.png`；`npm run test -- src/services/skillApi.test.ts --run` 5/5 passed；`npm run build` passed。成熟方案核对：本 slice 为现有审批 UI 的本地 browser mock 验收，复用 Playwright route/locator/screenshot 模式。Rollback：checkpoint `20260528-093726-j9-j12-skill-security-approval-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-j9-j12-20260528_094349.md`。残余风险：审批请求产生时机、真实用户身份和后端持久审计由后端 workflow 负责，本次只验收 Settings UI 的查看与决策提交链路。 |

---

## K. 发布与运维 (Release Gate / Soak / Runbooks)

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| K1 | Release Gate | 发布前检查清单 | 无前端 UI | `release_gate.py` 通过 | release-gate | 暂缓/P3 | 暂缓 | ⚠️ 本地 release-readiness 审计已完成并修复安全项 (2026-06-05)，但公共发布仍阻塞/approval-only：修复 `packaging/inno-setup/literature-assistant.iss` 与 `scripts/build_windows_exe.ps1` 的 installer 文件名契约，从旧 `Scholar-AI-Setup-{version}-windows-x64.exe` 统一为 `LiteratureAssistant-Setup-{version}-windows-x64.exe`；同步修正当前契约下的 frontend readiness 测试。验证：`run_literature_assistant.py paths` passed；`compileall -q literature_assistant run_literature_assistant.py sitecustomize.py tests\conftest.py workspace_tests\evaluation_scripts` passed；`pytest tests --collect-only -q -o addopts=""` 收集 3465 tests；`npm run build` passed；`npm run test -- --run` 114 files / 704 tests passed；`release_secret_scan.py --input packaging --skip-detect-secrets` OK；`release_secret_scan.py --input scripts --skip-detect-secrets` OK；`dump_pyinstaller_analysis.py` 生成 631 datas / 0 binaries / 144 hiddenimports manifest；`release_forbidden_path_scan.py --mode manifest` OK。追加 release-prep 修复：`.gitignore` 对 `/tests/` 改为路径显式例外，只放行 8 个 tracked public-candidate tests，避免 broad untracked test-tree 噪声；8 个测试 focused secret scan 全部 OK；`git ls-files -ci --exclude-standard` 现在只剩 `docs/plans/2026-05-26-functional-acceptance-matrix.md`。Rollback：`20260605-133638-k1-local-release-readiness-resume-preflight`、`20260605-134334-k1-local-readiness-safe-fixes-preedit`、`20260605-135254-k1-release-readiness-docs-final-preedit`、`20260605-224355-k1-public-boundary-ignore-safe-fix-preedit`，实体快照 `.rollback_snapshots/k1-release-readiness-docs-final-preedit-20260605_135318`、`.rollback_snapshots/k1-public-boundary-ignore-safe-fix-preedit-20260605_224355`。残余 blocker：`docs/plans/2026-05-26-functional-acceptance-matrix.md` 是 tracked ignored internal plan/evidence file；解决它需要显式 source-boundary/index 决策，本轮未 stage/commit/remove/waive。未执行 PyInstaller/Inno build、上传、tag、release、push、publish 或 deployment。 |
| K2 | Public Push 边界检查 | 推送前检查敏感文件 | 无前端 UI | `git diff --cached --check` + forbidden-path | git-branch-hygiene (memory) | 暂缓/P3 | 维护 | ✅ 已实现（静态定位）：工作区规则 + `release_forbidden_path_scan.py`/secret scan 脚本 |
| K3 | Bundle Split | 前端 bundle 拆分优化 | 前端 bundle 体积 < 阈值 | `npm run build` 输出分析 | bundle-split | 可延后/P2 | Codex(UI/build) | ✅ 已完成当前触发项 (2026-06-05)：C12 后 `npm run build` 曾显示 `PdfReaderShell` chunk 500.66 kB 并触发 Vite chunk-size warning；`vite.config.ts:50-64` 新增 typed `normalizeModuleId()`/`manualChunks()`，将 `react-pdf` 与 `pdfjs-dist` 拆到稳定 `pdf-viewer-vendor` chunk；`:86-92` 通过 `build.rollupOptions.output.manualChunks` 接入 Rollup，不提高 `chunkSizeWarningLimit`。验证：`npm run build` passed 且无 chunk-size warning，输出 `PdfReaderShell-B2ghmJn3.js` 38.00 kB、`pdf-viewer-vendor-DlBIBcPn.js` 471.07 kB、`GraphPayloadViewer` 387.70 kB、入口 chunks 449.68/462.12 kB，均低于 500 kB 阈值；`git diff --check -- frontend\vite.config.ts docs\plans\local-feature-execution-plan-2026-06-05.md docs\plans\2026-05-26-functional-acceptance-matrix.md frontend\src\components\PdfViewer\PdfViewer.tsx` passed（仅既有 CRLF warning）。成熟方案核对：Vite `build.rollupOptions`、Rollup `output.manualChunks` function form。Rollback：checkpoint `20260605-012553-k3-pdf-reader-bundle-split-preflight`，实体快照 `.rollback_snapshots/k3-pdf-reader-bundle-split-preedit-20260605_012553`。残余风险：本 slice 只关闭当前 PDF Reader chunk warning，不做全站 bundle architecture 重排；若后续新增重型 routes，再按触发项处理。 |
| K4 | God-module Split | 后端大模块拆分 | 无前端 UI | 模块行数 < 阈值 | god-module-split | 可延后/P2 | Claude(后端/接口) | ⚠️ 部分实现：resources router 已拆 endpoints；未做全仓模块行数验收 |
| K5 | Router Prefix | API 路由前缀统一 | 无前端 UI | 所有路由 `/api/*` | router-prefix | 暂缓/P3 | 暂缓 | ⚠️ 部分实现/决策为混合保留：`docs/architecture/router-prefix-convention.md`，当前仍有 `/resources`、`/evolution`、`/memory` |
| K6 | Audit Fixes (9 commits) | 0.1.8.2 bug 修复 | 各页面 bug 修复验证 | 9 个 commit 验证通过 | 0182-release 9-commit list | 暂缓/P3 | 暂缓 | ✅ 已完成本地历史 commit 审计 (2026-06-05)：新增 `tools/eval/check_0182_historical_baseline.py` 与 `tests/test_0182_historical_baseline.py`，只读核验 0182 release plan 中 9 个 audit-fix commits 均存在、均 reachable from HEAD，且 commit subject 与 release plan 记录相符：`7d4df308`、`4127bbc8`、`9444c5a4`、`da8221ad`、`6e511d35`、`8a3b60f1`、`b8f5f819`、`bf61ae95`、`d598fc61`。输出 `workspace_artifacts/evaluations/k6-k8-0182-historical-baseline-audit-20260605.json`，状态 `LOCAL_HISTORICAL_BASELINE_AUDIT_PASS`，`external_api_calls: 0`，guardrails 固定为不改历史、不 stage/commit/push/tag/release、不 build/upload artifact、不改 `github/`、不读凭据。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_0182_historical_baseline.py -q -o addopts=""` 4/4 passed；`.\.venv-1\Scripts\python.exe -m compileall -q tools\eval\check_0182_historical_baseline.py tests\test_0182_historical_baseline.py` passed；CLI report 生成 passed。成熟方案核对：Git 官方 `git show` / `git merge-base --is-ancestor` 祖先关系口径、NASA requirements verification matrix traceability 口径、本地 reference study coverage/adaptation audit。Rollback：checkpoint `20260605-140325-k6-k8-historical-baseline-audit-preflight`、`20260605-141245-k6-k8-baseline-crosslink-final-preedit`，实体快照 `.rollback_snapshots/k6-k8-historical-baseline-checker-preedit-20260605_140423` 与 `.rollback_snapshots/k6-k8-final-crosslink-preedit-20260605_141320`。残余风险：这是本地 Git/docs 审计，不重跑历史浏览器截图、release build、tag、upload、GitHub Release 或 public readiness。 |
| K7 | Identity Injection | 身份注入修复 | 无前端 UI | `identity_injector.py` | identity-injection | 暂缓/P3 | 暂缓 | ❓ 未定位 |
| K8 | Baseline Anchor | 基线锚点建立 | 无前端 UI | `baseline_anchor.py` | baseline-anchor | 暂缓/P3 | 暂缓 | ✅ 已完成本地 baseline-anchor 审计 (2026-06-05)：`tools/eval/check_0182_historical_baseline.py` 只读验证 `docs/plans/active/2026-05-24-0182-baseline-anchor.md` 记录 Dialog / Inspector / Discussion 行为锚、build/chunk 基线、rollback commit `5dc27f63`、localStorage key、sendMessage 路径、Input 行为锚、Hotfix v1-v5 回归清单、Verification 命令、Stop conditions、MessageRenderer 验收与 FE-1 已知不一致；同时验证 release baseline commit `321927cd` 与 anchor rollback commit `5dc27f63` 均 reachable from HEAD。补充 `docs/plans/active/2026-05-24-0182-release-plan.md` 对 `2026-05-24-0182-baseline-anchor.md` 的直接 crosslink 后，CLI 输出 `LOCAL_HISTORICAL_BASELINE_AUDIT_PASS`。验证同 K6：4/4 focused pytest passed、compileall passed、CLI report 生成 passed。成熟方案核对：Git 官方 commit/ancestor docs、requirements traceability matrix 做法、既有 `2026-05-24-fullstack-deduplication-modularization-plan.md` anchor 消费记录。Rollback：checkpoint `20260605-141245-k6-k8-baseline-crosslink-final-preedit`，实体快照 `.rollback_snapshots/k6-k8-final-crosslink-preedit-20260605_141320`。残余风险：没有创建独立 `baseline_anchor.py` 产品模块；当前闭合的是计划中实际存在的 baseline-anchor 文档与本地审计 checker。 |
| K9 | Soak Gate | 发布后浸泡测试 | 无前端 UI | `soak_gate.py` 通过 | soak-gate | 暂缓/P3 | 暂缓 | ✅ 已完成本地六小时 soak/readiness 证据审计 (2026-06-05)，但不是新的 24h release soak：新增 `tools/eval/check_soak_replacement_gate.py` 与 `tests/test_soak_replacement_gate.py`，只读审计既有 `workspace_artifacts/soak/dispatch-soak-24h-20260515_143030.jsonl.summary.json`、G3/G3-rerun metrics、D-CO-8 composite gate amendment、replacement-gate plan 和 factual soak runbook；输出 `workspace_artifacts/evaluations/k9-soak-replacement-local-readiness-20260605.json`，状态 `LOCAL_COMPOSITE_GATE_PASS_SIX_HOUR_EVIDENCE`，`external_api_calls: 0`，实际 duration `23345.849s = 6.485h`，371 successes / 0 failures，`leak_detected=false`，`leak_warnings=[]`，G3/G3-rerun `recall_at_5=0.84` 与 `mrr=0.6857` 稳定通过 post-drift floor。验证：`.\.venv-1\Scripts\python.exe -m pytest tests\test_soak_replacement_gate.py tests\test_dispatch_cooldown_leak.py -q -o addopts=""` 12/12 passed；`compileall -q tools\eval\check_soak_replacement_gate.py tests\test_soak_replacement_gate.py` passed；CLI report 生成 passed。成熟方案核对：Google SRE canary/testing reliability、NIST/TREC-style IR metric gate、本仓 soak replacement plan。Rollback：checkpoint `20260605-135502-k9-six-hour-soak-evidence-audit-preflight`，实体快照 `.rollback_snapshots/k9-soak-readiness-checker-preedit-20260605_135703`。残余风险：这是既有 6.49h dispatch ping + deterministic/canary composite evidence audit，不可表述为 fresh 24h release soak；未执行 release/upload/tag/push/publish/deployment/credential action。 |
| K10 | Runbooks | 运维手册 | 无前端 UI | `docs/plans/runbooks/` 文档齐全 | docs/plans/README.md | 暂缓/P3 | 维护 | ✅ 已实现（静态定位）：`docs/plans/runbooks/` 存在多项执行/发布/功能 runbook |

---

## L. 项目级思维偏置 (Project Reasoning Bias)

> 接续规则：当 A-K 中 P0/P1 行已完成、阻塞并记录、或被明确降级后，另一个终端的 Codex/Claude 必须继续本组，不得因为“功能矩阵主队列完成”而停顿。执行前读 `docs/plans/active/2026-05-28-project-reasoning-bias-injection-plan.md`、`AI_WORKSPACE_GUIDE.md`、`docs/plans/autonomous-execution-planning-playbook.md`，并按该计划的 Slice 0→8 顺序执行。

| ID | 功能 | 期望效果 | 前端验收点 (界面是否齐) | 后端验收点 (链路是否通) | 来源 plan | 文献助手必要性/优先级 | 执行方 | 状态 |
|----|------|----------|------------------------|------------------------|-----------|----------------------|--------|------|
| L1 | 项目思维偏置治理接入 | 执行者能从矩阵发现并接续项目思维偏置计划 | 无前端 UI | runbook 记录代码锚点、回档快照、成熟方案复核 | `2026-05-28-project-reasoning-bias-injection-plan.md` Slice 0 | 是/P0 必做 | Claude→Codex | ✅ 治理接入已完成 (2026-05-28)：已读取 `AI_WORKSPACE_GUIDE.md`、`docs/plans/autonomous-execution-planning-playbook.md`、本矩阵和 active plan；新增 runbook `docs/plans/runbooks/project-reasoning-bias-slice0-governance.md`，记录 dirty tree 边界、回档、成熟方案复核、当前代码锚点和 L2 执行边界。锚点验证：`rg -n "analysis_chain_rag|render_analysis_chain_prompt_block|KnowledgeBase|ProjectPayload|system_prompt" literature_assistant frontend`，确认 `feature_flags.py:68/78`、`analysis_chain_helpers.py:80`、`analysis_chain_rag_builder.py:151-160`、`frontend/src/pages/KnowledgeBase.tsx:117`、`models/resources.py:19`、`endpoints_projects.py:30/58/70/380`、`chat_router.py:434/593`、`intelligent_chat_router.py:775`、`discussion_orchestrator.py:470/1151` 仍是现行入口。成熟方案核对：FastAPI 官方 `response_model`/`TestClient` 模式、Pydantic 官方 `Field(default_factory/max_length)` 字段约束；本 slice 无产品代码外部依赖。Rollback：checkpoint `20260528-025223-l1-project-reasoning-bias-governance`，实体快照 `.rollback_snapshots/l1-project-reasoning-bias-governance-20260528_025318/`。残余风险：后续 L2-L7 仍需后端实现，L8/L9 需待 API 后做 UI 与浏览器验收。 |
| L2 | 后端模型 + CRUD | 项目 metadata 持久化 `project_reasoning_bias`，提供读取/保存 API | KnowledgeBase 后续能加载/保存该字段 | `GET/PUT /resources/project/{project_id}/reasoning-bias`；`ProjectPayload.project_reasoning_bias` additive 暴露；保留 `source_folder` | 同上 Slice 1 | 是/P0 必做 | Claude(后端/接口) | ✅ 后端模型与 CRUD 已完成并接口验收 (2026-05-28)：新增 `models/project_reasoning_bias.py`，定义 `ProjectReasoningBiasScopes`、`ProjectReasoningBiasPayload`、`ProjectReasoningBiasUpdateRequest`，约束 `human_bias <= 4000`、`language in zh/en/auto`、`updated_by` 枚举、`discussion_agent_ids` 去重并限制 16 个合法 agent id；`models/resources.py:19` 的 `ProjectPayload` additive 暴露 `project_reasoning_bias`；`models/__init__.py` 导出新模型；`resources_router/endpoints_projects.py` 新增 `_project_payload_from_resource()` 统一保留 `source_folder` 并解析偏置，新增 `GET /resources/project/{project_id}/reasoning-bias` 与 `PUT /resources/project/{project_id}/reasoning-bias`，保存时只替换 `metadata["project_reasoning_bias"]`，不覆盖其他 metadata。验证：`pytest tests/test_project_reasoning_bias_api.py -v -o addopts=""` 5/5 passed，覆盖旧项目默认空对象、保存/读取、`source_folder` 保留、二次保存替换、超长文本/非法 agent id 422、缺失项目 404；`compileall -q literature_assistant\core\models literature_assistant\core\routers` passed；`pytest tests/test_resources_router_contract.py -v -o addopts=""` 6/6 passed，证明项目/材料/导出/引用链路未回归。成熟方案核对：FastAPI 官方 `response_model`/`TestClient` 模式、Pydantic 官方 `Field(default_factory/max_length)` 与 validator 模式。Rollback：checkpoint `20260528-025824-l2-project-reasoning-bias-backend-crud`，实体快照 `.rollback_snapshots/l2-project-reasoning-bias-backend-20260528_025835/`；runbook `docs/plans/runbooks/project-reasoning-bias-slice1-backend-crud.md`。残余风险：OpenAPI/TS client 尚未再生成，L8 UI 尚未接入，L7 optimizer 未实现。 |
| L3 | 统一偏置渲染器与注入注册器 | 所有 AI surface 用同一套 scope 判定和安全 prompt block | 无直接 UI；后续 toggle 复用 scope 判定 | `prompts/project_reasoning_bias.py` 提供 load/should_apply/render/apply；支持 `analysis_chain`、`chat_generation`、`discussion_agent_ids`、`project_wide`、request override | 同上 Slice 2 | 是/P0 必做 | Claude(后端/接口) | ✅ 统一偏置渲染器与注入注册器已完成 (2026-05-28)：新增 `literature_assistant/core/prompts/project_reasoning_bias.py`，提供 `ProjectReasoningBiasContext`、`ProjectReasoningBiasSurfaceProfile`、`register_project_reasoning_bias_surface()`、`registered_project_reasoning_bias_surfaces()`、`load_project_reasoning_bias()`、`should_apply_project_reasoning_bias()`、`render_project_reasoning_bias_block()`、`apply_project_reasoning_bias()`；默认注册 analysis_chain / analysis_chain_rag / analysis_chain_discussion / analysis_chain_ui / inspiration_irac / inspiration_fincot / chat / dialog / intelligent_chat / writing_generation / outline_generation / citation_suggestion / discussion / discussion_agent / discussion_synthesis 等 surface family。`should_apply` 依据 `surface + agent_id + request_enabled` 决定是否注入，`project_wide` 优先，其次按 analysis_chain/chat_generation/discussion 分组；`render` 将保存的偏好作为 JSON 数据块包裹在 `[项目思维偏置]...[/项目思维偏置]` 或 English `[PROJECT_REASONING_BIAS]...[/PROJECT_REASONING_BIAS]` 中，并明确标注低优先级、不能覆盖系统/开发者规则、证据边界、安全要求、当前用户请求、最小证据原则或验证门。验证：`pytest tests/test_project_reasoning_bias_prompt.py -v -o addopts=""` 7/7 passed，覆盖 registry、scope 命中、空偏置不注入、中文/英文 render、安全分隔、append 语义、`load_project_reasoning_bias()` 读回 L2 metadata；`compileall -q literature_assistant\core\prompts` passed。成熟方案核对：OWASP LLM prompt injection guidance 的“把不可信内容与指令分隔并当作数据”原则、OpenFeature 式 evaluation context（surface/agent/request override）思路、仓库现有 `analysis_chain_helpers.py` 与 `identity_renderer.py` 的组合式 prompt assembly 模式。Rollback：checkpoint `20260528-030407-l3-project-reasoning-bias-prompt-registry`，实体快照 `.rollback_snapshots/l3-project-reasoning-bias-prompt-20260528_030418/`；runbook `docs/plans/runbooks/project-reasoning-bias-slice2-prompt-registry.md`。残余风险：尚未接入真实 chat/discussion surface，registry 需要在 L4-L6 继续消费。 |
| L4 | Chat / IntelligentChat 注入 | 项目偏置可影响聊天与生成；当前请求可临时关闭 | ChatInput/SmartRead 显示“项目偏置”状态或 toggle | `chat_router.py` 与 `intelligent_chat_router.py` 接入 `project_reasoning_bias_enabled`；stream/non-stream 一致；超预算优先丢弃偏置 | 同上 Slice 3 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端 `chat_router.py` 的 `ChatRequest/ChatStreamRequest` 与 `intelligent_chat_router.py` 的 `IntelligentChatRequest` 已支持 `project_id`、`project_reasoning_bias_enabled`，并在 stream/non-stream 系统提示中按 L3 scope 注入低优先级 `[项目思维偏置]` block；本次前端补齐用户可见 current-request toggle：`useProjectReasoningBiasState.ts:37-76` 从 `/resources/project/{id}/reasoning-bias` 读取项目 scope 并判断 `chat_generation` 是否可用；`ProjectBiasSurfaceToggle.tsx:10-40` 从只读状态 pill 升级为可控 `role="switch"`；`ChatInput.tsx:40-43,81-87,164-168,285-295` 渲染“项目偏置”开关并把 `projectReasoningBiasEnabled` 写进提交 payload；`Conversation.tsx:36-42,119` 将开关透传给共用输入框；`intelligentChatApi.ts:88,294,324` 在 `/api/chat` 与 `/api/chat/stream` 请求体包含 `project_reasoning_bias_enabled`；`SmartReadContext.tsx:72,620` 支持 Workbench SmartRead 发送时透传；`Dialog.tsx:320-323,491,731-736`、`ResearchWorkbench.tsx:102-127,270-289,384-389` + `ResearchWorkbenchInspector.tsx:33-40,116`、`IntelligentChat.tsx:108-115,210,447-455` 均展示/发送当前请求开关。验证：后端既有 `pytest tests/test_project_reasoning_bias_chat.py tests/test_chat_router_analysis_chain.py -v -o addopts=""` 13/13 passed、`pytest tests/test_intelligent_chat_router.py -v -o addopts=""` 27/27 passed；本次前端 `npm run test -- src/components/chat/ChatInput.test.tsx src/components/chat/Conversation.test.tsx src/services/intelligentChatApi.test.ts --run` 20/20 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；浏览器 smoke `L4_FRONTEND_PORT=5318 node workspace_artifacts/runtime_state/l4-chat-project-bias-toggle-smoke.mjs` passed，mock 项目启用 `chat_generation` 后打开 `/dialog`，确认开关可见、关闭后发送请求体含 `project_reasoning_bias_enabled:false`，截图 `workspace_artifacts/screenshots/l4-chat-project-bias-toggle-20260528.png`。成熟方案核对：React 官方 shared/controlled state 模式、WAI-ARIA switch pattern、Playwright `page.route` + screenshot smoke；沿用 FastAPI/Pydantic 与 OWASP prompt 分隔原则。Rollback：后端 checkpoint `20260528-031353-l4-project-reasoning-bias-chat-backend`，前端 checkpoint `20260528-050601-l4-chat-project-bias-toggle-ui`，矩阵实体快照 `.rollback_snapshots/matrix-l4-chat-project-bias-toggle-20260528_051727`。残余风险：该开关按当前项目保存的 `chat_generation/project_wide` scope 默认启用，失败时 fail-open 为不影响发送；RAGWorkflow fast path 仍按既有后端 L9 结果覆盖，真实 LLM 未在本 UI smoke 中调用。 |
| L5 | AnalysisChain / Inspiration 注入 | 用户偏置影响 6 字段推理摘要关注方向，但不覆盖最小证据/验证门 | Inspiration/Dialog 生成入口显示“思维链偏置”状态 | `analysis_chain_helpers.py`、`analysis_chain_rag_builder.py`、`inspiration_router.py`、IRAC/FinCoT 模板支持偏置 block；无 bias 行为等价 | 同上 Slice 4 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端保留既有 `analysis_chain_rag_builder.py:150-364` 的可选 `project_reasoning_bias` 注入，deterministic 输出不变；`inspiration_router.py:108` 增加 `project_reasoning_bias_enabled` request toggle，`:375-402` 在 LLM inspiration prompt 追加 L3 分隔 bias block，`:628-646` 按 `analysis_chain` scope 与当前请求开关解析项目偏置，`:667-671` 接入生成链路，IRAC/FinCoT 模板文件未改写。本次 Codex 补齐前端入口：`Inspiration.tsx:213-221,266-268,337-346` 与 `InspirationPanel.tsx:72-102,200-209` 在生成按钮附近展示可控“思维链偏置”switch，并仅把当前请求状态传入 `generateSparks()`；`inspirationService.ts:162-176` 将 `project_reasoning_bias_enabled` 发送到 `/inspiration/generate`；浏览器 smoke `workspace_artifacts/runtime_state/l5-inspiration-project-bias-toggle-smoke.mjs:1-238` mock 写作项目、reasoning-bias API 和 inspiration 生成，断言关闭开关后请求体含 `project_reasoning_bias_enabled:false` 且页面仍显示生成结果。验证：`pytest tests/test_project_reasoning_bias_analysis_chain.py tests/test_analysis_chain_helpers.py tests/test_analysis_chain_rag_builder.py tests/test_inspiration_prompt_templates.py -v -o addopts=""` 39/39 passed；`compileall -q literature_assistant\core\analysis_chain_rag_builder.py literature_assistant\core\routers\inspiration_router.py` passed；`npm run test -- src/services/inspirationService.test.ts src/components/writing/InspirationPanel.test.tsx --run` 4/4 passed；`npm run build` passed（仅既有 `annotationApi.ts` 动静态 import warning）；`L5_FRONTEND_PORT=5319 node workspace_artifacts/runtime_state/l5-inspiration-project-bias-toggle-smoke.mjs` passed，截图 `workspace_artifacts/screenshots/l5-inspiration-project-bias-toggle-20260528.png`。成熟方案核对：沿用 L3 的 OWASP prompt injection 分隔原则、仓库 AnalysisChain/Inspiration 组合式 prompt 架构、React controlled state 模式、WAI-ARIA switch pattern、Playwright 官方 `page.route` 网络 mock + screenshot smoke。Rollback：backend checkpoint `20260528-032510-l5-project-reasoning-bias-analysis-inspiration`，前端/smoke checkpoint `20260528-052212-l5-inspiration-project-bias-toggle-ui` 与 `20260528-053231-l5-inspiration-project-bias-toggle-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-l5-inspiration-project-bias-toggle-20260528_053825`。残余风险：当前浏览器验收使用本地 mock，不调用真实 LLM；`/inspiration` 独立路由现在按 B1 重定向 `/dialog`，可见 Inspiration 入口主要在写作右栏/Dialog 工作流，未恢复独立旧页面入口。 |
| L6 | Discussion 单 agent 注入 | 可指定单个 agent 受项目偏置影响；全项目时所有 agent/synthesis 生效 | DiscussionPanel agent 行可见偏置命中状态 + 开关；讨论前可切换本次请求 | `discussion_orchestrator.py` 按 `discussion_agent_ids` 注入；前端 `DiscussionPanel` 发送 `project_reasoning_bias_enabled`；旧 request 兼容；carry-over 预算不回归 | 同上 Slice 5 | 是/P0 必做 | Claude→Codex | ✅ 已完成并浏览器验收 (2026-05-28)：后端单 agent/project-wide 注入仍由 `discussion_orchestrator.py` + `models/discussion.py:204-206` 处理；Codex 补齐前端可见接线：`DiscussionPanel.tsx:445-452,723,943-951,1055-1059` 读取当前项目偏置、在角色卡显示“偏置作用中/偏置未命中”、在讨论区顶部提供 `ProjectBiasSurfaceToggle`，并将当前请求开关写入 `DiscussionRunConfig.project_reasoning_bias_enabled`；`discussionApi.ts:100-117` 增加该请求字段类型。验证：`npm run test -- src/components/DiscussionPanel/DiscussionPanel.mcpScope.test.tsx --run` 2/2 passed；`npm run build` passed；浏览器 smoke `L6_FRONTEND_PORT=5320 node workspace_artifacts/runtime_state/l6-discussion-project-bias-toggle-smoke.mjs` passed，断言开关可见、关闭后请求体含 `project_reasoning_bias_enabled:false`，并生成截图 `workspace_artifacts/screenshots/l6-discussion-project-bias-toggle-20260528.png`。成熟方案核对：React controlled state + shared switch 模式、WAI-ARIA switch pattern、Playwright `page.route` 网络 mock + screenshot smoke。Rollback：checkpoint `20260528-053934-l6-discussion-project-bias-toggle-ui`，矩阵实体快照 `.rollback_snapshots/matrix-l6-discussion-project-bias-toggle-20260528_054013`。残余风险：DiscussionPanel 的运行与历史分区仍保留既有结构，旧 `discussion_router.py` 简单 turn runner 未接入本次 toggle 接线；本 slice 只覆盖 RAG-aware DiscussionPanel/`DiscussionRunConfig` 流。 |
| L7 | AI 优化项目偏置 endpoint | AI 帮用户把偏置写成更清晰的 6 字段对齐建议，且不自动保存 | KnowledgeBase “AI 优化”弹窗后续可复用本契约；当前 slice 仅闭合后端建议 API | `POST /resources/project/{project_id}/reasoning-bias/optimize`；LLM 失败 deterministic fallback；不输出隐藏思维链要求；不写回 metadata | 同上 Slice 6 | 是/P1 应做 | Claude→Codex | ✅ 已完成并验证 (2026-05-28)：新增 `literature_assistant/core/prompts/reasoning_bias_optimizer.py`，提供 `build_reasoning_bias_optimizer_prompt()`、`resolve_optimizer_language()`、`deterministic_reasoning_bias_optimization()`、`parse_reasoning_bias_optimizer_response()`，对用户输入做结构化分隔、隐藏思维链短语清洗和中英 fallback；`literature_assistant/core/models/project_reasoning_bias.py` 新增 `ProjectReasoningBiasOptimizeRequest/Response` 与 six-field `ProjectReasoningBiasFieldSuggestions`；`resources_router/endpoints_projects.py:231-310` 增加 `POST /resources/project/{project_id}/reasoning-bias/optimize`，优先使用现有 `get_ai_adapter()`，LLM 不可用或抛错时回退 deterministic 建议，且仅返回建议、不修改 `project_reasoning_bias` metadata；`resources_router/__init__.py` 已 re-export 新 endpoint；`models/__init__.py` 导出新模型。验证：`pytest tests/test_project_reasoning_bias_optimizer.py tests/test_project_reasoning_bias_api.py -v -o addopts=""` 11/11 passed；`compileall -q literature_assistant\\core\\models literature_assistant\\core\\prompts literature_assistant\\core\\routers\\resources_router tests\\test_project_reasoning_bias_optimizer.py` passed。成熟方案复核：OpenAI Prompt Optimizer/Prompting 官方文档、OWASP LLM Prompt Injection Prevention Cheat Sheet、FastAPI `response_model`/`TestClient` 模式。Rollback：checkpoint `20260528-034416-l7-project-reasoning-bias-optimizer`，实体快照 `.rollback_snapshots/l7-project-reasoning-bias-optimizer-20260528_035141/`；runbook `docs/plans/runbooks/project-reasoning-bias-slice6-optimizer.md`。残余风险：前端 KnowledgeBase “AI 优化”弹窗尚未接入该契约，L8 继续。 |
| L8 | KnowledgeBase 项目偏置栏 | 项目创建后在知识库界面可输入偏置、选范围、保存、AI 优化 | `KnowledgeBase.tsx:553` 挂载 `ReasoningBiasBar` 于 `PageHeader` 下；`ReasoningBiasBar.tsx:44-209` 提供偏置文本框、`ReasoningBiasScopePopover.tsx:39-104` 提供思维链/聊天与生成/单个 agent/全项目范围、`ReasoningBiasOptimizerDialog.tsx:34-121` 提供 AI 优化弹窗；移动/桌面无水平溢出 | `frontend/src/services/projectReasoningBiasApi.ts:56-134` 调用 `/resources/project/{id}/reasoning-bias` 与 `/optimize`；`KnowledgeBase.tsx:150-173` 继续复用项目上下文与材料加载；保存后状态刷新，`activeProjectId` 为空时整栏隐藏 | 同上 Slice 7 | 是/P0 必做 | Codex(UI) | ✅ 已完成并验证 (2026-05-28)：新增 `frontend/src/components/knowledge/ReasoningBiasBar.tsx`、`ReasoningBiasScopePopover.tsx`、`ReasoningBiasOptimizerDialog.tsx`、`ProjectBiasSurfaceToggle.tsx` 与 `frontend/src/services/projectReasoningBiasApi.ts`；`KnowledgeBase.tsx:553` 直接挂载偏置栏。验证：`npm run test -- src/services/projectReasoningBiasApi.test.ts src/components/knowledge/ReasoningBiasBar.test.tsx --run` 6/6 passed；`npm run build` passed；浏览器 smoke `node workspace_artifacts/runtime_state/l8-knowledgebase-reasoning-bias-smoke.mjs` 通过，断言加载项目偏置、勾选范围、打开 AI 优化弹窗、采纳优化版、保存成功、桌面/移动无水平溢出；截图 `workspace_artifacts/screenshots/l8-knowledgebase-reasoning-bias-20260528.png` 与 `workspace_artifacts/screenshots/l8-knowledgebase-reasoning-bias-mobile-20260528.png`。成熟方案复核：Playwright 官方 `page.route` + `page.screenshot` 的网络 mock / 截图模式；OpenAPI 生成链继续沿用 FastAPI schema + openapi-typescript CLI。Rollback：checkpoint `20260528-042101-l8-knowledgebase-reasoning-bias-browser-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-l8-l9-20260528_044213`。残余风险：`ProjectBiasSurfaceToggle` 仍是状态展示型按钮，尚未接入每个 AI surface 的局部切换；L9 继续做全链路 smoke 与契约收口。 |
| L9 | OpenAPI + 全链路 smoke | 后端、类型、UI、chat、discussion、AnalysisChain 全链路可验收 | `frontend/openapi/modular-pipeline-openapi.json:8758-9039` 与 `frontend/src/generated/openapi.ts:8484-8676` 已同步新增 `ProjectReasoningBias*` schema；`workspace_artifacts/runtime_state/l9-project-reasoning-bias-full-chain-smoke.py:80-242` 走 chat / AnalysisChain / discussion / inspiration 四条校验链路，断言 `[项目思维偏置]` 注入与 request toggle-off 关闭都成立 | `tests/test_project_reasoning_bias_api.py`、`tests/test_project_reasoning_bias_prompt.py`、`tests/test_project_reasoning_bias_chat.py`、`tests/test_project_reasoning_bias_analysis_chain.py`、`tests/test_project_reasoning_bias_discussion.py`、`tests/test_project_reasoning_bias_optimizer.py` 共 34 条全部通过；`npm run build` 通过；OpenAPI 生成命令 `npm run generate:openapi` 通过；full-chain smoke 生成 `workspace_artifacts/runtime_state/l9-project-reasoning-bias-full-chain-smoke.json` | 同上 Slice 8 | 是/P0 必做 | Claude→Codex | ✅ 已完成并验证 (2026-05-28)：OpenAPI schema 与 TS 生成物已同步 `ProjectReasoningBiasPayload/UpdateRequest/OptimizeRequest/OptimizeResponse/Scopes`；后端/前端/浏览器兼容证据齐全。验证：`npm run generate:openapi` passed；`.\\.venv-1\\Scripts\\python.exe workspace_artifacts\\runtime_state\\l9-project-reasoning-bias-full-chain-smoke.py` passed；`.\\.venv-1\\Scripts\\python.exe -m pytest tests\\test_project_reasoning_bias_api.py tests\\test_project_reasoning_bias_prompt.py tests\\test_project_reasoning_bias_chat.py tests\\test_project_reasoning_bias_analysis_chain.py tests\\test_project_reasoning_bias_discussion.py tests\\test_project_reasoning_bias_optimizer.py -v -o addopts=\"\"` 34/34 passed；`npm run test -- src/services/projectReasoningBiasApi.test.ts src/components/knowledge/ReasoningBiasBar.test.tsx --run` 6/6 passed；`npm run build` passed。Artifact：`workspace_artifacts/runtime_state/l9-project-reasoning-bias-full-chain-smoke.json`。Rollback：checkpoint `20260528-043022-l9-openapi-full-chain-smoke`，矩阵实体快照 `.rollback_snapshots/matrix-l8-l9-20260528_044213`。残余风险：L9 smoke 仍是本地 TestClient + prompt-assembly 验证，未连真实 LLM；每个 AI surface 的显式前端 toggle 仍可作为后续 UI slice。 |

---

## 统计摘要

- **总计**: 154 功能点
- **模块分布**: A(11) + B(8) + C(16) + D(23) + E(16) + F(13) + G(16) + H(10) + I(10) + J(12) + K(10) + L(9) = 154 行
- **状态**: 已按静态代码证据、自动化测试、构建验证和浏览器验收持续更新为 ✅ / ⚠️ / ❓；每行状态是当前队列事实源，旧摘要不得覆盖表格行状态
- **当前队列审计 (2026-06-05 01:15 CST)**: A-K 的 P0/P1 行与 L1-L9 当前均为 ✅；用户已通过 `docs/plans/local-feature-execution-plan-2026-06-05.md` 重新授权实用本地产品功能，E12 已按该计划完成本地闭环。
- **下一步**: 不再按旧摘要继续执行 H3/H4/G 或 D/J/K；这些 P0/P1 行在表格中已有完成证据。若继续执行 P2/P3 实用本地功能，必须先做 global fit check、rollback、成熟/官方方案搜索、目标 dirty diff inspection、focused verification，并回写本矩阵和 active plan。不要写“矩阵已闭合”。

---

## 使用说明

1. **验收流程**: 实现功能后，先验证前端界面是否齐全，再验证后端链路是否联通
2. **状态更新**: ✅ = 静态定位支持或前端+后端验收通过 | ⚠️ = 部分完成/契约不一致 | ❌ = 明确未实现 | ❓ = 待验证/未定位
3. **来源追溯**: "来源 plan" 列可追溯到 `docs/plans/active/` 具体计划文件
4. **功能统计优先级**: 本文件是 145 项功能的当前验收入口，优先级高于零散历史计划；后续每次执行前先对照本文件选 slice，每次执行后必须回到本文件更新状态、代码定位、验证结果和下一步判断
5. **持续更新**: 每次 slice / sprint 结束后更新本矩阵，保持与实现同步；如果实现发现某项不是文献助手当前需要的功能，改优先级而不是直接删除
6. **证据要求**: 不能只因代码存在就改 ✅；✅ 必须写清前端入口、后端/API 链路、关键代码定位和至少一个验证方式。未跑浏览器或后端测试时，状态说明里必须保留“静态定位”或“待验收”
7. **回档要求**: 修改本文件前要按 `AI_WORKSPACE_GUIDE.md` 建立回档点；`docs/` 当前被忽略，除 git checkpoint 外还应保留目标文件实体快照到 `.rollback_snapshots/`
8. **接续要求**: 另一个终端的 Codex/Claude 若发现 A-K 的 P0/P1 已完成、阻塞并记录、或明确降级，必须把 L1-L9 作为下一批授权任务继续执行；不能在“矩阵主队列完成”后停下等待用户。

---

## 本轮代码定位索引（2026-05-26 静态对照）

- 路由注册：`literature_assistant/core/python_adapter_server.py:674`-`702` 注册 resources、wiki、discussion、credentials、MCP、evolution、feature flags。
- SmartRead：`literature_assistant/core/routers/intelligent_chat_router.py:1410` `/api/chat`，`:1427` `/api/chat/stream`，`:1928` sessions，`:1958` resume；`literature_assistant/core/routers/chat_router.py:862` deprecated `/chat/ask`；`literature_assistant/core/chat/pipeline.py:534`、`:688` session/evidence helpers；`frontend/src/pages/Dialog.tsx:709` `Conversation`。
- Chunk locator / evidence jump：`literature_assistant/core/routers/resources_router/endpoints_search_upload.py:363` `/resources/chunks/{chunk_id}/locator`；`frontend/src/components/evidence/EvidencePill.tsx:107` locator upgrade，`:122` 跳 `/workbench/paper/:materialId`。
- Workbench / PDF：`frontend/src/components/workbench/ResearchWorkbenchInspector.tsx:52` Inspector；`frontend/src/components/PdfViewer/PdfViewer.tsx:2` react-pdf/pdfjs，`:453` selection rects，`:572` continuous pages；`frontend/src/components/PdfViewer/PdfReaderShell.tsx:94` highlights/notes/outline shell。
- Discussion：`literature_assistant/core/routers/discussion_advanced_router.py:304` run，`:334` stream，`:558` get run，`:576` resume stream；`literature_assistant/core/discussion_orchestrator.py:654` evidence pack，`:721` auto-stop，`:783` parallel round，`:1083` analysis chain；`frontend/src/components/DiscussionPanel/DiscussionPanel.tsx:914` evidence pack UI，`:968` cited evidence pills。
- Inspiration：`literature_assistant/core/routers/inspiration_router.py:123` `SparkEvidenceRef`，`:155` evidence refs builder，`:753` generate，`:876` context；`frontend/src/components/inspiration/SparkEvidencePills.tsx:33` evidence refs UI；`frontend/src/components/inspiration/InspirationGraphSection.tsx:31` graph section。
- Evolution / Memory：`literature_assistant/core/routers/evolution_router.py:80` candidates，`:158` accept，`:167` reject，`:185` rollback，`:194` promote，`:236` curate，`:327` manual capture；`literature_assistant/core/evolution/store.py:101` candidates table；`literature_assistant/core/evolution/promotion.py:161` MemPalace add_memory；`literature_assistant/core/routers/memory_router.py:33` `/memory/search`。
- Wiki：`literature_assistant/core/runtime_env.py:103` wiki 默认关闭；`literature_assistant/core/routers/wiki_router.py:311` status，`:338` compile，`:387` query，`:456` pages，`:494` doctor，`:501` graph，`:509` review；`frontend/src/pages/WikiWorkbench.tsx:272` 当前 `WikiKnowledgeFlowCard`；`frontend/src/components/wiki/*` 对应 status/pages/doctor/graph/review panels。
- Writing：`literature_assistant/core/routers/resources_router/endpoints_projects.py:30` projects，`:293` sections；`endpoints_materials_drafts.py:31` materials，`:151` draft citation anchors；`endpoints_search_upload.py:307` figure/table candidates；`endpoints_export_stats.py:19` project export；`frontend/src/pages/writing/SourcesCitations.tsx:24`，`FiguresTables.tsx:25`，`frontend/src/components/writing/ReferenceDrawer.tsx:99` export/citation chain，`WritingCanvas.tsx:111` insert citation token。
- Settings/Credentials：`literature_assistant/core/routers/credentials_router.py:44` `/api/credentials`，`:69` list，`:84` create，`:200` test；`literature_assistant/core/models/credentials.py:39` strategy enum；`frontend/src/pages/Settings.tsx:452` smart read cost tier，`:992` rerank config，`:1704` discussion defaults；`frontend/src/components/settings/CredentialsSection.tsx` credentials UI。
- MCP/Skill：`literature_assistant/core/models/mcp.py:35` `STREAMABLE_HTTP`；`literature_assistant/core/routers/mcp_router.py:91` servers，`:497` pending calls，`:509` decide；`mcp_installer_router.py:151` scan，`:216` install；`skills_router.py:74` approvals，`:112` security，`:209` import，`:263` uninstall/rollback snapshot path。
- Release/Ops：`scripts/release_forbidden_path_scan.py:1`，`scripts/release_secret_scan.py:47`，`scripts/build_windows_exe.ps1:1`，`scripts/smoke_frozen_first_launch.py:95`，`scripts/soak_dispatch_path.py:195`，`docs/architecture/router-prefix-convention.md`。

---

## 结论与下一步

1. **当前事实源是表格行状态**：截至 2026-06-04 23:36 CST，A-K 中 P0/P1 行已全部标为 ✅，L1-L9 也已全部标为 ✅。旧摘要里提到的 H3/H4/G、D/J/K 缺口已被后续行级记录覆盖，不能再作为下一 slice 队列。
2. **剩余非 ✅ 项只剩 P2/P3**：A1、A6、C16、K1、K4-K9 仍未完成、未定位或需要重新审计；这些当前均是可延后/P2 或暂缓/P3，只能在 active plan 明确授权、global fit check 通过且不触及 approval-only 边界时执行。E13/E14 已按 2026-06-05 reduced local contract/display slice 完成，真实因果抽取和 provider/eval 仍保留为显式授权后续项。
3. **PDF-first 计划是 overlay，不是功能矩阵替代品**：`docs/plans/pdf-first-implementation-master-plan-2026-06-04.md` 的 S24 后停止理由只说明 PDF-first overlay 队列耗尽，不得解释为产品功能总计划闭合。
4. **若继续实现，必须沿 active plan 逐 slice 审计**：当前重新授权入口是 `docs/plans/local-feature-execution-plan-2026-06-05.md`；每个 P2/P3 本地功能仍需单独判断是否是实用产品功能、是否可本地验证、是否避开发布/上传/tag/deploy/凭据/生产/付费边界。
5. **回档记录**：本次只修正文档队列口径，未改产品代码。回档快照位于 `.rollback_snapshots/plan-boundary-audit-20260604-233604`。

---

## Claude 长跑提示词（后端与接口）

把下面整段复制给 Claude。它的范围是后端、数据模型、API 契约、OpenAPI/类型客户端和接口测试；前端 UI、交互逻辑、布局、浏览器验收由 Codex 接续完成。若另一个终端已经在跑本矩阵，它读到本段后应把 L 组作为 A-K 之后的强制接续队列。

```text
你在 C:\Users\xiao\Desktop\tools\Modular-Pipeline-Script 工作。请进入自决策长跑模式，但只做本提示授权的本地代码工作。

硬性前置：
1. 先读 AI_WORKSPACE_GUIDE.md、docs/plans/autonomous-execution-planning-playbook.md、docs/plans/2026-05-26-functional-acceptance-matrix.md。
2. 执行 git status --short --branch，确认 dirty worktree。不要回滚、覆盖、格式化或删除你没改的文件。
3. 每个非平凡 slice 开始前建立回档：优先用本地 checkpoint；涉及 docs/plans/2026-05-26-functional-acceptance-matrix.md 时，还要复制实体快照到 .rollback_snapshots/。
4. 每个 slice 设计前搜索官方或成熟方案。按 slice 选择 FastAPI/Pydantic/Starlette SSE、MCP 官方协议、凭证/权限/沙箱安全实践、引用/参考文献管理、需求追踪矩阵等主来源。记录你参考了什么，不要复制外部代码。

工作入口：
- 以 docs/plans/2026-05-26-functional-acceptance-matrix.md 为唯一功能队列入口。
- 只处理“状态不是 ✅”且“文献助手必要性/优先级”为 是/P0 必做 或 是/P1 应做 的行。
- 你优先处理“执行方”为 Claude(后端/接口) 或 Claude→Codex 的行；Codex(UI) 行只允许补后端契约、OpenAPI schema、类型客户端，不做页面布局和视觉。
- 每个 slice 结束必须回写该矩阵：状态、代码定位、验证命令、剩余缺口、下一步。不要写“矩阵已闭合”。
- A-K 中 P0/P1 行全部完成、阻塞并记录、或明确降级后，不要停顿；直接继续执行 L1-L9（项目级思维偏置注入计划）。执行 L 组前必须读取 docs/plans/active/2026-05-28-project-reasoning-bias-injection-plan.md。

最高优先级顺序：
1. AI 基础设施与权限协议：
   - B5、I5、I6：凭证采样、默认成本档位、后端枚举/前端类型契约统一。
   - D1、D2：Discussion 运行时凭证选择与采样接口契约。
   - J1、J4、J7、J8、J10、J11：MCP v0.3/v0.4 兼容证据、工具审批/状态、安装探测、per-agent scope、Skill 沙箱、导入导出。
   - G14：Wiki 页面访问控制的后端模型与 API，先做可本地验证的最小权限层，不接生产身份系统。
2. Wiki 后端与接口：
   - G1、G2、G5、G6、G7、G11、G12、G15：页面存储/CRUD、query/search alias、版本历史、compiler 非破坏性提交路径、标签/分类、导出。
   - 保持 Wiki 为“证据支撑的编译知识层”，不要把它收窄成 Evolution 入口。
3. 写作后端与接口：
   - H1-H5、H7、H8、H10：/api/writing/* 兼容 alias 或矩阵契约修正，项目/大纲/引用/图表/审稿提交/大纲生成/引用建议/导出。
   - H3 当前不是 Word 交叉引用和自动参考文献目录；若实现，先补 source metadata、citation anchor、bibliography export 的后端数据模型和 API。
   - H4 当前是文本派生候选；若实现图表管理，先补 figure/table asset CRUD、编号、source/bbox/asset_path、PDF locator 契约。
4. 证据链与定位：
   - A4、A5、A7、D5、D8、E6、E10：独立 evidence refs/source_labels、chunk locator bbox、evidence pack 持久化、引用重叠检测、source_labels 过滤。
5. Discussion 历史与任务中心能力：
   - D10-D18：历史上限、归档、分页、只读查看、搜索、导出、删除、持久化、stream path 兼容。
6. 项目级思维偏置（A-K 后强制接续）：
   - L1-L9：先做治理/回档/成熟方案复核，再做 `project_reasoning_bias` 模型和 CRUD、统一注入注册器、chat/intelligent chat 注入、AnalysisChain/Inspiration 注入、Discussion 单 agent 注入、AI 优化 endpoint、KnowledgeBase UI、OpenAPI + 全链路 smoke。
   - 该计划来源：docs/plans/active/2026-05-28-project-reasoning-bias-injection-plan.md。
   - 完成 L 组每个 slice 后也必须回写本矩阵，不得只更新 active plan。

实现规则：
- Active backend 只在 literature_assistant/core/ 下改。
- API 路由优先兼容现有调用；新增 alias 时保留旧路由，不做破坏性改名。
- Pydantic 模型要有明确类型、边界校验和向后兼容默认值。
- OpenAPI 或前端服务/类型客户端如果受影响，可以更新；但不要改 UI 页面、布局、视觉、浏览器交互。
- 真实凭证、真实 token、runtime credential store 内容、.env 不得打印或写入文档。
- 需要外部服务、生产权限、付费调用、push/tag/release/upload、破坏性删除或产品方向判断时停止并记录 blocker。

验证梯度：
- 每个后端 slice 至少运行 Python compile 或 focused pytest。
- 路由/API slice 用 TestClient 或现有 router tests 验证 schema、错误边界、兼容 alias。
- 影响 OpenAPI/前端类型时运行前端 build 或最小 typecheck/build。
- 建议默认命令：
  - .\.venv-1\Scripts\python.exe -m compileall -q literature_assistant run_literature_assistant.py sitecustomize.py tests\conftest.py
  - .\.venv-1\Scripts\python.exe -m pytest <focused tests> -q
  - npm run build（在 frontend/ 下，仅当改到前端类型、service、generated client 时）
  - git diff --check -- <changed files>

每个 slice 的完成记录：
- 变更文件。
- 对应矩阵 ID。
- 代码定位：文件:行。
- 验证命令与结果。
- 是否改了 docs/plans/2026-05-26-functional-acceptance-matrix.md。
- 回档 checkpoint/snapshot 路径。
- 下一批可继续的矩阵 ID。

继续规则：
- 完成一个 slice 且验证通过后，直接进入下一项 P0/P1 Claude 后端/API slice。
- 如果某项被阻塞，写清 blocker 并转入下一项独立安全任务。
- 不因为一个 slice 完成就停；只在所有授权项完成或触及停止条件时停。
```

### 给 Codex 的接续边界

- Claude 完成后端/API/类型客户端后，Codex 负责 UI、交互、布局和浏览器验收。
- Codex 接手优先顺序：Wiki UI 边界与启用状态、H4 图表管理界面、H3 来源与引用说明和交互、Discussion 历史/凭证选择 UI、Settings 权限与协议可见状态。
- Codex 完成 A-K 中可做的 P0/P1 UI/浏览器验收后，不要停；直接读取 `docs/plans/active/2026-05-28-project-reasoning-bias-injection-plan.md` 并执行 L1-L9 中的 Codex/Codex 接续部分，尤其是 L4/L5/L6 的 toggle 接线、L8 KnowledgeBase 项目偏置栏、L9 浏览器 smoke。
- Codex 每次接手前也必须先对照本矩阵，执行后回写状态、代码定位和浏览器验收结果。

### 本轮参考依据

- Claude Code 官方 common workflows：用于长任务、代码库探索、测试、文档和计划式执行提示词结构。
- Agile Business Consortium MoSCoW prioritisation：用于 P0/P1/P2/P3 的需求优先级口径。
- Requirements traceability matrix 成熟实践：用于把功能、验收、状态、代码定位和验证记录绑定到同一矩阵。
