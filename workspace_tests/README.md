# 测试与评估区

这里放核心代码外侧的测试、评估、诊断和基准资产。

- `evaluation_scripts/`：评估、批处理、验证脚本。
- `evaluation_data/`：评估集、goldset、metrics。
- `benchmarks/`：基准数据和基准报告。
- `diagnostics/`、`scratch/`：本机诊断和临时脚本，已忽略。
- `../tests/legacy_root/`：原根目录散落测试文件。
