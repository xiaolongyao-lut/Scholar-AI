---
description: "Use when asked to fix lint errors recursively, auto-fix Python code style issues, run flake8 and auto-apply fixes, migrate from flake8 to ruff, clean up code quality warnings across all Python files, or bulk-fix formatting and import order issues."
---

# ruff-recursive-fix

This Claude skill is a wrapper around the original repository skill at `.github/skills/ruff-recursive-fix/SKILL.md`.

Before applying the workflow, read the source skill file and any sibling `references/`, `assets/`, `scripts/`, or prompt files it mentions.

Primary source: `.github/skills/ruff-recursive-fix/SKILL.md`

Use the source skill as the authoritative procedure. This wrapper exists so Claude Code can discover and auto-load the skill inside this repository.

