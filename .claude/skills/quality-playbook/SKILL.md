---
description: "Use when asked to run all quality checks, do a pre-commit quality pass, check code health, run the full linting and type-checking pipeline, verify code is ready to merge, or do a comprehensive code quality audit on this Python project."
---

# quality-playbook

This Claude skill is a wrapper around the original repository skill at `.github/skills/quality-playbook/SKILL.md`.

Before applying the workflow, read the source skill file and any sibling `references/`, `assets/`, `scripts/`, or prompt files it mentions.

Primary source: `.github/skills/quality-playbook/SKILL.md`

Use the source skill as the authoritative procedure. This wrapper exists so Claude Code can discover and auto-load the skill inside this repository.

