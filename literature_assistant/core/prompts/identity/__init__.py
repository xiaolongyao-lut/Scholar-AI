"""Identity injection package.

Files in this package are loaded by ``identity_renderer`` to assemble the
prompt header that precedes each AI-entry task template. Wording follows the
template-layer hard guard locked in D-ID-P0-2: prefer ``默认 / 若 / 跟随``
phrasing; avoid declarative ``必须 / 应该 / 不允许 / 不要`` commands.
"""
