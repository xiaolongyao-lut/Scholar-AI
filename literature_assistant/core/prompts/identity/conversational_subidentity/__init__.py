"""Conversational sub-identity package.

Each ``<entry_id>.txt`` here is loaded by ``identity_renderer`` when the
entry's ``is_extractive=False``. Wording follows the D-ID-P0-2 hard guard
(prefer ``默认 / 若 / 跟随``).
"""
