# Redirect: Plans Moved

Real plan files now live under `docs/plans/`.

Do not create new plan content in `.kilo/plans/`. Keep this directory only for legacy redirect stubs when an old tool or conversation still references a historical path.
