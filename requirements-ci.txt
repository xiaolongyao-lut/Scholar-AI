# FastAPI runtime stack - required for recovery adapter and testing
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
httpx==0.28.1

# Testing dependencies
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
pytest-mock==3.15.1

# Data processing and ML
requests
urllib3
chromadb>=0.4.0
numpy
pandas
pyarrow
networkx==3.6.1

# Document and configuration
pyyaml
python-docx
Pillow
PyMuPDF==1.27.1

# MCP (Model Context Protocol) — required for MCP integration tests + runtime
# Mirror of requirements-pin.txt; both must move together when bumping.
mcp==1.27.0
fastmcp==3.2.0
anyio==4.13.0
